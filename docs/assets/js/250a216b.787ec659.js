"use strict";(self.webpackChunkpgdoc_02=self.webpackChunkpgdoc_02||[]).push([[1314],{28453:(e,n,s)=>{s.d(n,{R:()=>o,x:()=>r});var i=s(96540);const t={},a=i.createContext(t);function o(e){const n=i.useContext(a);return i.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function r(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(t):e.components||t:o(e.components),i.createElement(a.Provider,{value:n},e.children)}},68477:(e,n,s)=>{s.r(n),s.d(n,{assets:()=>d,contentTitle:()=>r,default:()=>h,frontMatter:()=>o,metadata:()=>i,toc:()=>l});const i=JSON.parse('{"id":"II. The SQL Language/Indexes/indexes-index-only-scans","title":"11.9. Index-Only Scans and Covering Indexes","description":"Supported versions: 13 / 14 / 15 / 16 / 17","source":"@site/docs/II. The SQL Language/11. Indexes/indexes-index-only-scans.md","sourceDirName":"II. The SQL Language/11. Indexes","slug":"/II. The SQL Language/Indexes/indexes-index-only-scans","permalink":"/demo_docu/II. The SQL Language/Indexes/indexes-index-only-scans","draft":false,"unlisted":false,"tags":[],"version":"current","frontMatter":{"title":"11.9. Index-Only Scans and Covering Indexes"},"sidebar":"autoGeneratedSidebar","previous":{"title":"11.7. Indexes on Expressions","permalink":"/demo_docu/II. The SQL Language/Indexes/indexes-expressional"},"next":{"title":"11.1. Introduction","permalink":"/demo_docu/II. The SQL Language/Indexes/indexes-intro"}}');var t=s(74848),a=s(28453);const o={title:"11.9. Index-Only Scans and Covering Indexes"},r=void 0,d={},l=[];function c(e){const n={a:"a",code:"code",em:"em",hr:"hr",li:"li",ol:"ol",p:"p",pre:"pre",...(0,a.R)(),...e.components};return(0,t.jsxs)(t.Fragment,{children:[(0,t.jsx)(n.p,{children:"Supported versions: 13 / 14 / 15 / 16 / 17"}),"\n",(0,t.jsx)(n.p,{children:"Development versions: 18 / devel"}),"\n",(0,t.jsx)(n.p,{children:"Unsupported versions: 10 / 11 / 12 / 7.1 / 7.2 / 7.3 / 7.4 / 8.0 / 8.1 / 8.2 / 8.3 / 8.4 / 9.0 / 9.1 / 9.2 / 9.3 / 9.4 / 9.5 / 9.6"}),"\n",(0,t.jsx)(n.hr,{}),"\n",(0,t.jsxs)(n.p,{children:["All indexes in PostgreSQL are ",(0,t.jsx)(n.em,{children:"secondary"})," indexes, meaning that each index is stored separately from the table's main data area (which is called the table's ",(0,t.jsx)(n.em,{children:"heap"})," in PostgreSQL terminology). This means that in an ordinary index scan, each row retrieval requires fetching data from both the index and the heap. Furthermore, while the index entries that match a given indexable ",(0,t.jsx)(n.code,{children:"WHERE"})," condition are usually close together in the index, the table rows they reference might be anywhere in the heap. The heap-access portion of an index scan thus involves a lot of random access into the heap, which can be slow, particularly on traditional rotating media. (As described in ",(0,t.jsx)(n.a,{href:"/demo_docu/II.%20The%20SQL%20Language/Indexes/indexes-bitmap-scans",children:"Section 11.5"}),", bitmap scans try to alleviate this cost by doing the heap accesses in sorted order, but that only goes so far.)"]}),"\n",(0,t.jsxs)(n.p,{children:["To solve this performance problem, PostgreSQL supports ",(0,t.jsx)(n.em,{children:"index-only scans"})," , which can answer queries from an index alone without any heap access. The basic idea is to return values directly out of each index entry instead of consulting the associated heap entry. There are two fundamental restrictions on when this method can be used:"]}),"\n",(0,t.jsxs)(n.ol,{children:["\n",(0,t.jsxs)(n.li,{children:["\n",(0,t.jsx)(n.p,{children:"The index type must support index-only scans. B-tree indexes always do. GiST and SP-GiST indexes support index-only scans for some operator classes but not others. Other index types have no support. The underlying requirement is that the index must physically store, or else be able to reconstruct, the original data value for each index entry. As a counterexample, GIN indexes cannot support index-only scans because each index entry typically holds only part of the original data value."}),"\n"]}),"\n",(0,t.jsxs)(n.li,{children:["\n",(0,t.jsxs)(n.p,{children:["The query must reference only columns stored in the index. For example, given an index on columns ",(0,t.jsx)(n.code,{children:"x"})," and ",(0,t.jsx)(n.code,{children:"y"})," of a table that also has a column ",(0,t.jsx)(n.code,{children:"z"}),", these queries could use index-only scans:"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{children:"SELECT x, y FROM tab WHERE x = 'key';\nSELECT x FROM tab WHERE x = 'key' AND y < 42;\n"})}),"\n"]}),"\n"]}),"\n",(0,t.jsx)(n.p,{children:"but these queries could not:"}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{children:"     SELECT x, z FROM tab WHERE x = 'key';\n     SELECT x FROM tab WHERE x = 'key' AND z < 42;\n     \n"})}),"\n",(0,t.jsx)(n.p,{children:"(Expression indexes and partial indexes complicate this rule, as discussed below.)"}),"\n",(0,t.jsxs)(n.p,{children:["If these two fundamental requirements are met, then all the data values required by the query are available from the index, so an index-only scan is physically possible. But there is an additional requirement for any table scan in PostgreSQL: it must verify that each retrieved row be \u201cvisible\u201d to the query's MVCC snapshot, as discussed in ",(0,t.jsx)(n.a,{href:"/demo_docu/II.%20The%20SQL%20Language/Concurrency%20Control/",children:"Chapter 13"}),". Visibility information is not stored in index entries, only in heap entries; so at first glance it would seem that every row retrieval would require a heap access anyway. And this is indeed the case, if the table row has been modified recently. However, for seldom-changing data there is a way around this problem. PostgreSQL tracks, for each page in a table's heap, whether all rows stored in that page are old enough to be visible to all current and future transactions. This information is stored in a bit in the table's ",(0,t.jsx)(n.em,{children:"visibility map"}),". An index-only scan, after finding a candidate index entry, checks the visibility map bit for the corresponding heap page. If it's set, the row is known visible and so the data can be returned with no further work. If it's not set, the heap entry must be visited to find out whether it's visible, so no performance advantage is gained over a standard index scan. Even in the successful case, this approach trades visibility map accesses for heap accesses; but since the visibility map is four orders of magnitude smaller than the heap it describes, far less physical I/O is needed to access it. In most situations the visibility map remains cached in memory all the time."]}),"\n",(0,t.jsx)(n.p,{children:"In short, while an index-only scan is possible given the two fundamental requirements, it will be a win only if a significant fraction of the table's heap pages have their all-visible map bits set. But tables in which a large fraction of the rows are unchanging are common enough to make this type of scan very useful in practice."}),"\n",(0,t.jsxs)(n.p,{children:["To make effective use of the index-only scan feature, you might choose to create a ",(0,t.jsx)(n.em,{children:"covering index"})," , which is an index specifically designed to include the columns needed by a particular type of query that you run frequently. Since queries typically need to retrieve more columns than just the ones they search on, PostgreSQL allows you to create an index in which some columns are just \u201cpayload\u201d and are not part of the search key. This is done by adding an ",(0,t.jsx)(n.code,{children:"INCLUDE"})," clause listing the extra columns. For example, if you commonly run queries like"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{children:"SELECT y FROM tab WHERE x = 'key';\n"})}),"\n",(0,t.jsxs)(n.p,{children:["the traditional approach to speeding up such queries would be to create an index on ",(0,t.jsx)(n.code,{children:"x"})," only. However, an index defined as"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{children:"CREATE INDEX tab_x_y ON tab(x) INCLUDE (y);\n"})}),"\n",(0,t.jsxs)(n.p,{children:["could handle these queries as index-only scans, because ",(0,t.jsx)(n.code,{children:"y"})," can be obtained from the index without visiting the heap."]}),"\n",(0,t.jsxs)(n.p,{children:["Because column ",(0,t.jsx)(n.code,{children:"y"})," is not part of the index's search key, it does not have to be of a data type that the index can handle; it's merely stored in the index and is not interpreted by the index machinery. Also, if the index is a unique index, that is"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{children:"CREATE UNIQUE INDEX tab_x_y ON tab(x) INCLUDE (y);\n"})}),"\n",(0,t.jsxs)(n.p,{children:["the uniqueness condition applies to just column ",(0,t.jsx)(n.code,{children:"x"}),", not to the combination of ",(0,t.jsx)(n.code,{children:"x"})," and ",(0,t.jsx)(n.code,{children:"y"}),". (An ",(0,t.jsx)(n.code,{children:"INCLUDE"})," clause can also be written in ",(0,t.jsx)(n.code,{children:"UNIQUE"})," and ",(0,t.jsx)(n.code,{children:"PRIMARY KEY"})," constraints, providing alternative syntax for setting up an index like this.)"]}),"\n",(0,t.jsx)(n.p,{children:"It's wise to be conservative about adding non-key payload columns to an index, especially wide columns. If an index tuple exceeds the maximum size allowed for the index type, data insertion will fail. In any case, non-key columns duplicate data from the index's table and bloat the size of the index, thus potentially slowing searches. And remember that there is little point in including payload columns in an index unless the table changes slowly enough that an index-only scan is likely to not need to access the heap. If the heap tuple must be visited anyway, it costs nothing more to get the column's value from there. Other restrictions are that expressions are not currently supported as included columns, and that only B-tree, GiST and SP-GiST indexes currently support included columns."}),"\n",(0,t.jsxs)(n.p,{children:["Before PostgreSQL had the ",(0,t.jsx)(n.code,{children:"INCLUDE"})," feature, people sometimes made covering indexes by writing the payload columns as ordinary index columns, that is writing"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{children:"CREATE INDEX tab_x_y ON tab(x, y);\n"})}),"\n",(0,t.jsxs)(n.p,{children:["even though they had no intention of ever using ",(0,t.jsx)(n.code,{children:"y"})," as part of a ",(0,t.jsx)(n.code,{children:"WHERE"})," clause. This works fine as long as the extra columns are trailing columns; making them be leading columns is unwise for the reasons explained in ",(0,t.jsx)(n.a,{href:"/demo_docu/II.%20The%20SQL%20Language/Indexes/indexes-multicolumn",children:"Section 11.3"}),". However, this method doesn't support the case where you want the index to enforce uniqueness on the key column(s)."]}),"\n",(0,t.jsxs)(n.p,{children:[(0,t.jsx)(n.em,{children:"Suffix truncation"})," always removes non-key columns from upper B-Tree levels. As payload columns, they are never used to guide index scans. The truncation process also removes one or more trailing key column(s) when the remaining prefix of key column(s) happens to be sufficient to describe tuples on the lowest B-Tree level. In practice, covering indexes without an ",(0,t.jsx)(n.code,{children:"INCLUDE"})," clause often avoid storing columns that are effectively payload in the upper levels. However, explicitly defining payload columns as non-key columns ",(0,t.jsx)(n.em,{children:"reliably"})," keeps the tuples in upper levels small."]}),"\n",(0,t.jsxs)(n.p,{children:["In principle, index-only scans can be used with expression indexes. For example, given an index on ",(0,t.jsx)(n.code,{children:"f(x)"})," where ",(0,t.jsx)(n.code,{children:"x"})," is a table column, it should be possible to execute"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{children:"SELECT f(x) FROM tab WHERE f(x) < 1;\n"})}),"\n",(0,t.jsxs)(n.p,{children:["as an index-only scan; and this is very attractive if ",(0,t.jsx)(n.code,{children:"f()"})," is an expensive-to-compute function. However, PostgreSQL's planner is currently not very smart about such cases. It considers a query to be potentially executable by index-only scan only when all ",(0,t.jsx)(n.em,{children:"columns"})," needed by the query are available from the index. In this example, ",(0,t.jsx)(n.code,{children:"x"})," is not needed except in the context ",(0,t.jsx)(n.code,{children:"f(x)"}),", but the planner does not notice that and concludes that an index-only scan is not possible. If an index-only scan seems sufficiently worthwhile, this can be worked around by adding ",(0,t.jsx)(n.code,{children:"x"})," as an included column, for example"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{children:"CREATE INDEX tab_f_x ON tab (f(x)) INCLUDE (x);\n"})}),"\n",(0,t.jsxs)(n.p,{children:["An additional caveat, if the goal is to avoid recalculating ",(0,t.jsx)(n.code,{children:"f(x)"}),", is that the planner won't necessarily match uses of ",(0,t.jsx)(n.code,{children:"f(x)"})," that aren't in indexable ",(0,t.jsx)(n.code,{children:"WHERE"})," clauses to the index column. It will usually get this right in simple queries such as shown above, but not in queries that involve joins. These deficiencies may be remedied in future versions of PostgreSQL."]}),"\n",(0,t.jsxs)(n.p,{children:["Partial indexes also have interesting interactions with index-only scans. Consider the partial index shown in ",(0,t.jsx)(n.a,{href:"indexes-partial.html#INDEXES-PARTIAL-EX3",title:"Example\xa011.3.\xa0Setting up a Partial Unique Index",children:"Example 11.3"}),":"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{children:"CREATE UNIQUE INDEX tests_success_constraint ON tests (subject, target)\n    WHERE success;\n"})}),"\n",(0,t.jsx)(n.p,{children:"In principle, we could do an index-only scan on this index to satisfy a query like"}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{children:"SELECT target FROM tests WHERE subject = 'some-subject' AND success;\n"})}),"\n",(0,t.jsxs)(n.p,{children:["But there's a problem: the ",(0,t.jsx)(n.code,{children:"WHERE"})," clause refers to ",(0,t.jsx)(n.code,{children:"success"})," which is not available as a result column of the index. Nonetheless, an index-only scan is possible because the plan does not need to recheck that part of the ",(0,t.jsx)(n.code,{children:"WHERE"})," clause at run time: all entries found in the index necessarily have ",(0,t.jsx)(n.code,{children:"success = true"})," so this need not be explicitly checked in the plan. PostgreSQL versions 9.6 and later will recognize such cases and allow index-only scans to be generated, but older versions will not."]})]})}function h(e={}){const{wrapper:n}={...(0,a.R)(),...e.components};return n?(0,t.jsx)(n,{...e,children:(0,t.jsx)(c,{...e})}):c(e)}}}]);