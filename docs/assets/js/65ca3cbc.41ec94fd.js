"use strict";(self.webpackChunkpgdoc_02=self.webpackChunkpgdoc_02||[]).push([[78003],{28453:(e,t,a)=>{a.d(t,{R:()=>r,x:()=>o});var n=a(96540);const i={},s=n.createContext(i);function r(e){const t=n.useContext(s);return n.useMemo(function(){return"function"==typeof e?e(t):{...t,...e}},[t,e])}function o(e){let t;return t=e.disableParentContext?"function"==typeof e.components?e.components(i):e.components||i:r(e.components),n.createElement(s.Provider,{value:t},e.children)}},43128:(e,t,a)=>{a.r(t),a.d(t,{assets:()=>c,contentTitle:()=>o,default:()=>h,frontMatter:()=>r,metadata:()=>n,toc:()=>l});const n=JSON.parse('{"id":"III. Server Administration/Routine Database Maintenance Tasks/routine-vacuuming","title":"25.1.\xa0Routine Vacuuming","description":"Supported Versions:","source":"@site/docs/III. Server Administration/25. Routine Database Maintenance Tasks/routine-vacuuming.md","sourceDirName":"III. Server Administration/25. Routine Database Maintenance Tasks","slug":"/III. Server Administration/Routine Database Maintenance Tasks/routine-vacuuming","permalink":"/III. Server Administration/Routine Database Maintenance Tasks/routine-vacuuming","draft":false,"unlisted":false,"tags":[],"version":"current","frontMatter":{"title":"25.1.\xa0Routine Vacuuming"},"sidebar":"autoGeneratedSidebar","previous":{"title":"25.2.\xa0Routine Reindexing","permalink":"/III. Server Administration/Routine Database Maintenance Tasks/routine-reindex"},"next":{"title":"Chapter\xa026.\xa0Backup and Restore","permalink":"/III. Server Administration/Backup and Restore/"}}');var i=a(74848),s=a(28453);const r={title:"25.1.\xa0Routine Vacuuming"},o=void 0,c={},l=[{value:"25.1.\xa0Routine Vacuuming #",id:"251routine-vacuuming-",level:2},{value:"25.1.1. Vacuuming Basics",id:"2511-vacuuming-basics",level:3},{value:"25.1.2. Recovering Disk Space",id:"2512-recovering-disk-space",level:3},{value:"Tip",id:"tip",level:3},{value:"Tip",id:"tip-1",level:3},{value:"25.1.3. Updating Planner Statistics",id:"2513-updating-planner-statistics",level:3},{value:"Tip",id:"tip-2",level:3},{value:"Tip",id:"tip-3",level:3},{value:"Tip",id:"tip-4",level:3},{value:"25.1.4. Updating the Visibility Map",id:"2514-updating-the-visibility-map",level:3},{value:"25.1.5. Preventing Transaction ID Wraparound Failures",id:"2515-preventing-transaction-id-wraparound-failures",level:3},{value:"Note",id:"note",level:3},{value:"Tip",id:"tip-5",level:3},{value:"Note",id:"note-1",level:3},{value:"25.1.5.1. Multixacts and Wraparound",id:"25151-multixacts-and-wraparound",level:4},{value:"25.1.6. The Autovacuum Daemon",id:"2516-the-autovacuum-daemon",level:3},{value:"Warning",id:"warning",level:3},{value:"Submit correction",id:"submit-correction",level:2}];function d(e){const t={a:"a",code:"code",em:"em",h2:"h2",h3:"h3",h4:"h4",hr:"hr",li:"li",ol:"ol",p:"p",pre:"pre",table:"table",tbody:"tbody",td:"td",th:"th",thead:"thead",tr:"tr",...(0,s.R)(),...e.components};return(0,i.jsxs)(i.Fragment,{children:[(0,i.jsx)(t.p,{children:"Supported Versions:\r\n17\r\n/\r\n16\r\n/\r\n15\r\n/\r\n14\r\n/\r\n13"}),"\n",(0,i.jsx)(t.p,{children:"Development Versions:\r\n18\r\n/\r\ndevel"}),"\n",(0,i.jsx)(t.p,{children:"Unsupported versions:\r\n12\r\n/\r\n11\r\n/\r\n10\r\n/\r\n9.6\r\n/\r\n9.5\r\n/\r\n9.4\r\n/\r\n9.3\r\n/\r\n9.2\r\n/\r\n9.1\r\n/\r\n9.0\r\n/\r\n8.4\r\n/\r\n8.3\r\n/\r\n8.2\r\n/\r\n7.3\r\n/\r\n7.2"}),"\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n",(0,i.jsxs)(t.table,{children:[(0,i.jsx)(t.thead,{children:(0,i.jsxs)(t.tr,{children:[(0,i.jsx)(t.th,{children:"25.1.\xa0Routine Vacuuming"}),(0,i.jsx)(t.th,{}),(0,i.jsx)(t.th,{}),(0,i.jsx)(t.th,{}),(0,i.jsx)(t.th,{})]})}),(0,i.jsx)(t.tbody,{children:(0,i.jsxs)(t.tr,{children:[(0,i.jsx)(t.td,{children:(0,i.jsx)(t.a,{href:"maintenance.html",title:"Chapter\xa025.\xa0Routine Database Maintenance Tasks",children:"Prev"})}),(0,i.jsx)(t.td,{children:(0,i.jsx)(t.a,{href:"maintenance.html",title:"Chapter\xa025.\xa0Routine Database Maintenance Tasks",children:"Up"})}),(0,i.jsx)(t.td,{children:"Chapter\xa025.\xa0Routine Database Maintenance Tasks"}),(0,i.jsx)(t.td,{children:(0,i.jsx)(t.a,{href:"index.html",title:"PostgreSQL 16.9 Documentation",children:"Home"})}),(0,i.jsx)(t.td,{children:(0,i.jsx)(t.a,{href:"routine-reindex.html",title:"25.2.\xa0Routine Reindexing",children:"Next"})})]})})]}),"\n",(0,i.jsx)(t.hr,{}),"\n",(0,i.jsxs)(t.h2,{id:"251routine-vacuuming-",children:["25.1.\xa0Routine Vacuuming ",(0,i.jsx)(t.a,{href:"#ROUTINE-VACUUMING",children:"#"})]}),"\n",(0,i.jsx)(t.p,{children:(0,i.jsx)(t.a,{href:"#2511-vacuuming-basics",children:"25.1.1. Vacuuming Basics"})}),"\n",(0,i.jsx)(t.p,{children:(0,i.jsx)(t.a,{href:"#2512-recovering-disk-space",children:"25.1.2. Recovering Disk Space"})}),"\n",(0,i.jsx)(t.p,{children:(0,i.jsx)(t.a,{href:"#2513-updating-planner-statistics",children:"25.1.3. Updating Planner Statistics"})}),"\n",(0,i.jsx)(t.p,{children:(0,i.jsx)(t.a,{href:"#2514-updating-the-visibility-map",children:"25.1.4. Updating the Visibility Map"})}),"\n",(0,i.jsx)(t.p,{children:(0,i.jsx)(t.a,{href:"#2515-preventing-transaction-id-wraparound-failures",children:"25.1.5. Preventing Transaction ID Wraparound Failures"})}),"\n",(0,i.jsx)(t.p,{children:(0,i.jsx)(t.a,{href:"#2516-the-autovacuum-daemon",children:"25.1.6. The Autovacuum Daemon"})}),"\n",(0,i.jsxs)(t.p,{children:["PostgreSQL databases require periodic maintenance known as ",(0,i.jsx)(t.em,{children:"vacuuming"}),". For many installations, it is sufficient to let vacuuming be performed by the ",(0,i.jsx)(t.em,{children:"autovacuum daemon"}),", which is described in ",(0,i.jsx)(t.a,{href:"routine-vacuuming.html#AUTOVACUUM",title:"25.1.6.\xa0The Autovacuum Daemon",children:"Section\xa025.1.6"}),". You might need to adjust the autovacuuming parameters described there to obtain best results for your situation. Some database administrators will want to supplement or replace the daemon's activities with manually-managed ",(0,i.jsx)(t.code,{children:"VACUUM"})," commands, which typically are executed according to a schedule by cron or Task Scheduler scripts. To set up manually-managed vacuuming properly, it is essential to understand the issues discussed in the next few subsections. Administrators who rely on autovacuuming may still wish to skim this material to help them understand and adjust autovacuuming."]}),"\n",(0,i.jsx)(t.h3,{id:"2511-vacuuming-basics",children:"25.1.1. Vacuuming Basics"}),"\n",(0,i.jsxs)(t.p,{children:["PostgreSQL's ",(0,i.jsx)(t.a,{href:"sql-vacuum.html",title:"VACUUM",children:(0,i.jsx)(t.code,{children:"VACUUM"})})," command has to process each table on a regular basis for several reasons:"]}),"\n",(0,i.jsxs)(t.ol,{children:["\n",(0,i.jsx)(t.li,{children:"To recover or reuse disk space occupied by updated or deleted rows."}),"\n",(0,i.jsx)(t.li,{children:"To update data statistics used by the PostgreSQL query planner."}),"\n",(0,i.jsxs)(t.li,{children:["To update the visibility map, which speeds up ",(0,i.jsx)(t.a,{href:"indexes-index-only-scans.html",title:"11.9.\xa0Index-Only Scans and Covering Indexes",children:"index-only scans"}),"."]}),"\n",(0,i.jsxs)(t.li,{children:["To protect against loss of very old data due to ",(0,i.jsx)(t.em,{children:"transaction ID wraparound"})," or ",(0,i.jsx)(t.em,{children:"multixact ID wraparound"}),"."]}),"\n"]}),"\n",(0,i.jsxs)(t.p,{children:["Each of these reasons dictates performing ",(0,i.jsx)(t.code,{children:"VACUUM"})," operations of varying frequency and scope, as explained in the following subsections."]}),"\n",(0,i.jsxs)(t.p,{children:["There are two variants of ",(0,i.jsx)(t.code,{children:"VACUUM"}),": standard ",(0,i.jsx)(t.code,{children:"VACUUM"})," and ",(0,i.jsx)(t.code,{children:"VACUUM FULL"}),". ",(0,i.jsx)(t.code,{children:"VACUUM FULL"})," can reclaim more disk space but runs much more slowly. Also, the standard form of ",(0,i.jsx)(t.code,{children:"VACUUM"})," can run in parallel with production database operations. (Commands such as ",(0,i.jsx)(t.code,{children:"SELECT"}),", ",(0,i.jsx)(t.code,{children:"INSERT"}),", ",(0,i.jsx)(t.code,{children:"UPDATE"}),", and ",(0,i.jsx)(t.code,{children:"DELETE"})," will continue to function normally, though you will not be able to modify the definition of a table with commands such as ",(0,i.jsx)(t.code,{children:"ALTER TABLE"})," while it is being vacuumed.) ",(0,i.jsx)(t.code,{children:"VACUUM FULL"})," requires an ",(0,i.jsx)(t.code,{children:"ACCESS EXCLUSIVE"})," lock on the table it is working on, and therefore cannot be done in parallel with other use of the table. Generally, therefore, administrators should strive to use standard ",(0,i.jsx)(t.code,{children:"VACUUM"})," and avoid ",(0,i.jsx)(t.code,{children:"VACUUM FULL"}),"."]}),"\n",(0,i.jsxs)(t.p,{children:[(0,i.jsx)(t.code,{children:"VACUUM"})," creates a substantial amount of I/O traffic, which can cause poor performance for other active sessions. There are configuration parameters that can be adjusted to reduce the performance impact of background vacuuming \u2014 see ",(0,i.jsx)(t.a,{href:"runtime-config-resource.html#RUNTIME-CONFIG-RESOURCE-VACUUM-COST",title:"20.4.4.\xa0Cost-based Vacuum Delay",children:"Section\xa020.4.4"}),"."]}),"\n",(0,i.jsx)(t.h3,{id:"2512-recovering-disk-space",children:"25.1.2. Recovering Disk Space"}),"\n",(0,i.jsxs)(t.p,{children:["In PostgreSQL, an ",(0,i.jsx)(t.code,{children:"UPDATE"})," or ",(0,i.jsx)(t.code,{children:"DELETE"})," of a row does not immediately remove the old version of the row. This approach is necessary to gain the benefits of multiversion concurrency control (MVCC, see ",(0,i.jsx)(t.a,{href:"mvcc.html",title:"Chapter\xa013.\xa0Concurrency Control",children:"Chapter\xa013"}),"): the row version must not be deleted while it is still potentially visible to other transactions. But eventually, an outdated or deleted row version is no longer of interest to any transaction. The space it occupies must then be reclaimed for reuse by new rows, to avoid unbounded growth of disk space requirements. This is done by running ",(0,i.jsx)(t.code,{children:"VACUUM"}),"."]}),"\n",(0,i.jsxs)(t.p,{children:["The standard form of ",(0,i.jsx)(t.code,{children:"VACUUM"})," removes dead row versions in tables and indexes and marks the space available for future reuse. However, it will not return the space to the operating system, except in the special case where one or more pages at the end of a table become entirely free and an exclusive table lock can be easily obtained. In contrast, ",(0,i.jsx)(t.code,{children:"VACUUM FULL"})," actively compacts tables by writing a complete new version of the table file with no dead space. This minimizes the size of the table, but can take a long time. It also requires extra disk space for the new copy of the table, until the operation completes."]}),"\n",(0,i.jsxs)(t.p,{children:["The usual goal of routine vacuuming is to do standard ",(0,i.jsx)(t.code,{children:"VACUUM"}),"s often enough to avoid needing ",(0,i.jsx)(t.code,{children:"VACUUM FULL"}),". The autovacuum daemon attempts to work this way, and in fact will never issue ",(0,i.jsx)(t.code,{children:"VACUUM FULL"}),". In this approach, the idea is not to keep tables at their minimum size, but to maintain steady-state usage of disk space: each table occupies space equivalent to its minimum size plus however much space gets used up between vacuum runs. Although ",(0,i.jsx)(t.code,{children:"VACUUM FULL"})," can be used to shrink a table back to its minimum size and return the disk space to the operating system, there is not much point in this if the table will just grow again in the future. Thus, moderately-frequent standard ",(0,i.jsx)(t.code,{children:"VACUUM"})," runs are a better approach than infrequent ",(0,i.jsx)(t.code,{children:"VACUUM FULL"})," runs for maintaining heavily-updated tables."]}),"\n",(0,i.jsxs)(t.p,{children:["Some administrators prefer to schedule vacuuming themselves, for example doing all the work at night when load is low. The difficulty with doing vacuuming according to a fixed schedule is that if a table has an unexpected spike in update activity, it may get bloated to the point that ",(0,i.jsx)(t.code,{children:"VACUUM FULL"})," is really necessary to reclaim space. Using the autovacuum daemon alleviates this problem, since the daemon schedules vacuuming dynamically in response to update activity. It is unwise to disable the daemon completely unless you have an extremely predictable workload. One possible compromise is to set the daemon's parameters so that it will only react to unusually heavy update activity, thus keeping things from getting out of hand, while scheduled ",(0,i.jsx)(t.code,{children:"VACUUM"}),"s are expected to do the bulk of the work when the load is typical."]}),"\n",(0,i.jsxs)(t.p,{children:["For those not using autovacuum, a typical approach is to schedule a database-wide ",(0,i.jsx)(t.code,{children:"VACUUM"})," once a day during a low-usage period, supplemented by more frequent vacuuming of heavily-updated tables as necessary. (Some installations with extremely high update rates vacuum their busiest tables as often as once every few minutes.) If you have multiple databases in a cluster, don't forget to ",(0,i.jsx)(t.code,{children:"VACUUM"})," each one; the program ",(0,i.jsx)(t.a,{href:"app-vacuumdb.html",title:"vacuumdb",children:"vacuumdb"})," might be helpful."]}),"\n",(0,i.jsx)(t.h3,{id:"tip",children:"Tip"}),"\n",(0,i.jsxs)(t.p,{children:["Plain ",(0,i.jsx)(t.code,{children:"VACUUM"})," may not be satisfactory when a table contains large numbers of dead row versions as a result of massive update or delete activity. If you have such a table and you need to reclaim the excess disk space it occupies, you will need to use ",(0,i.jsx)(t.code,{children:"VACUUM FULL"}),", or alternatively ",(0,i.jsx)(t.a,{href:"sql-cluster.html",title:"CLUSTER",children:(0,i.jsx)(t.code,{children:"CLUSTER"})})," or one of the table-rewriting variants of ",(0,i.jsx)(t.a,{href:"sql-altertable.html",title:"ALTER TABLE",children:(0,i.jsx)(t.code,{children:"ALTER TABLE"})}),". These commands rewrite an entire new copy of the table and build new indexes for it. All these options require an ",(0,i.jsx)(t.code,{children:"ACCESS EXCLUSIVE"})," lock. Note that they also temporarily use extra disk space approximately equal to the size of the table, since the old copies of the table and indexes can't be released until the new ones are complete."]}),"\n",(0,i.jsx)(t.h3,{id:"tip-1",children:"Tip"}),"\n",(0,i.jsxs)(t.p,{children:["If you have a table whose entire contents are deleted on a periodic basis, consider doing it with ",(0,i.jsx)(t.a,{href:"sql-truncate.html",title:"TRUNCATE",children:(0,i.jsx)(t.code,{children:"TRUNCATE"})})," rather than using ",(0,i.jsx)(t.code,{children:"DELETE"})," followed by ",(0,i.jsx)(t.code,{children:"VACUUM"}),". ",(0,i.jsx)(t.code,{children:"TRUNCATE"})," removes the entire content of the table immediately, without requiring a subsequent ",(0,i.jsx)(t.code,{children:"VACUUM"})," or ",(0,i.jsx)(t.code,{children:"VACUUM FULL"})," to reclaim the now-unused disk space. The disadvantage is that strict MVCC semantics are violated."]}),"\n",(0,i.jsx)(t.h3,{id:"2513-updating-planner-statistics",children:"25.1.3. Updating Planner Statistics"}),"\n",(0,i.jsxs)(t.p,{children:["The PostgreSQL query planner relies on statistical information about the contents of tables in order to generate good plans for queries. These statistics are gathered by the ",(0,i.jsx)(t.a,{href:"sql-analyze.html",title:"ANALYZE",children:(0,i.jsx)(t.code,{children:"ANALYZE"})})," command, which can be invoked by itself or as an optional step in ",(0,i.jsx)(t.code,{children:"VACUUM"}),". It is important to have reasonably accurate statistics, otherwise poor choices of plans might degrade database performance."]}),"\n",(0,i.jsxs)(t.p,{children:["The autovacuum daemon, if enabled, will automatically issue ",(0,i.jsx)(t.code,{children:"ANALYZE"})," commands whenever the content of a table has changed sufficiently. However, administrators might prefer to rely on manually-scheduled ",(0,i.jsx)(t.code,{children:"ANALYZE"})," operations, particularly if it is known that update activity on a table will not affect the statistics of \u201cinteresting\u201d columns. The daemon schedules ",(0,i.jsx)(t.code,{children:"ANALYZE"})," strictly as a function of the number of rows inserted or updated; it has no knowledge of whether that will lead to meaningful statistical changes."]}),"\n",(0,i.jsxs)(t.p,{children:["Tuples changed in partitions and inheritance children do not trigger analyze on the parent table. If the parent table is empty or rarely changed, it may never be processed by autovacuum, and the statistics for the inheritance tree as a whole won't be collected. It is necessary to run ",(0,i.jsx)(t.code,{children:"ANALYZE"})," on the parent table manually in order to keep the statistics up to date."]}),"\n",(0,i.jsxs)(t.p,{children:["As with vacuuming for space recovery, frequent updates of statistics are more useful for heavily-updated tables than for seldom-updated ones. But even for a heavily-updated table, there might be no need for statistics updates if the statistical distribution of the data is not changing much. A simple rule of thumb is to think about how much the minimum and maximum values of the columns in the table change. For example, a ",(0,i.jsx)(t.code,{children:"timestamp"})," column that contains the time of row update will have a constantly-increasing maximum value as rows are added and updated; such a column will probably need more frequent statistics updates than, say, a column containing URLs for pages accessed on a website. The URL column might receive changes just as often, but the statistical distribution of its values probably changes relatively slowly."]}),"\n",(0,i.jsxs)(t.p,{children:["It is possible to run ",(0,i.jsx)(t.code,{children:"ANALYZE"})," on specific tables and even just specific columns of a table, so the flexibility exists to update some statistics more frequently than others if your application requires it. In practice, however, it is usually best to just analyze the entire database, because it is a fast operation. ",(0,i.jsx)(t.code,{children:"ANALYZE"})," uses a statistically random sampling of the rows of a table rather than reading every single row."]}),"\n",(0,i.jsx)(t.h3,{id:"tip-2",children:"Tip"}),"\n",(0,i.jsxs)(t.p,{children:["Although per-column tweaking of ",(0,i.jsx)(t.code,{children:"ANALYZE"})," frequency might not be very productive, you might find it worthwhile to do per-column adjustment of the level of detail of the statistics collected by ",(0,i.jsx)(t.code,{children:"ANALYZE"}),". Columns that are heavily used in ",(0,i.jsx)(t.code,{children:"WHERE"})," clauses and have highly irregular data distributions might require a finer-grain data histogram than other columns. See ",(0,i.jsx)(t.code,{children:"ALTER TABLE SET STATISTICS"}),", or change the database-wide default using the ",(0,i.jsx)(t.a,{href:"runtime-config-query.html#GUC-DEFAULT-STATISTICS-TARGET",children:"default_statistics_target"})," configuration parameter."]}),"\n",(0,i.jsx)(t.p,{children:"Also, by default there is limited information available about the selectivity of functions. However, if you create a statistics object or an expression index that uses a function call, useful statistics will be gathered about the function, which can greatly improve query plans that use the expression index."}),"\n",(0,i.jsx)(t.h3,{id:"tip-3",children:"Tip"}),"\n",(0,i.jsxs)(t.p,{children:["The autovacuum daemon does not issue ",(0,i.jsx)(t.code,{children:"ANALYZE"})," commands for foreign tables, since it has no means of determining how often that might be useful. If your queries require statistics on foreign tables for proper planning, it's a good idea to run manually-managed ",(0,i.jsx)(t.code,{children:"ANALYZE"})," commands on those tables on a suitable schedule."]}),"\n",(0,i.jsx)(t.h3,{id:"tip-4",children:"Tip"}),"\n",(0,i.jsxs)(t.p,{children:["The autovacuum daemon does not issue ",(0,i.jsx)(t.code,{children:"ANALYZE"})," commands for partitioned tables. Inheritance parents will only be analyzed if the parent itself is changed - changes to child tables do not trigger autoanalyze on the parent table. If your queries require statistics on parent tables for proper planning, it is necessary to periodically run a manual ",(0,i.jsx)(t.code,{children:"ANALYZE"})," on those tables to keep the statistics up to date."]}),"\n",(0,i.jsx)(t.h3,{id:"2514-updating-the-visibility-map",children:"25.1.4. Updating the Visibility Map"}),"\n",(0,i.jsxs)(t.p,{children:["Vacuum maintains a ",(0,i.jsx)(t.a,{href:"storage-vm.html",title:"73.4.\xa0Visibility Map",children:"visibility map"})," for each table to keep track of which pages contain only tuples that are known to be visible to all active transactions (and all future transactions, until the page is again modified). This has two purposes. First, vacuum itself can skip such pages on the next run, since there is nothing to clean up."]}),"\n",(0,i.jsxs)(t.p,{children:["Second, it allows PostgreSQL to answer some queries using only the index, without reference to the underlying table. Since PostgreSQL indexes don't contain tuple visibility information, a normal index scan fetches the heap tuple for each matching index entry, to check whether it should be seen by the current transaction. An ",(0,i.jsx)(t.a,{href:"indexes-index-only-scans.html",title:"11.9.\xa0Index-Only Scans and Covering Indexes",children:(0,i.jsx)(t.em,{children:"index-only scan"})}),", on the other hand, checks the visibility map first. If it's known that all tuples on the page are visible, the heap fetch can be skipped. This is most useful on large data sets where the visibility map can prevent disk accesses. The visibility map is vastly smaller than the heap, so it can easily be cached even when the heap is very large."]}),"\n",(0,i.jsx)(t.h3,{id:"2515-preventing-transaction-id-wraparound-failures",children:"25.1.5. Preventing Transaction ID Wraparound Failures"}),"\n",(0,i.jsxs)(t.p,{children:["PostgreSQL's ",(0,i.jsx)(t.a,{href:"mvcc-intro.html",title:"13.1.\xa0Introduction",children:"MVCC"})," transaction semantics depend on being able to compare transaction ID (XID) numbers: a row version with an insertion XID greater than the current transaction's XID is \u201cin the future\u201d and should not be visible to the current transaction. But since transaction IDs have limited size (32 bits) a cluster that runs for a long time (more than 4 billion transactions) would suffer ",(0,i.jsx)(t.em,{children:"transaction ID wraparound"}),": the XID counter wraps around to zero, and all of a sudden transactions that were in the past appear to be in the future \u2014 which means their output become invisible. In short, catastrophic data loss. (Actually the data is still there, but that's cold comfort if you cannot get at it.) To avoid this, it is necessary to vacuum every table in every database at least once every two billion transactions."]}),"\n",(0,i.jsxs)(t.p,{children:["The reason that periodic vacuuming solves the problem is that ",(0,i.jsx)(t.code,{children:"VACUUM"})," will mark rows as ",(0,i.jsx)(t.em,{children:"frozen"}),", indicating that they were inserted by a transaction that committed sufficiently far in the past that the effects of the inserting transaction are certain to be visible to all current and future transactions. Normal XIDs are compared using modulo-232 arithmetic. This means that for every normal XID, there are two billion XIDs that are \u201colder\u201d and two billion that are \u201cnewer\u201d; another way to say it is that the normal XID space is circular with no endpoint. Therefore, once a row version has been created with a particular normal XID, the row version will appear to be \u201cin the past\u201d for the next two billion transactions, no matter which normal XID we are talking about. If the row version still exists after more than two billion transactions, it will suddenly appear to be in the future. To prevent this, PostgreSQL reserves a special XID, ",(0,i.jsx)(t.code,{children:"FrozenTransactionId"}),", which does not follow the normal XID comparison rules and is always considered older than every normal XID. Frozen row versions are treated as if the inserting XID were ",(0,i.jsx)(t.code,{children:"FrozenTransactionId"}),", so that they will appear to be \u201cin the past\u201d to all normal transactions regardless of wraparound issues, and so such row versions will be valid until deleted, no matter how long that is."]}),"\n",(0,i.jsx)(t.h3,{id:"note",children:"Note"}),"\n",(0,i.jsxs)(t.p,{children:["In PostgreSQL versions before 9.4, freezing was implemented by actually replacing a row's insertion XID with ",(0,i.jsx)(t.code,{children:"FrozenTransactionId"}),", which was visible in the row's ",(0,i.jsx)(t.code,{children:"xmin"})," system column. Newer versions just set a flag bit, preserving the row's original ",(0,i.jsx)(t.code,{children:"xmin"})," for possible forensic use. However, rows with ",(0,i.jsx)(t.code,{children:"xmin"})," equal to ",(0,i.jsx)(t.code,{children:"FrozenTransactionId"})," (2) may still be found in databases pg_upgrade'd from pre-9.4 versions."]}),"\n",(0,i.jsxs)(t.p,{children:["Also, system catalogs may contain rows with ",(0,i.jsx)(t.code,{children:"xmin"})," equal to ",(0,i.jsx)(t.code,{children:"BootstrapTransactionId"})," (1), indicating that they were inserted during the first phase of initdb. Like ",(0,i.jsx)(t.code,{children:"FrozenTransactionId"}),", this special XID is treated as older than every normal XID."]}),"\n",(0,i.jsxs)(t.p,{children:[(0,i.jsx)(t.a,{href:"runtime-config-client.html#GUC-VACUUM-FREEZE-MIN-AGE",children:"vacuum_freeze_min_age"})," controls how old an XID value has to be before rows bearing that XID will be frozen. Increasing this setting may avoid unnecessary work if the rows that would otherwise be frozen will soon be modified again, but decreasing this setting increases the number of transactions that can elapse before the table must be vacuumed again."]}),"\n",(0,i.jsxs)(t.p,{children:[(0,i.jsx)(t.code,{children:"VACUUM"})," uses the ",(0,i.jsx)(t.a,{href:"storage-vm.html",title:"73.4.\xa0Visibility Map",children:"visibility map"})," to determine which pages of a table must be scanned. Normally, it will skip pages that don't have any dead row versions even if those pages might still have row versions with old XID values. Therefore, normal ",(0,i.jsx)(t.code,{children:"VACUUM"}),"s won't always freeze every old row version in the table. When that happens, ",(0,i.jsx)(t.code,{children:"VACUUM"})," will eventually need to perform an ",(0,i.jsx)(t.em,{children:"aggressive vacuum"}),", which will freeze all eligible unfrozen XID and MXID values, including those from all-visible but not all-frozen pages. In practice most tables require periodic aggressive vacuuming. ",(0,i.jsx)(t.a,{href:"runtime-config-client.html#GUC-VACUUM-FREEZE-TABLE-AGE",children:"vacuum_freeze_table_age"})," controls when ",(0,i.jsx)(t.code,{children:"VACUUM"})," does that: all-visible but not all-frozen pages are scanned if the number of transactions that have passed since the last such scan is greater than ",(0,i.jsx)(t.code,{children:"vacuum_freeze_table_age"})," minus ",(0,i.jsx)(t.code,{children:"vacuum_freeze_min_age"}),". Setting ",(0,i.jsx)(t.code,{children:"vacuum_freeze_table_age"})," to 0 forces ",(0,i.jsx)(t.code,{children:"VACUUM"})," to always use its aggressive strategy."]}),"\n",(0,i.jsxs)(t.p,{children:["The maximum time that a table can go unvacuumed is two billion transactions minus the ",(0,i.jsx)(t.code,{children:"vacuum_freeze_min_age"})," value at the time of the last aggressive vacuum. If it were to go unvacuumed for longer than that, data loss could result. To ensure that this does not happen, autovacuum is invoked on any table that might contain unfrozen rows with XIDs older than the age specified by the configuration parameter ",(0,i.jsx)(t.a,{href:"runtime-config-autovacuum.html#GUC-AUTOVACUUM-FREEZE-MAX-AGE",children:"autovacuum_freeze_max_age"}),". (This will happen even if autovacuum is disabled.)"]}),"\n",(0,i.jsxs)(t.p,{children:["This implies that if a table is not otherwise vacuumed, autovacuum will be invoked on it approximately once every ",(0,i.jsx)(t.code,{children:"autovacuum_freeze_max_age"})," minus ",(0,i.jsx)(t.code,{children:"vacuum_freeze_min_age"})," transactions. For tables that are regularly vacuumed for space reclamation purposes, this is of little importance. However, for static tables (including tables that receive inserts, but no updates or deletes), there is no need to vacuum for space reclamation, so it can be useful to try to maximize the interval between forced autovacuums on very large static tables. Obviously one can do this either by increasing ",(0,i.jsx)(t.code,{children:"autovacuum_freeze_max_age"})," or decreasing ",(0,i.jsx)(t.code,{children:"vacuum_freeze_min_age"}),"."]}),"\n",(0,i.jsxs)(t.p,{children:["The effective maximum for ",(0,i.jsx)(t.code,{children:"vacuum_freeze_table_age"})," is 0.95 * ",(0,i.jsx)(t.code,{children:"autovacuum_freeze_max_age"}),"; a setting higher than that will be capped to the maximum. A value higher than ",(0,i.jsx)(t.code,{children:"autovacuum_freeze_max_age"})," wouldn't make sense because an anti-wraparound autovacuum would be triggered at that point anyway, and the 0.95 multiplier leaves some breathing room to run a manual ",(0,i.jsx)(t.code,{children:"VACUUM"})," before that happens. As a rule of thumb, ",(0,i.jsx)(t.code,{children:"vacuum_freeze_table_age"})," should be set to a value somewhat below ",(0,i.jsx)(t.code,{children:"autovacuum_freeze_max_age"}),", leaving enough gap so that a regularly scheduled ",(0,i.jsx)(t.code,{children:"VACUUM"})," or an autovacuum triggered by normal delete and update activity is run in that window. Setting it too close could lead to anti-wraparound autovacuums, even though the table was recently vacuumed to reclaim space, whereas lower values lead to more frequent aggressive vacuuming."]}),"\n",(0,i.jsxs)(t.p,{children:["The sole disadvantage of increasing ",(0,i.jsx)(t.code,{children:"autovacuum_freeze_max_age"})," (and ",(0,i.jsx)(t.code,{children:"vacuum_freeze_table_age"})," along with it) is that the ",(0,i.jsx)(t.code,{children:"pg_xact"})," and ",(0,i.jsx)(t.code,{children:"pg_commit_ts"})," subdirectories of the database cluster will take more space, because it must store the commit status and (if ",(0,i.jsx)(t.code,{children:"track_commit_timestamp"})," is enabled) timestamp of all transactions back to the ",(0,i.jsx)(t.code,{children:"autovacuum_freeze_max_age"})," horizon. The commit status uses two bits per transaction, so if ",(0,i.jsx)(t.code,{children:"autovacuum_freeze_max_age"})," is set to its maximum allowed value of two billion, ",(0,i.jsx)(t.code,{children:"pg_xact"})," can be expected to grow to about half a gigabyte and ",(0,i.jsx)(t.code,{children:"pg_commit_ts"})," to about 20GB. If this is trivial compared to your total database size, setting ",(0,i.jsx)(t.code,{children:"autovacuum_freeze_max_age"})," to its maximum allowed value is recommended. Otherwise, set it depending on what you are willing to allow for ",(0,i.jsx)(t.code,{children:"pg_xact"})," and ",(0,i.jsx)(t.code,{children:"pg_commit_ts"})," storage. (The default, 200 million transactions, translates to about 50MB of ",(0,i.jsx)(t.code,{children:"pg_xact"})," storage and about 2GB of ",(0,i.jsx)(t.code,{children:"pg_commit_ts"})," storage.)"]}),"\n",(0,i.jsxs)(t.p,{children:["One disadvantage of decreasing ",(0,i.jsx)(t.code,{children:"vacuum_freeze_min_age"})," is that it might cause ",(0,i.jsx)(t.code,{children:"VACUUM"})," to do useless work: freezing a row version is a waste of time if the row is modified soon thereafter (causing it to acquire a new XID). So the setting should be large enough that rows are not frozen until they are unlikely to change any more."]}),"\n",(0,i.jsxs)(t.p,{children:["To track the age of the oldest unfrozen XIDs in a database, ",(0,i.jsx)(t.code,{children:"VACUUM"})," stores XID statistics in the system tables ",(0,i.jsx)(t.code,{children:"pg_class"})," and ",(0,i.jsx)(t.code,{children:"pg_database"}),". In particular, the ",(0,i.jsx)(t.code,{children:"relfrozenxid"})," column of a table's ",(0,i.jsx)(t.code,{children:"pg_class"})," row contains the oldest remaining unfrozen XID at the end of the most recent ",(0,i.jsx)(t.code,{children:"VACUUM"})," that successfully advanced ",(0,i.jsx)(t.code,{children:"relfrozenxid"})," (typically the most recent aggressive VACUUM). Similarly, the ",(0,i.jsx)(t.code,{children:"datfrozenxid"})," column of a database's ",(0,i.jsx)(t.code,{children:"pg_database"})," row is a lower bound on the unfrozen XIDs appearing in that database \u2014 it is just the minimum of the per-table ",(0,i.jsx)(t.code,{children:"relfrozenxid"})," values within the database. A convenient way to examine this information is to execute queries such as:"]}),"\n",(0,i.jsx)(t.pre,{children:(0,i.jsx)(t.code,{children:"\r\nSELECT c.oid::regclass as table_name,\r\n       greatest(age(c.relfrozenxid),age(t.relfrozenxid)) as age\r\nFROM pg_class c\r\nLEFT JOIN pg_class t ON c.reltoastrelid = t.oid\r\nWHERE c.relkind IN ('r', 'm');\r\n\r\nSELECT datname, age(datfrozenxid) FROM pg_database;\r\n\n"})}),"\n",(0,i.jsxs)(t.p,{children:["The ",(0,i.jsx)(t.code,{children:"age"})," column measures the number of transactions from the cutoff XID to the current transaction's XID."]}),"\n",(0,i.jsx)(t.h3,{id:"tip-5",children:"Tip"}),"\n",(0,i.jsxs)(t.p,{children:["When the ",(0,i.jsx)(t.code,{children:"VACUUM"})," command's ",(0,i.jsx)(t.code,{children:"VERBOSE"})," parameter is specified, ",(0,i.jsx)(t.code,{children:"VACUUM"})," prints various statistics about the table. This includes information about how ",(0,i.jsx)(t.code,{children:"relfrozenxid"})," and ",(0,i.jsx)(t.code,{children:"relminmxid"})," advanced, and the number of newly frozen pages. The same details appear in the server log when autovacuum logging (controlled by ",(0,i.jsx)(t.a,{href:"runtime-config-logging.html#GUC-LOG-AUTOVACUUM-MIN-DURATION",children:"log_autovacuum_min_duration"}),") reports on a ",(0,i.jsx)(t.code,{children:"VACUUM"})," operation executed by autovacuum."]}),"\n",(0,i.jsxs)(t.p,{children:[(0,i.jsx)(t.code,{children:"VACUUM"})," normally only scans pages that have been modified since the last vacuum, but ",(0,i.jsx)(t.code,{children:"relfrozenxid"})," can only be advanced when every page of the table that might contain unfrozen XIDs is scanned. This happens when ",(0,i.jsx)(t.code,{children:"relfrozenxid"})," is more than ",(0,i.jsx)(t.code,{children:"vacuum_freeze_table_age"})," transactions old, when ",(0,i.jsx)(t.code,{children:"VACUUM"}),"'s ",(0,i.jsx)(t.code,{children:"FREEZE"})," option is used, or when all pages that are not already all-frozen happen to require vacuuming to remove dead row versions. When ",(0,i.jsx)(t.code,{children:"VACUUM"})," scans every page in the table that is not already all-frozen, it should set ",(0,i.jsx)(t.code,{children:"age(relfrozenxid)"})," to a value just a little more than the ",(0,i.jsx)(t.code,{children:"vacuum_freeze_min_age"})," setting that was used (more by the number of transactions started since the ",(0,i.jsx)(t.code,{children:"VACUUM"})," started). ",(0,i.jsx)(t.code,{children:"VACUUM"})," will set ",(0,i.jsx)(t.code,{children:"relfrozenxid"})," to the oldest XID that remains in the table, so it's possible that the final value will be much more recent than strictly required. If no ",(0,i.jsx)(t.code,{children:"relfrozenxid"}),"-advancing ",(0,i.jsx)(t.code,{children:"VACUUM"})," is issued on the table until ",(0,i.jsx)(t.code,{children:"autovacuum_freeze_max_age"})," is reached, an autovacuum will soon be forced for the table."]}),"\n",(0,i.jsx)(t.p,{children:"If for some reason autovacuum fails to clear old XIDs from a table, the system will begin to emit warning messages like this when the database's oldest XIDs reach forty million transactions from the wraparound point:"}),"\n",(0,i.jsx)(t.pre,{children:(0,i.jsx)(t.code,{children:'\r\nWARNING:  database "mydb" must be vacuumed within 39985967 transactions\r\nHINT:  To avoid a database shutdown, execute a database-wide VACUUM in that database.\r\n\n'})}),"\n",(0,i.jsxs)(t.p,{children:["(A manual ",(0,i.jsx)(t.code,{children:"VACUUM"})," should fix the problem, as suggested by the hint; but note that the ",(0,i.jsx)(t.code,{children:"VACUUM"})," should be performed by a superuser, else it will fail to process system catalogs, which prevent it from being able to advance the database's ",(0,i.jsx)(t.code,{children:"datfrozenxid"}),".) If these warnings are ignored, the system will refuse to assign new XIDs once there are fewer than three million transactions left until wraparound:"]}),"\n",(0,i.jsx)(t.pre,{children:(0,i.jsx)(t.code,{children:'\r\nERROR:  database is not accepting commands to avoid wraparound data loss in database "mydb"\r\nHINT:  Stop the postmaster and vacuum that database in single-user mode.\r\n\n'})}),"\n",(0,i.jsxs)(t.p,{children:["In this condition any transactions already in progress can continue, but only read-only transactions can be started. Operations that modify database records or truncate relations will fail. The ",(0,i.jsx)(t.code,{children:"VACUUM"})," command can still be run normally. Contrary to what the hint states, it is not necessary or desirable to stop the postmaster or enter single user-mode in order to restore normal operation. Instead, follow these steps:"]}),"\n",(0,i.jsxs)(t.ol,{children:["\n",(0,i.jsxs)(t.li,{children:["Resolve old prepared transactions. You can find these by checking ",(0,i.jsx)(t.a,{href:"view-pg-prepared-xacts.html",title:"54.16.\xa0pg_prepared_xacts",children:"pg_prepared_xacts"})," for rows where ",(0,i.jsx)(t.code,{children:"age(transactionid)"})," is large. Such transactions should be committed or rolled back."]}),"\n",(0,i.jsxs)(t.li,{children:["End long-running open transactions. You can find these by checking ",(0,i.jsx)(t.a,{href:"monitoring-stats.html#MONITORING-PG-STAT-ACTIVITY-VIEW",title:"28.2.3.\xa0pg_stat_activity",children:"pg_stat_activity"})," for rows where ",(0,i.jsx)(t.code,{children:"age(backend_xid)"})," or ",(0,i.jsx)(t.code,{children:"age(backend_xmin)"})," is large. Such transactions should be committed or rolled back, or the session can be terminated using ",(0,i.jsx)(t.code,{children:"pg_terminate_backend"}),"."]}),"\n",(0,i.jsxs)(t.li,{children:["Drop any old replication slots. Use ",(0,i.jsx)(t.a,{href:"monitoring-stats.html#MONITORING-PG-STAT-REPLICATION-VIEW",title:"28.2.4.\xa0pg_stat_replication",children:"pg_stat_replication"})," to find slots where ",(0,i.jsx)(t.code,{children:"age(xmin)"})," or ",(0,i.jsx)(t.code,{children:"age(catalog_xmin)"})," is large. In many cases, such slots were created for replication to servers that no longer exist, or that have been down for a long time. If you drop a slot for a server that still exists and might still try to connect to that slot, that replica may need to be rebuilt."]}),"\n",(0,i.jsxs)(t.li,{children:["Execute ",(0,i.jsx)(t.code,{children:"VACUUM"})," in the target database. A database-wide ",(0,i.jsx)(t.code,{children:"VACUUM"})," is simplest; to reduce the time required, it as also possible to issue manual ",(0,i.jsx)(t.code,{children:"VACUUM"})," commands on the tables where ",(0,i.jsx)(t.code,{children:"relminxid"})," is oldest. Do not use ",(0,i.jsx)(t.code,{children:"VACUUM FULL"})," in this scenario, because it requires an XID and will therefore fail, except in super-user mode, where it will instead consume an XID and thus increase the risk of transaction ID wraparound. Do not use ",(0,i.jsx)(t.code,{children:"VACUUM FREEZE"})," either, because it will do more than the minimum amount of work required to restore normal operation."]}),"\n",(0,i.jsx)(t.li,{children:"Once normal operation is restored, ensure that autovacuum is properly configured in the target database in order to avoid future problems."}),"\n"]}),"\n",(0,i.jsx)(t.h3,{id:"note-1",children:"Note"}),"\n",(0,i.jsxs)(t.p,{children:["In earlier versions, it was sometimes necessary to stop the postmaster and ",(0,i.jsx)(t.code,{children:"VACUUM"})," the database in a single-user mode. In typical scenarios, this is no longer necessary, and should be avoided whenever possible, since it involves taking the system down. It is also riskier, since it disables transaction ID wraparound safeguards that are designed to prevent data loss. The only reason to use single-user mode in this scenario is if you wish to ",(0,i.jsx)(t.code,{children:"TRUNCATE"})," or ",(0,i.jsx)(t.code,{children:"DROP"})," unneeded tables to avoid needing to ",(0,i.jsx)(t.code,{children:"VACUUM"})," them. The three-million-transaction safety margin exists to let the administrator do this. See the ",(0,i.jsx)(t.a,{href:"app-postgres.html",title:"postgres",children:"postgres"})," reference page for details about using single-user mode."]}),"\n",(0,i.jsx)(t.h4,{id:"25151-multixacts-and-wraparound",children:"25.1.5.1. Multixacts and Wraparound"}),"\n",(0,i.jsxs)(t.p,{children:[(0,i.jsx)(t.em,{children:"Multixact IDs"})," are used to support row locking by multiple transactions. Since there is only limited space in a tuple header to store lock information, that information is encoded as a \u201cmultiple transaction ID\u201d, or multixact ID for short, whenever there is more than one transaction concurrently locking a row. Information about which transaction IDs are included in any particular multixact ID is stored separately in the ",(0,i.jsx)(t.code,{children:"pg_multixact"})," subdirectory, and only the multixact ID appears in the ",(0,i.jsx)(t.code,{children:"xmax"})," field in the tuple header. Like transaction IDs, multixact IDs are implemented as a 32-bit counter and corresponding storage, all of which requires careful aging management, storage cleanup, and wraparound handling. There is a separate storage area which holds the list of members in each multixact, which also uses a 32-bit counter and which must also be managed."]}),"\n",(0,i.jsxs)(t.p,{children:["Whenever ",(0,i.jsx)(t.code,{children:"VACUUM"})," scans any part of a table, it will replace any multixact ID it encounters which is older than ",(0,i.jsx)(t.a,{href:"runtime-config-client.html#GUC-VACUUM-MULTIXACT-FREEZE-MIN-AGE",children:"vacuum_multixact_freeze_min_age"})," by a different value, which can be the zero value, a single transaction ID, or a newer multixact ID. For each table, ",(0,i.jsx)(t.code,{children:"pg_class"}),".",(0,i.jsx)(t.code,{children:"relminmxid"})," stores the oldest possible multixact ID still appearing in any tuple of that table. If this value is older than ",(0,i.jsx)(t.a,{href:"runtime-config-client.html#GUC-VACUUM-MULTIXACT-FREEZE-TABLE-AGE",children:"vacuum_multixact_freeze_table_age"}),", an aggressive vacuum is forced. As discussed in the previous section, an aggressive vacuum means that only those pages which are known to be all-frozen will be skipped. ",(0,i.jsx)(t.code,{children:"mxid_age()"})," can be used on ",(0,i.jsx)(t.code,{children:"pg_class"}),".",(0,i.jsx)(t.code,{children:"relminmxid"})," to find its age."]}),"\n",(0,i.jsxs)(t.p,{children:["Aggressive ",(0,i.jsx)(t.code,{children:"VACUUM"}),"s, regardless of what causes them, are ",(0,i.jsx)(t.em,{children:"guaranteed"})," to be able to advance the table's ",(0,i.jsx)(t.code,{children:"relminmxid"}),". Eventually, as all tables in all databases are scanned and their oldest multixact values are advanced, on-disk storage for older multixacts can be removed."]}),"\n",(0,i.jsxs)(t.p,{children:["As a safety device, an aggressive vacuum scan will occur for any table whose multixact-age is greater than ",(0,i.jsx)(t.a,{href:"runtime-config-autovacuum.html#GUC-AUTOVACUUM-MULTIXACT-FREEZE-MAX-AGE",children:"autovacuum_multixact_freeze_max_age"}),". Also, if the storage occupied by multixacts members exceeds about 10GB, aggressive vacuum scans will occur more often for all tables, starting with those that have the oldest multixact-age. Both of these kinds of aggressive scans will occur even if autovacuum is nominally disabled. The members storage area can grow up to about 20GB before reaching wraparound."]}),"\n",(0,i.jsx)(t.p,{children:"Similar to the XID case, if autovacuum fails to clear old MXIDs from a table, the system will begin to emit warning messages when the database's oldest MXIDs reach forty million transactions from the wraparound point. And, just as an the XID case, if these warnings are ignored, the system will refuse to generate new MXIDs once there are fewer than three million left until wraparound."}),"\n",(0,i.jsx)(t.p,{children:"Normal operation when MXIDs are exhausted can be restored in much the same way as when XIDs are exhausted. Follow the same steps in the previous section, but with the following differences:"}),"\n",(0,i.jsxs)(t.ol,{children:["\n",(0,i.jsx)(t.li,{children:"Running transactions and prepared transactions can be ignored if there is no chance that they might appear in a multixact."}),"\n",(0,i.jsxs)(t.li,{children:["MXID information is not directly visible in system views such as ",(0,i.jsx)(t.code,{children:"pg_stat_activity"}),"; however, looking for old XIDs is still a good way of determining which transactions are causing MXID wraparound problems."]}),"\n",(0,i.jsx)(t.li,{children:"XID exhaustion will block all write transactions, but MXID exhaustion will only block a subset of write transactions, specifically those that involve row locks that require an MXID."}),"\n"]}),"\n",(0,i.jsx)(t.h3,{id:"2516-the-autovacuum-daemon",children:"25.1.6. The Autovacuum Daemon"}),"\n",(0,i.jsxs)(t.p,{children:["PostgreSQL has an optional but highly recommended feature called ",(0,i.jsx)(t.em,{children:"autovacuum"}),", whose purpose is to automate the execution of ",(0,i.jsx)(t.code,{children:"VACUUM"})," and ",(0,i.jsx)(t.code,{children:"ANALYZE"})," commands. When enabled, autovacuum checks for tables that have had a large number of inserted, updated or deleted tuples. These checks use the statistics collection facility; therefore, autovacuum cannot be used unless ",(0,i.jsx)(t.a,{href:"runtime-config-statistics.html#GUC-TRACK-COUNTS",children:"track_counts"})," is set to ",(0,i.jsx)(t.code,{children:"true"}),". In the default configuration, autovacuuming is enabled and the related configuration parameters are appropriately set."]}),"\n",(0,i.jsxs)(t.p,{children:["The \u201cautovacuum daemon\u201d actually consists of multiple processes. There is a persistent daemon process, called the ",(0,i.jsx)(t.em,{children:"autovacuum launcher"}),", which is in charge of starting ",(0,i.jsx)(t.em,{children:"autovacuum worker"})," processes for all databases. The launcher will distribute the work across time, attempting to start one worker within each database every ",(0,i.jsx)(t.a,{href:"runtime-config-autovacuum.html#GUC-AUTOVACUUM-NAPTIME",children:"autovacuum_naptime"})," seconds. (Therefore, if the installation has ",(0,i.jsx)(t.em,{children:(0,i.jsx)(t.code,{children:"N"})})," databases, a new worker will be launched every ",(0,i.jsx)(t.code,{children:"autovacuum_naptime"}),"/",(0,i.jsx)(t.em,{children:(0,i.jsx)(t.code,{children:"N"})})," seconds.) A maximum of ",(0,i.jsx)(t.a,{href:"runtime-config-autovacuum.html#GUC-AUTOVACUUM-MAX-WORKERS",children:"autovacuum_max_workers"})," worker processes are allowed to run at the same time. If there are more than ",(0,i.jsx)(t.code,{children:"autovacuum_max_workers"})," databases to be processed, the next database will be processed as soon as the first worker finishes. Each worker process will check each table within its database and execute ",(0,i.jsx)(t.code,{children:"VACUUM"})," and/or ",(0,i.jsx)(t.code,{children:"ANALYZE"})," as needed. ",(0,i.jsx)(t.a,{href:"runtime-config-logging.html#GUC-LOG-AUTOVACUUM-MIN-DURATION",children:"log_autovacuum_min_duration"})," can be set to monitor autovacuum workers' activity."]}),"\n",(0,i.jsxs)(t.p,{children:["If several large tables all become eligible for vacuuming in a short amount of time, all autovacuum workers might become occupied with vacuuming those tables for a long period. This would result in other tables and databases not being vacuumed until a worker becomes available. There is no limit on how many workers might be in a single database, but workers do try to avoid repeating work that has already been done by other workers. Note that the number of running workers does not count towards ",(0,i.jsx)(t.a,{href:"runtime-config-connection.html#GUC-MAX-CONNECTIONS",children:"max_connections"})," or ",(0,i.jsx)(t.a,{href:"runtime-config-connection.html#GUC-SUPERUSER-RESERVED-CONNECTIONS",children:"superuser_reserved_connections"})," limits."]}),"\n",(0,i.jsxs)(t.p,{children:["Tables whose ",(0,i.jsx)(t.code,{children:"relfrozenxid"})," value is more than ",(0,i.jsx)(t.a,{href:"runtime-config-autovacuum.html#GUC-AUTOVACUUM-FREEZE-MAX-AGE",children:"autovacuum_freeze_max_age"})," transactions old are always vacuumed (this also applies to those tables whose freeze max age has been modified via storage parameters; see below). Otherwise, if the number of tuples obsoleted since the last ",(0,i.jsx)(t.code,{children:"VACUUM"})," exceeds the \u201cvacuum threshold\u201d, the table is vacuumed. The vacuum threshold is defined as:"]}),"\n",(0,i.jsx)(t.pre,{children:(0,i.jsx)(t.code,{children:"\r\nvacuum threshold = vacuum base threshold + vacuum scale factor * number of tuples\r\n\n"})}),"\n",(0,i.jsxs)(t.p,{children:["where the vacuum base threshold is ",(0,i.jsx)(t.a,{href:"runtime-config-autovacuum.html#GUC-AUTOVACUUM-VACUUM-THRESHOLD",children:"autovacuum_vacuum_threshold"}),", the vacuum scale factor is ",(0,i.jsx)(t.a,{href:"runtime-config-autovacuum.html#GUC-AUTOVACUUM-VACUUM-SCALE-FACTOR",children:"autovacuum_vacuum_scale_factor"}),", and the number of tuples is ",(0,i.jsx)(t.code,{children:"pg_class"}),".",(0,i.jsx)(t.code,{children:"reltuples"}),"."]}),"\n",(0,i.jsx)(t.p,{children:"The table is also vacuumed if the number of tuples inserted since the last vacuum has exceeded the defined insert threshold, which is defined as:"}),"\n",(0,i.jsx)(t.pre,{children:(0,i.jsx)(t.code,{children:"\r\nvacuum insert threshold = vacuum base insert threshold + vacuum insert scale factor * number of tuples\r\n\n"})}),"\n",(0,i.jsxs)(t.p,{children:["where the vacuum insert base threshold is ",(0,i.jsx)(t.a,{href:"runtime-config-autovacuum.html#GUC-AUTOVACUUM-VACUUM-INSERT-THRESHOLD",children:"autovacuum_vacuum_insert_threshold"}),", and vacuum insert scale factor is ",(0,i.jsx)(t.a,{href:"runtime-config-autovacuum.html#GUC-AUTOVACUUM-VACUUM-INSERT-SCALE-FACTOR",children:"autovacuum_vacuum_insert_scale_factor"}),". Such vacuums may allow portions of the table to be marked as ",(0,i.jsx)(t.em,{children:"all visible"})," and also allow tuples to be frozen, which can reduce the work required in subsequent vacuums. For tables which receive ",(0,i.jsx)(t.code,{children:"INSERT"})," operations but no or almost no ",(0,i.jsx)(t.code,{children:"UPDATE"}),"/",(0,i.jsx)(t.code,{children:"DELETE"})," operations, it may be beneficial to lower the table's ",(0,i.jsx)(t.a,{href:"sql-createtable.html#RELOPTION-AUTOVACUUM-FREEZE-MIN-AGE",children:"autovacuum_freeze_min_age"})," as this may allow tuples to be frozen by earlier vacuums. The number of obsolete tuples and the number of inserted tuples are obtained from the cumulative statistics system; it is a semi-accurate count updated by each ",(0,i.jsx)(t.code,{children:"UPDATE"}),", ",(0,i.jsx)(t.code,{children:"DELETE"})," and ",(0,i.jsx)(t.code,{children:"INSERT"})," operation. (It is only semi-accurate because some information might be lost under heavy load.) If the ",(0,i.jsx)(t.code,{children:"relfrozenxid"})," value of the table is more than ",(0,i.jsx)(t.code,{children:"vacuum_freeze_table_age"})," transactions old, an aggressive vacuum is performed to freeze old tuples and advance ",(0,i.jsx)(t.code,{children:"relfrozenxid"}),"; otherwise, only pages that have been modified since the last vacuum are scanned."]}),"\n",(0,i.jsx)(t.p,{children:"For analyze, a similar condition is used: the threshold, defined as:"}),"\n",(0,i.jsx)(t.pre,{children:(0,i.jsx)(t.code,{children:"\r\nanalyze threshold = analyze base threshold + analyze scale factor * number of tuples\r\n\n"})}),"\n",(0,i.jsxs)(t.p,{children:["is compared to the total number of tuples inserted, updated, or deleted since the last ",(0,i.jsx)(t.code,{children:"ANALYZE"}),"."]}),"\n",(0,i.jsxs)(t.p,{children:["Partitioned tables do not directly store tuples and consequently are not processed by autovacuum. (Autovacuum does process table partitions just like other tables.) Unfortunately, this means that autovacuum does not run ",(0,i.jsx)(t.code,{children:"ANALYZE"})," on partitioned tables, and this can cause suboptimal plans for queries that reference partitioned table statistics. You can work around this problem by manually running ",(0,i.jsx)(t.code,{children:"ANALYZE"})," on partitioned tables when they are first populated, and again whenever the distribution of data in their partitions changes significantly."]}),"\n",(0,i.jsx)(t.p,{children:"Temporary tables cannot be accessed by autovacuum. Therefore, appropriate vacuum and analyze operations should be performed via session SQL commands."}),"\n",(0,i.jsxs)(t.p,{children:["The default thresholds and scale factors are taken from ",(0,i.jsx)(t.code,{children:"postgresql.conf"}),", but it is possible to override them (and many other autovacuum control parameters) on a per-table basis; see ",(0,i.jsx)(t.a,{href:"sql-createtable.html#SQL-CREATETABLE-STORAGE-PARAMETERS",title:"Storage Parameters",children:"Storage Parameters"})," for more information. If a setting has been changed via a table's storage parameters, that value is used when processing that table; otherwise the global settings are used. See ",(0,i.jsx)(t.a,{href:"runtime-config-autovacuum.html",title:"20.10.\xa0Automatic Vacuuming",children:"Section\xa020.10"})," for more details on the global settings."]}),"\n",(0,i.jsxs)(t.p,{children:["When multiple workers are running, the autovacuum cost delay parameters (see ",(0,i.jsx)(t.a,{href:"runtime-config-resource.html#RUNTIME-CONFIG-RESOURCE-VACUUM-COST",title:"20.4.4.\xa0Cost-based Vacuum Delay",children:"Section\xa020.4.4"}),") are \u201cbalanced\u201d among all the running workers, so that the total I/O impact on the system is the same regardless of the number of workers actually running. However, any workers processing tables whose per-table ",(0,i.jsx)(t.code,{children:"autovacuum_vacuum_cost_delay"})," or ",(0,i.jsx)(t.code,{children:"autovacuum_vacuum_cost_limit"})," storage parameters have been set are not considered in the balancing algorithm."]}),"\n",(0,i.jsxs)(t.p,{children:["Autovacuum workers generally don't block other commands. If a process attempts to acquire a lock that conflicts with the ",(0,i.jsx)(t.code,{children:"SHARE UPDATE EXCLUSIVE"})," lock held by autovacuum, lock acquisition will interrupt the autovacuum. For conflicting lock modes, see ",(0,i.jsx)(t.a,{href:"explicit-locking.html#TABLE-LOCK-COMPATIBILITY",title:"Table\xa013.2.\xa0Conflicting Lock Modes",children:"Table\xa013.2"}),". However, if the autovacuum is running to prevent transaction ID wraparound (i.e., the autovacuum query name in the ",(0,i.jsx)(t.code,{children:"pg_stat_activity"})," view ends with ",(0,i.jsx)(t.code,{children:"(to prevent wraparound)"}),"), the autovacuum is not automatically interrupted."]}),"\n",(0,i.jsx)(t.h3,{id:"warning",children:"Warning"}),"\n",(0,i.jsxs)(t.p,{children:["Regularly running commands that acquire locks conflicting with a ",(0,i.jsx)(t.code,{children:"SHARE UPDATE EXCLUSIVE"})," lock (e.g., ANALYZE) can effectively prevent autovacuums from ever completing."]}),"\n",(0,i.jsx)(t.hr,{}),"\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n",(0,i.jsxs)(t.table,{children:[(0,i.jsx)(t.thead,{children:(0,i.jsxs)(t.tr,{children:[(0,i.jsx)(t.th,{}),(0,i.jsx)(t.th,{}),(0,i.jsx)(t.th,{})]})}),(0,i.jsxs)(t.tbody,{children:[(0,i.jsxs)(t.tr,{children:[(0,i.jsx)(t.td,{children:(0,i.jsx)(t.a,{href:"maintenance.html",title:"Chapter\xa025.\xa0Routine Database Maintenance Tasks",children:"Prev"})}),(0,i.jsx)(t.td,{children:(0,i.jsx)(t.a,{href:"maintenance.html",title:"Chapter\xa025.\xa0Routine Database Maintenance Tasks",children:"Up"})}),(0,i.jsx)(t.td,{children:(0,i.jsx)(t.a,{href:"routine-reindex.html",title:"25.2.\xa0Routine Reindexing",children:"Next"})})]}),(0,i.jsxs)(t.tr,{children:[(0,i.jsx)(t.td,{children:"Chapter\xa025.\xa0Routine Database Maintenance Tasks"}),(0,i.jsx)(t.td,{children:(0,i.jsx)(t.a,{href:"index.html",title:"PostgreSQL 16.9 Documentation",children:"Home"})}),(0,i.jsx)(t.td,{children:"25.2.\xa0Routine Reindexing"})]})]})]}),"\n",(0,i.jsx)(t.h2,{id:"submit-correction",children:"Submit correction"}),"\n",(0,i.jsxs)(t.p,{children:["If you see anything in the documentation that is not correct, does not match\r\nyour experience with the particular feature or requires further clarification,\r\nplease use\r\n",(0,i.jsx)(t.a,{href:"/account/comments/new/16/routine-vacuuming.html/",children:"this form"}),"\r\nto report a documentation issue."]}),"\n",(0,i.jsxs)(t.p,{children:[(0,i.jsx)(t.a,{href:"/about/privacypolicy",children:"Privacy Policy"})," |\r\n",(0,i.jsx)(t.a,{href:"/about/policies/coc/",children:"Code of Conduct"})," |\r\n",(0,i.jsx)(t.a,{href:"/about/",children:"About PostgreSQL"})," |\r\n",(0,i.jsx)(t.a,{href:"/about/contact/",children:"Contact"})]}),"\n",(0,i.jsx)(t.p,{children:"Copyright \xa9 1996-2025 The PostgreSQL Global Development Group"})]})}function h(e={}){const{wrapper:t}={...(0,s.R)(),...e.components};return t?(0,i.jsx)(t,{...e,children:(0,i.jsx)(d,{...e})}):d(e)}}}]);