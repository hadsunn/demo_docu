"use strict";(self.webpackChunkpgdoc_02=self.webpackChunkpgdoc_02||[]).push([[69675],{28453:(e,t,n)=>{n.d(t,{R:()=>s,x:()=>r});var a=n(96540);const i={},o=a.createContext(i);function s(e){const t=a.useContext(o);return a.useMemo(function(){return"function"==typeof e?e(t):{...t,...e}},[t,e])}function r(e){let t;return t=e.disableParentContext?"function"==typeof e.components?e.components(i):e.components||i:s(e.components),a.createElement(o.Provider,{value:t},e.children)}},43836:(e,t,n)=>{n.r(t),n.d(t,{assets:()=>c,contentTitle:()=>r,default:()=>h,frontMatter:()=>s,metadata:()=>a,toc:()=>l});const a=JSON.parse('{"id":"II. The SQL Language/Concurrency Control/transaction-iso","title":"13.2. Transaction Isolation","description":"Supported versions: 13 / 14 / 15 / 16 / 17","source":"@site/docs/II. The SQL Language/13. Concurrency Control/transaction-iso.md","sourceDirName":"II. The SQL Language/13. Concurrency Control","slug":"/II. The SQL Language/Concurrency Control/transaction-iso","permalink":"/demo_docu/II. The SQL Language/Concurrency Control/transaction-iso","draft":false,"unlisted":false,"tags":[],"version":"current","frontMatter":{"title":"13.2. Transaction Isolation"},"sidebar":"autoGeneratedSidebar","previous":{"title":"13.5. Serialization Failure Handling","permalink":"/demo_docu/II. The SQL Language/Concurrency Control/mvcc-serialization-failure-handling"},"next":{"title":"Chapter 14. Performance Tips","permalink":"/demo_docu/II. The SQL Language/Performance Tips/"}}');var i=n(74848),o=n(28453);const s={title:"13.2. Transaction Isolation"},r=void 0,c={},l=[{value:"Important",id:"important",level:3},{value:"13.2.1. Read Committed Isolation Level",id:"1321-read-committed-isolation-level",level:3},{value:"13.2.2. Repeatable Read Isolation Level",id:"1322-repeatable-read-isolation-level",level:3},{value:"Note",id:"note",level:3},{value:"13.2.3. Serializable Isolation Level",id:"1323-serializable-isolation-level",level:3}];function d(e){const t={a:"a",br:"br",code:"code",em:"em",h3:"h3",hr:"hr",li:"li",p:"p",pre:"pre",strong:"strong",ul:"ul",...(0,o.R)(),...e.components};return(0,i.jsxs)(i.Fragment,{children:[(0,i.jsx)(t.p,{children:"Supported versions: 13 / 14 / 15 / 16 / 17"}),"\n",(0,i.jsx)(t.p,{children:"Development versions: 18 / devel"}),"\n",(0,i.jsx)(t.p,{children:"Unsupported versions: 10 / 11 / 12 / 7.1 / 7.2 / 7.3 / 7.4 / 8.0 / 8.1 / 8.2 / 8.3 / 8.4 / 9.0 / 9.1 / 9.2 / 9.3 / 9.4 / 9.5 / 9.6"}),"\n",(0,i.jsx)(t.hr,{}),"\n",(0,i.jsx)(t.p,{children:(0,i.jsx)(t.a,{href:"/demo_docu/II.%20The%20SQL%20Language/Concurrency%20Control/transaction-iso#1321-read-committed-isolation-level",children:"13.2.1. Read Committed Isolation Level"})}),"\n",(0,i.jsx)(t.p,{children:(0,i.jsx)(t.a,{href:"/demo_docu/II.%20The%20SQL%20Language/Concurrency%20Control/transaction-iso#1322-repeatable-read-isolation-level",children:"13.2.2. Repeatable Read Isolation Level"})}),"\n",(0,i.jsx)(t.p,{children:(0,i.jsx)(t.a,{href:"/demo_docu/II.%20The%20SQL%20Language/Concurrency%20Control/transaction-iso#1323-serializable-isolation-level",children:"13.2.3. Serializable Isolation Level"})}),"\n",(0,i.jsx)(t.p,{children:"The SQL standard defines four levels of transaction isolation. The most strict is Serializable, which is defined by the standard in a paragraph which says that any concurrent execution of a set of Serializable transactions is guaranteed to produce the same effect as running them one at a time in some order. The other three levels are defined in terms of phenomena, resulting from interaction between concurrent transactions, which must not occur at each level. The standard notes that due to the definition of Serializable, none of these phenomena are possible at that level. (This is hardly surprising -- if the effect of the transactions must be consistent with having been run one at a time, how could you see any phenomena caused by interactions?)"}),"\n",(0,i.jsx)(t.p,{children:"The phenomena which are prohibited at various levels are:"}),"\n",(0,i.jsx)(t.p,{children:"dirty read"}),"\n",(0,i.jsx)(t.p,{children:"A transaction reads data written by a concurrent uncommitted transaction."}),"\n",(0,i.jsx)(t.p,{children:"nonrepeatable read"}),"\n",(0,i.jsx)(t.p,{children:"A transaction re-reads data it has previously read and finds that data has been modified by another transaction (that committed since the initial read)."}),"\n",(0,i.jsx)(t.p,{children:"phantom read"}),"\n",(0,i.jsx)(t.p,{children:"A transaction re-executes a query returning a set of rows that satisfy a search condition and finds that the set of rows satisfying the condition has changed due to another recently-committed transaction."}),"\n",(0,i.jsx)(t.p,{children:"serialization anomaly"}),"\n",(0,i.jsx)(t.p,{children:"The result of successfully committing a group of transactions is inconsistent with all possible orderings of running those transactions one at a time."}),"\n",(0,i.jsxs)(t.p,{children:["The SQL standard and PostgreSQL-implemented transaction isolation levels are described in ",(0,i.jsx)(t.a,{href:"transaction-iso.html#MVCC-ISOLEVEL-TABLE",title:"Table\xa013.1.\xa0Transaction Isolation Levels",children:"Table 13.1"}),"."]}),"\n",(0,i.jsx)(t.p,{children:(0,i.jsx)(t.strong,{children:"Table  13.1. Transaction Isolation Levels"})}),"\n",(0,i.jsxs)(t.p,{children:["Isolation Level | Dirty Read | Nonrepeatable Read | Phantom Read | Serialization Anomaly",(0,i.jsx)(t.br,{}),"\nRead uncommitted | Allowed, but not in PG | Possible | Possible | Possible",(0,i.jsx)(t.br,{}),"\nRead committed | Not possible | Possible | Possible | Possible",(0,i.jsx)(t.br,{}),"\nRepeatable read | Not possible | Not possible | Allowed, but not in PG | Possible",(0,i.jsx)(t.br,{}),"\nSerializable | Not possible | Not possible | Not possible | Not possible"]}),"\n",(0,i.jsx)(t.p,{children:"In PostgreSQL, you can request any of the four standard transaction isolation levels, but internally only three distinct isolation levels are implemented, i.e., PostgreSQL's Read Uncommitted mode behaves like Read Committed. This is because it is the only sensible way to map the standard isolation levels to PostgreSQL's multiversion concurrency control architecture."}),"\n",(0,i.jsxs)(t.p,{children:["The table also shows that PostgreSQL's Repeatable Read implementation does not allow phantom reads. This is acceptable under the SQL standard because the standard specifies which anomalies must ",(0,i.jsx)(t.em,{children:"not"})," occur at certain isolation levels; higher guarantees are acceptable. The behavior of the available isolation levels is detailed in the following subsections."]}),"\n",(0,i.jsxs)(t.p,{children:["To set the transaction isolation level of a transaction, use the command ",(0,i.jsx)(t.a,{href:"/demo_docu/VI.%20Reference/I.%20SQL%20Commands/sql-set-transaction",children:"SET TRANSACTION"}),"."]}),"\n",(0,i.jsx)(t.h3,{id:"important",children:"Important"}),"\n",(0,i.jsxs)(t.p,{children:["Some PostgreSQL data types and functions have special rules regarding transactional behavior. In particular, changes made to a sequence (and therefore the counter of a column declared using ",(0,i.jsx)(t.code,{children:"serial"}),") are immediately visible to all other transactions and are not rolled back if the transaction that made the changes aborts. See ",(0,i.jsx)(t.a,{href:"/demo_docu/II.%20The%20SQL%20Language/Functions%20and%20Operators/functions-sequence",children:"Section 9.17"}),"."]}),"\n",(0,i.jsx)(t.h3,{id:"1321-read-committed-isolation-level",children:"13.2.1. Read Committed Isolation Level"}),"\n",(0,i.jsxs)(t.p,{children:[(0,i.jsx)(t.em,{children:"Read Committed"})," is the default isolation level in PostgreSQL. When a transaction uses this isolation level, a ",(0,i.jsx)(t.code,{children:"SELECT"})," query (without a ",(0,i.jsx)(t.code,{children:"FOR UPDATE/SHARE"})," clause) sees only data committed before the query began; it never sees either uncommitted data or changes committed by concurrent transactions during the query's execution. In effect, a ",(0,i.jsx)(t.code,{children:"SELECT"})," query sees a snapshot of the database as of the instant the query begins to run. However, ",(0,i.jsx)(t.code,{children:"SELECT"})," does see the effects of previous updates executed within its own transaction, even though they are not yet committed. Also note that two successive ",(0,i.jsx)(t.code,{children:"SELECT"})," commands can see different data, even though they are within a single transaction, if other transactions commit changes after the first ",(0,i.jsx)(t.code,{children:"SELECT"})," starts and before the second ",(0,i.jsx)(t.code,{children:"SELECT"})," starts."]}),"\n",(0,i.jsxs)(t.p,{children:[(0,i.jsx)(t.code,{children:"UPDATE"}),", ",(0,i.jsx)(t.code,{children:"DELETE"}),", ",(0,i.jsx)(t.code,{children:"SELECT FOR UPDATE"}),", and ",(0,i.jsx)(t.code,{children:"SELECT FOR SHARE"})," commands behave the same as ",(0,i.jsx)(t.code,{children:"SELECT"})," in terms of searching for target rows: they will only find target rows that were committed as of the command start time. However, such a target row might have already been updated (or deleted or locked) by another concurrent transaction by the time it is found. In this case, the would-be updater will wait for the first updating transaction to commit or roll back (if it is still in progress). If the first updater rolls back, then its effects are negated and the second updater can proceed with updating the originally found row. If the first updater commits, the second updater will ignore the row if the first updater deleted it, otherwise it will attempt to apply its operation to the updated version of the row. The search condition of the command (the ",(0,i.jsx)(t.code,{children:"WHERE"})," clause) is re-evaluated to see if the updated version of the row still matches the search condition. If so, the second updater proceeds with its operation using the updated version of the row. In the case of ",(0,i.jsx)(t.code,{children:"SELECT FOR UPDATE"})," and ",(0,i.jsx)(t.code,{children:"SELECT FOR SHARE"}),", this means it is the updated version of the row that is locked and returned to the client."]}),"\n",(0,i.jsxs)(t.p,{children:[(0,i.jsx)(t.code,{children:"INSERT"})," with an ",(0,i.jsx)(t.code,{children:"ON CONFLICT DO UPDATE"})," clause behaves similarly. In Read Committed mode, each row proposed for insertion will either insert or update. Unless there are unrelated errors, one of those two outcomes is guaranteed. If a conflict originates in another transaction whose effects are not yet visible to the ",(0,i.jsx)(t.code,{children:"INSERT"}),", the ",(0,i.jsx)(t.code,{children:"UPDATE"})," clause will affect that row, even though possibly ",(0,i.jsx)(t.em,{children:"no"})," version of that row is conventionally visible to the command."]}),"\n",(0,i.jsxs)(t.p,{children:[(0,i.jsx)(t.code,{children:"INSERT"})," with an ",(0,i.jsx)(t.code,{children:"ON CONFLICT DO NOTHING"})," clause may have insertion not proceed for a row due to the outcome of another transaction whose effects are not visible to the ",(0,i.jsx)(t.code,{children:"INSERT"})," snapshot. Again, this is only the case in Read Committed mode."]}),"\n",(0,i.jsxs)(t.p,{children:[(0,i.jsx)(t.code,{children:"MERGE"})," allows the user to specify various combinations of ",(0,i.jsx)(t.code,{children:"INSERT"}),", ",(0,i.jsx)(t.code,{children:"UPDATE"})," and ",(0,i.jsx)(t.code,{children:"DELETE"})," subcommands. A ",(0,i.jsx)(t.code,{children:"MERGE"})," command with both ",(0,i.jsx)(t.code,{children:"INSERT"})," and ",(0,i.jsx)(t.code,{children:"UPDATE"})," subcommands looks similar to ",(0,i.jsx)(t.code,{children:"INSERT"})," with an ",(0,i.jsx)(t.code,{children:"ON CONFLICT DO UPDATE"})," clause but does not guarantee that either ",(0,i.jsx)(t.code,{children:"INSERT"})," or ",(0,i.jsx)(t.code,{children:"UPDATE"})," will occur. If ",(0,i.jsx)(t.code,{children:"MERGE"})," attempts an ",(0,i.jsx)(t.code,{children:"UPDATE"})," or ",(0,i.jsx)(t.code,{children:"DELETE"})," and the row is concurrently updated but the join condition still passes for the current target and the current source tuple, then ",(0,i.jsx)(t.code,{children:"MERGE"})," will behave the same as the ",(0,i.jsx)(t.code,{children:"UPDATE"})," or ",(0,i.jsx)(t.code,{children:"DELETE"})," commands and perform its action on the updated version of the row. However, because ",(0,i.jsx)(t.code,{children:"MERGE"})," can specify several actions and they can be conditional, the conditions for each action are re-evaluated on the updated version of the row, starting from the first action, even if the action that had originally matched appears later in the list of actions. On the other hand, if the row is concurrently updated or deleted so that the join condition fails, then ",(0,i.jsx)(t.code,{children:"MERGE"})," will evaluate the condition's ",(0,i.jsx)(t.code,{children:"NOT MATCHED"})," actions next, and execute the first one that succeeds. If ",(0,i.jsx)(t.code,{children:"MERGE"})," attempts an ",(0,i.jsx)(t.code,{children:"INSERT"})," and a unique index is present and a duplicate row is concurrently inserted, then a uniqueness violation error is raised; ",(0,i.jsx)(t.code,{children:"MERGE"})," does not attempt to avoid such errors by restarting evaluation of ",(0,i.jsx)(t.code,{children:"MATCHED"})," conditions."]}),"\n",(0,i.jsx)(t.p,{children:"Because of the above rules, it is possible for an updating command to see an inconsistent snapshot: it can see the effects of concurrent updating commands on the same rows it is trying to update, but it does not see effects of those commands on other rows in the database. This behavior makes Read Committed mode unsuitable for commands that involve complex search conditions; however, it is just right for simpler cases. For example, consider updating bank balances with transactions like:"}),"\n",(0,i.jsx)(t.pre,{children:(0,i.jsx)(t.code,{children:"BEGIN;\nUPDATE accounts SET balance = balance + 100.00 WHERE acctnum = 12345;\nUPDATE accounts SET balance = balance - 100.00 WHERE acctnum = 7534;\nCOMMIT;\n"})}),"\n",(0,i.jsx)(t.p,{children:"If two such transactions concurrently try to change the balance of account 12345, we clearly want the second transaction to start with the updated version of the account's row. Because each command is affecting only a predetermined row, letting it see the updated version of the row does not create any troublesome inconsistency."}),"\n",(0,i.jsxs)(t.p,{children:["More complex usage can produce undesirable results in Read Committed mode. For example, consider a ",(0,i.jsx)(t.code,{children:"DELETE"})," command operating on data that is being both added and removed from its restriction criteria by another command, e.g., assume ",(0,i.jsx)(t.code,{children:"website"})," is a two-row table with ",(0,i.jsx)(t.code,{children:"website.hits"})," equaling ",(0,i.jsx)(t.code,{children:"9"})," and ",(0,i.jsx)(t.code,{children:"10"}),":"]}),"\n",(0,i.jsx)(t.pre,{children:(0,i.jsx)(t.code,{children:"BEGIN;\nUPDATE website SET hits = hits + 1;\n-- run from another session:  DELETE FROM website WHERE hits = 10;\nCOMMIT;\n"})}),"\n",(0,i.jsxs)(t.p,{children:["The ",(0,i.jsx)(t.code,{children:"DELETE"})," will have no effect even though there is a ",(0,i.jsx)(t.code,{children:"website.hits = 10"})," row before and after the ",(0,i.jsx)(t.code,{children:"UPDATE"}),". This occurs because the pre-update row value ",(0,i.jsx)(t.code,{children:"9"})," is skipped, and when the ",(0,i.jsx)(t.code,{children:"UPDATE"})," completes and ",(0,i.jsx)(t.code,{children:"DELETE"})," obtains a lock, the new row value is no longer ",(0,i.jsx)(t.code,{children:"10"})," but ",(0,i.jsx)(t.code,{children:"11"}),", which no longer matches the criteria."]}),"\n",(0,i.jsxs)(t.p,{children:["Because Read Committed mode starts each command with a new snapshot that includes all transactions committed up to that instant, subsequent commands in the same transaction will see the effects of the committed concurrent transaction in any case. The point at issue above is whether or not a ",(0,i.jsx)(t.em,{children:"single"})," command sees an absolutely consistent view of the database."]}),"\n",(0,i.jsx)(t.p,{children:"The partial transaction isolation provided by Read Committed mode is adequate for many applications, and this mode is fast and simple to use; however, it is not sufficient for all cases. Applications that do complex queries and updates might require a more rigorously consistent view of the database than Read Committed mode provides."}),"\n",(0,i.jsx)(t.h3,{id:"1322-repeatable-read-isolation-level",children:"13.2.2. Repeatable Read Isolation Level"}),"\n",(0,i.jsxs)(t.p,{children:["The ",(0,i.jsx)(t.em,{children:"Repeatable Read"})," isolation level only sees data committed before the transaction began; it never sees either uncommitted data or changes committed by concurrent transactions during the transaction's execution. (However, each query does see the effects of previous updates executed within its own transaction, even though they are not yet committed.) This is a stronger guarantee than is required by the SQL standard for this isolation level, and prevents all of the phenomena described in ",(0,i.jsx)(t.a,{href:"transaction-iso.html#MVCC-ISOLEVEL-TABLE",title:"Table\xa013.1.\xa0Transaction Isolation Levels",children:"Table 13.1"})," except for serialization anomalies. As mentioned above, this is specifically allowed by the standard, which only describes the ",(0,i.jsx)(t.em,{children:"minimum"})," protections each isolation level must provide."]}),"\n",(0,i.jsxs)(t.p,{children:["This level is different from Read Committed in that a query in a repeatable read transaction sees a snapshot as of the start of the first non-transaction-control statement in the ",(0,i.jsx)(t.em,{children:"transaction"})," , not as of the start of the current statement within the transaction. Thus, successive ",(0,i.jsx)(t.code,{children:"SELECT"})," commands within a ",(0,i.jsx)(t.em,{children:"single"})," transaction see the same data, i.e., they do not see changes made by other transactions that committed after their own transaction started."]}),"\n",(0,i.jsx)(t.p,{children:"Applications using this level must be prepared to retry transactions due to serialization failures."}),"\n",(0,i.jsxs)(t.p,{children:[(0,i.jsx)(t.code,{children:"UPDATE"}),", ",(0,i.jsx)(t.code,{children:"DELETE"}),", ",(0,i.jsx)(t.code,{children:"MERGE"}),", ",(0,i.jsx)(t.code,{children:"SELECT FOR UPDATE"}),", and ",(0,i.jsx)(t.code,{children:"SELECT FOR SHARE"})," commands behave the same as ",(0,i.jsx)(t.code,{children:"SELECT"})," in terms of searching for target rows: they will only find target rows that were committed as of the transaction start time. However, such a target row might have already been updated (or deleted or locked) by another concurrent transaction by the time it is found. In this case, the repeatable read transaction will wait for the first updating transaction to commit or roll back (if it is still in progress). If the first updater rolls back, then its effects are negated and the repeatable read transaction can proceed with updating the originally found row. But if the first updater commits (and actually updated or deleted the row, not just locked it) then the repeatable read transaction will be rolled back with the message"]}),"\n",(0,i.jsx)(t.pre,{children:(0,i.jsx)(t.code,{children:"ERROR:  could not serialize access due to concurrent update\n"})}),"\n",(0,i.jsx)(t.p,{children:"because a repeatable read transaction cannot modify or lock rows changed by other transactions after the repeatable read transaction began."}),"\n",(0,i.jsx)(t.p,{children:"When an application receives this error message, it should abort the current transaction and retry the whole transaction from the beginning. The second time through, the transaction will see the previously-committed change as part of its initial view of the database, so there is no logical conflict in using the new version of the row as the starting point for the new transaction's update."}),"\n",(0,i.jsx)(t.p,{children:"Note that only updating transactions might need to be retried; read-only transactions will never have serialization conflicts."}),"\n",(0,i.jsxs)(t.p,{children:["The Repeatable Read mode provides a rigorous guarantee that each transaction sees a completely stable view of the database. However, this view will not necessarily always be consistent with some serial (one at a time) execution of concurrent transactions of the same level. For example, even a read-only transaction at this level may see a control record updated to show that a batch has been completed but ",(0,i.jsx)(t.em,{children:"not"})," see one of the detail records which is logically part of the batch because it read an earlier revision of the control record. Attempts to enforce business rules by transactions running at this isolation level are not likely to work correctly without careful use of explicit locks to block conflicting transactions."]}),"\n",(0,i.jsxs)(t.p,{children:["The Repeatable Read isolation level is implemented using a technique known in academic database literature and in some other database products as ",(0,i.jsx)(t.em,{children:"Snapshot Isolation"}),". Differences in behavior and performance may be observed when compared with systems that use a traditional locking technique that reduces concurrency. Some other systems may even offer Repeatable Read and Snapshot Isolation as distinct isolation levels with different behavior. The permitted phenomena that distinguish the two techniques were not formalized by database researchers until after the SQL standard was developed, and are outside the scope of this manual. For a full treatment, please see ",(0,i.jsx)(t.a,{href:"/demo_docu/biblio#berenson95",children:"[berenson95]"}),"."]}),"\n",(0,i.jsx)(t.h3,{id:"note",children:"Note"}),"\n",(0,i.jsx)(t.p,{children:"Prior to PostgreSQL version 9.1, a request for the Serializable transaction isolation level provided exactly the same behavior described here. To retain the legacy Serializable behavior, Repeatable Read should now be requested."}),"\n",(0,i.jsx)(t.h3,{id:"1323-serializable-isolation-level",children:"13.2.3. Serializable Isolation Level"}),"\n",(0,i.jsxs)(t.p,{children:["The ",(0,i.jsx)(t.em,{children:"Serializable"})," isolation level provides the strictest transaction isolation. This level emulates serial transaction execution for all committed transactions; as if transactions had been executed one after another, serially, rather than concurrently. However, like the Repeatable Read level, applications using this level must be prepared to retry transactions due to serialization failures. In fact, this isolation level works exactly the same as Repeatable Read except that it also monitors for conditions which could make execution of a concurrent set of serializable transactions behave in a manner inconsistent with all possible serial (one at a time) executions of those transactions. This monitoring does not introduce any blocking beyond that present in repeatable read, but there is some overhead to the monitoring, and detection of the conditions which could cause a ",(0,i.jsx)(t.em,{children:"serialization anomaly"})," will trigger a ",(0,i.jsx)(t.em,{children:"serialization failure"}),"."]}),"\n",(0,i.jsxs)(t.p,{children:["As an example, consider a table ",(0,i.jsx)(t.code,{children:"mytab"}),", initially containing:"]}),"\n",(0,i.jsx)(t.pre,{children:(0,i.jsx)(t.code,{children:" class | value\n-------+-------\n     1 |    10\n     1 |    20\n     2 |   100\n     2 |   200\n"})}),"\n",(0,i.jsx)(t.p,{children:"Suppose that serializable transaction A computes:"}),"\n",(0,i.jsx)(t.pre,{children:(0,i.jsx)(t.code,{children:"SELECT SUM(value) FROM mytab WHERE class = 1;\n"})}),"\n",(0,i.jsxs)(t.p,{children:["and then inserts the result (30) as the ",(0,i.jsx)(t.code,{children:"value"})," in a new row with ",(0,i.jsx)(t.code,{children:"class"})," ",(0,i.jsx)(t.code,{children:"= 2"}),". Concurrently, serializable transaction B computes:"]}),"\n",(0,i.jsx)(t.pre,{children:(0,i.jsx)(t.code,{children:"SELECT SUM(value) FROM mytab WHERE class = 2;\n"})}),"\n",(0,i.jsxs)(t.p,{children:["and obtains the result 300, which it inserts in a new row with ",(0,i.jsx)(t.code,{children:"class"})," ",(0,i.jsx)(t.code,{children:"= 1"}),". Then both transactions try to commit. If either transaction were running at the Repeatable Read isolation level, both would be allowed to commit; but since there is no serial order of execution consistent with the result, using Serializable transactions will allow one transaction to commit and will roll the other back with this message:"]}),"\n",(0,i.jsx)(t.pre,{children:(0,i.jsx)(t.code,{children:"ERROR:  could not serialize access due to read/write dependencies among transactions\n"})}),"\n",(0,i.jsx)(t.p,{children:"This is because if A had executed before B, B would have computed the sum 330, not 300, and similarly the other order would have resulted in a different sum computed by A."}),"\n",(0,i.jsxs)(t.p,{children:["When relying on Serializable transactions to prevent anomalies, it is important that any data read from a permanent user table not be considered valid until the transaction which read it has successfully committed. This is true even for read-only transactions, except that data read within a ",(0,i.jsx)(t.em,{children:"deferrable"})," read-only transaction is known to be valid as soon as it is read, because such a transaction waits until it can acquire a snapshot guaranteed to be free from such problems before starting to read any data. In all other cases applications must not depend on results read during a transaction that later aborted; instead, they should retry the transaction until it succeeds."]}),"\n",(0,i.jsxs)(t.p,{children:["To guarantee true serializability PostgreSQL uses ",(0,i.jsx)(t.em,{children:"predicate locking"})," , which means that it keeps locks which allow it to determine when a write would have had an impact on the result of a previous read from a concurrent transaction, had it run first. In PostgreSQL these locks do not cause any blocking and therefore can ",(0,i.jsx)(t.em,{children:"not"})," play any part in causing a deadlock. They are used to identify and flag dependencies among concurrent Serializable transactions which in certain combinations can lead to serialization anomalies. In contrast, a Read Committed or Repeatable Read transaction which wants to ensure data consistency may need to take out a lock on an entire table, which could block other users attempting to use that table, or it may use ",(0,i.jsx)(t.code,{children:"SELECT FOR UPDATE"})," or ",(0,i.jsx)(t.code,{children:"SELECT FOR SHARE"})," which not only can block other transactions but cause disk access."]}),"\n",(0,i.jsxs)(t.p,{children:["Predicate locks in PostgreSQL, like in most other database systems, are based on data actually accessed by a transaction. These will show up in the ",(0,i.jsx)(t.a,{href:"/demo_docu/VII.%20Internals/System%20Views/view-pg-locks",children:(0,i.jsx)(t.code,{children:"pg_locks"})})," system view with a ",(0,i.jsx)(t.code,{children:"mode"})," of ",(0,i.jsx)(t.code,{children:"SIReadLock"}),". The particular locks acquired during execution of a query will depend on the plan used by the query, and multiple finer-grained locks (e.g., tuple locks) may be combined into fewer coarser-grained locks (e.g., page locks) during the course of the transaction to prevent exhaustion of the memory used to track the locks. A ",(0,i.jsx)(t.code,{children:"READ ONLY"})," transaction may be able to release its SIRead locks before completion, if it detects that no conflicts can still occur which could lead to a serialization anomaly. In fact, ",(0,i.jsx)(t.code,{children:"READ ONLY"})," transactions will often be able to establish that fact at startup and avoid taking any predicate locks. If you explicitly request a ",(0,i.jsx)(t.code,{children:"SERIALIZABLE READ ONLY DEFERRABLE"})," transaction, it will block until it can establish this fact. (This is the ",(0,i.jsx)(t.em,{children:"only"})," case where Serializable transactions block but Repeatable Read transactions don't.) On the other hand, SIRead locks often need to be kept past transaction commit, until overlapping read write transactions complete."]}),"\n",(0,i.jsxs)(t.p,{children:["Consistent use of Serializable transactions can simplify development. The guarantee that any set of successfully committed concurrent Serializable transactions will have the same effect as if they were run one at a time means that if you can demonstrate that a single transaction, as written, will do the right thing when run by itself, you can have confidence that it will do the right thing in any mix of Serializable transactions, even without any information about what those other transactions might do, or it will not successfully commit. It is important that an environment which uses this technique have a generalized way of handling serialization failures (which always return with an SQLSTATE value of '40001'), because it will be very hard to predict exactly which transactions might contribute to the read/write dependencies and need to be rolled back to prevent serialization anomalies. The monitoring of read/write dependencies has a cost, as does the restart of transactions which are terminated with a serialization failure, but balanced against the cost and blocking involved in use of explicit locks and ",(0,i.jsx)(t.code,{children:"SELECT FOR UPDATE"})," or ",(0,i.jsx)(t.code,{children:"SELECT FOR SHARE"}),", Serializable transactions are the best performance choice for some environments."]}),"\n",(0,i.jsxs)(t.p,{children:["While PostgreSQL's Serializable transaction isolation level only allows concurrent transactions to commit if it can prove there is a serial order of execution that would produce the same effect, it doesn't always prevent errors from being raised that would not occur in true serial execution. In particular, it is possible to see unique constraint violations caused by conflicts with overlapping Serializable transactions even after explicitly checking that the key isn't present before attempting to insert it. This can be avoided by making sure that ",(0,i.jsx)(t.em,{children:"all"})," Serializable transactions that insert potentially conflicting keys explicitly check if they can do so first. For example, imagine an application that asks the user for a new key and then checks that it doesn't exist already by trying to select it first, or generates a new key by selecting the maximum existing key and adding one. If some Serializable transactions insert new keys directly without following this protocol, unique constraints violations might be reported even in cases where they could not occur in a serial execution of the concurrent transactions."]}),"\n",(0,i.jsx)(t.p,{children:"For optimal performance when relying on Serializable transactions for concurrency control, these issues should be considered:"}),"\n",(0,i.jsxs)(t.ul,{children:["\n",(0,i.jsxs)(t.li,{children:["\n",(0,i.jsxs)(t.p,{children:["Declare transactions as ",(0,i.jsx)(t.code,{children:"READ ONLY"})," when possible."]}),"\n"]}),"\n",(0,i.jsxs)(t.li,{children:["\n",(0,i.jsx)(t.p,{children:"Control the number of active connections, using a connection pool if needed. This is always an important performance consideration, but it can be particularly important in a busy system using Serializable transactions."}),"\n"]}),"\n",(0,i.jsxs)(t.li,{children:["\n",(0,i.jsx)(t.p,{children:"Don't put more into a single transaction than needed for integrity purposes."}),"\n"]}),"\n",(0,i.jsxs)(t.li,{children:["\n",(0,i.jsxs)(t.p,{children:["Don't leave connections dangling \u201cidle in transaction\u201d longer than necessary. The configuration parameter ",(0,i.jsx)(t.a,{href:"runtime-config-client.html#GUC-IDLE-IN-TRANSACTION-SESSION-TIMEOUT",children:"idle_in_transaction_session_timeout"})," may be used to automatically disconnect lingering sessions."]}),"\n"]}),"\n",(0,i.jsxs)(t.li,{children:["\n",(0,i.jsxs)(t.p,{children:["Eliminate explicit locks, ",(0,i.jsx)(t.code,{children:"SELECT FOR UPDATE"}),", and ",(0,i.jsx)(t.code,{children:"SELECT FOR SHARE"})," where no longer needed due to the protections automatically provided by Serializable transactions."]}),"\n"]}),"\n",(0,i.jsxs)(t.li,{children:["\n",(0,i.jsxs)(t.p,{children:["When the system is forced to combine multiple page-level predicate locks into a single relation-level predicate lock because the predicate lock table is short of memory, an increase in the rate of serialization failures may occur. You can avoid this by increasing ",(0,i.jsx)(t.a,{href:"runtime-config-locks.html#GUC-MAX-PRED-LOCKS-PER-TRANSACTION",children:"max_pred_locks_per_transaction"}),", ",(0,i.jsx)(t.a,{href:"runtime-config-locks.html#GUC-MAX-PRED-LOCKS-PER-RELATION",children:"max_pred_locks_per_relation"}),", and/or ",(0,i.jsx)(t.a,{href:"runtime-config-locks.html#GUC-MAX-PRED-LOCKS-PER-PAGE",children:"max_pred_locks_per_page"}),"."]}),"\n"]}),"\n",(0,i.jsxs)(t.li,{children:["\n",(0,i.jsxs)(t.p,{children:["A sequential scan will always necessitate a relation-level predicate lock. This can result in an increased rate of serialization failures. It may be helpful to encourage the use of index scans by reducing ",(0,i.jsx)(t.a,{href:"runtime-config-query.html#GUC-RANDOM-PAGE-COST",children:"random_page_cost"})," and/or increasing ",(0,i.jsx)(t.a,{href:"runtime-config-query.html#GUC-CPU-TUPLE-COST",children:"cpu_tuple_cost"}),". Be sure to weigh any decrease in transaction rollbacks and restarts against any overall change in query execution time."]}),"\n"]}),"\n"]}),"\n",(0,i.jsxs)(t.p,{children:["The Serializable isolation level is implemented using a technique known in academic database literature as Serializable Snapshot Isolation, which builds on Snapshot Isolation by adding checks for serialization anomalies. Some differences in behavior and performance may be observed when compared with other systems that use a traditional locking technique. Please see ",(0,i.jsx)(t.a,{href:"/demo_docu/biblio#ports12",children:"[ports12]"})," for detailed information."]})]})}function h(e={}){const{wrapper:t}={...(0,o.R)(),...e.components};return t?(0,i.jsx)(t,{...e,children:(0,i.jsx)(d,{...e})}):d(e)}}}]);