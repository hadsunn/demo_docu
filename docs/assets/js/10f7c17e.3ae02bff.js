"use strict";(self.webpackChunkpgdoc_02=self.webpackChunkpgdoc_02||[]).push([[6113],{5420:(e,t,r)=>{r.r(t),r.d(t,{assets:()=>d,contentTitle:()=>a,default:()=>h,frontMatter:()=>s,metadata:()=>i,toc:()=>c});const i=JSON.parse('{"id":"V. Server Programming/Triggers/trigger-definition","title":"39.1. Overview of Trigger Behavior","description":"Supported versions: 13 / 14 / 15 / 16 / 17","source":"@site/docs/V. Server Programming/39. Triggers/trigger-definition.md","sourceDirName":"V. Server Programming/39. Triggers","slug":"/V. Server Programming/Triggers/trigger-definition","permalink":"/demo_docu/V. Server Programming/Triggers/trigger-definition","draft":false,"unlisted":false,"tags":[],"version":"current","frontMatter":{"title":"39.1. Overview of Trigger Behavior"},"sidebar":"autoGeneratedSidebar","previous":{"title":"39.2. Visibility of Data Changes","permalink":"/demo_docu/V. Server Programming/Triggers/trigger-datachanges"},"next":{"title":"39.4. A Complete Trigger Example","permalink":"/demo_docu/V. Server Programming/Triggers/trigger-example"}}');var n=r(74848),o=r(28453);const s={title:"39.1. Overview of Trigger Behavior"},a=void 0,d={},c=[];function l(e){const t={a:"a",code:"code",em:"em",hr:"hr",li:"li",p:"p",ul:"ul",...(0,o.R)(),...e.components};return(0,n.jsxs)(n.Fragment,{children:[(0,n.jsx)(t.p,{children:"Supported versions: 13 / 14 / 15 / 16 / 17"}),"\n",(0,n.jsx)(t.p,{children:"Development versions: 18 / devel"}),"\n",(0,n.jsx)(t.p,{children:"Unsupported versions: 10 / 11 / 12 / 7.1 / 7.2 / 7.3 / 7.4 / 8.0 / 8.1 / 8.2 / 8.3 / 8.4 / 9.0 / 9.1 / 9.2 / 9.3 / 9.4 / 9.5 / 9.6"}),"\n",(0,n.jsx)(t.hr,{}),"\n",(0,n.jsx)(t.p,{children:"A trigger is a specification that the database should automatically execute a particular function whenever a certain type of operation is performed. Triggers can be attached to tables (partitioned or not), views, and foreign tables."}),"\n",(0,n.jsxs)(t.p,{children:["On tables and foreign tables, triggers can be defined to execute either before or after any ",(0,n.jsx)(t.code,{children:"INSERT"}),", ",(0,n.jsx)(t.code,{children:"UPDATE"}),", or ",(0,n.jsx)(t.code,{children:"DELETE"})," operation, either once per modified row, or once per SQL statement. ",(0,n.jsx)(t.code,{children:"UPDATE"})," triggers can moreover be set to fire only if certain columns are mentioned in the ",(0,n.jsx)(t.code,{children:"SET"})," clause of the ",(0,n.jsx)(t.code,{children:"UPDATE"})," statement. Triggers can also fire for ",(0,n.jsx)(t.code,{children:"TRUNCATE"})," statements. If a trigger event occurs, the trigger's function is called at the appropriate time to handle the event."]}),"\n",(0,n.jsxs)(t.p,{children:["On views, triggers can be defined to execute instead of ",(0,n.jsx)(t.code,{children:"INSERT"}),", ",(0,n.jsx)(t.code,{children:"UPDATE"}),", or ",(0,n.jsx)(t.code,{children:"DELETE"})," operations. Such ",(0,n.jsx)(t.code,{children:"INSTEAD OF"})," triggers are fired once for each row that needs to be modified in the view. It is the responsibility of the trigger's function to perform the necessary modifications to the view's underlying base table(s) and, where appropriate, return the modified row as it will appear in the view. Triggers on views can also be defined to execute once per SQL statement, before or after ",(0,n.jsx)(t.code,{children:"INSERT"}),", ",(0,n.jsx)(t.code,{children:"UPDATE"}),", or ",(0,n.jsx)(t.code,{children:"DELETE"})," operations. However, such triggers are fired only if there is also an ",(0,n.jsx)(t.code,{children:"INSTEAD OF"})," trigger on the view. Otherwise, any statement targeting the view must be rewritten into a statement affecting its underlying base table(s), and then the triggers that will be fired are the ones attached to the base table(s)."]}),"\n",(0,n.jsxs)(t.p,{children:["The trigger function must be defined before the trigger itself can be created. The trigger function must be declared as a function taking no arguments and returning type ",(0,n.jsx)(t.code,{children:"trigger"}),". (The trigger function receives its input through a specially-passed ",(0,n.jsx)(t.code,{children:"TriggerData"})," structure, not in the form of ordinary function arguments.)"]}),"\n",(0,n.jsxs)(t.p,{children:["Once a suitable trigger function has been created, the trigger is established with ",(0,n.jsx)(t.a,{href:"/VI.%20Reference/I.%20SQL%20Commands/sql-createtrigger.md",children:"CREATE TRIGGER"}),". The same trigger function can be used for multiple triggers."]}),"\n",(0,n.jsxs)(t.p,{children:["PostgreSQL offers both ",(0,n.jsx)(t.em,{children:"per-row"})," triggers and ",(0,n.jsx)(t.em,{children:"per-statement"})," triggers. With a per-row trigger, the trigger function is invoked once for each row that is affected by the statement that fired the trigger. In contrast, a per-statement trigger is invoked only once when an appropriate statement is executed, regardless of the number of rows affected by that statement. In particular, a statement that affects zero rows will still result in the execution of any applicable per-statement triggers. These two types of triggers are sometimes called ",(0,n.jsx)(t.em,{children:"row-level"})," triggers and ",(0,n.jsx)(t.em,{children:"statement-level"})," triggers, respectively. Triggers on ",(0,n.jsx)(t.code,{children:"TRUNCATE"})," may only be defined at statement level, not per-row."]}),"\n",(0,n.jsxs)(t.p,{children:["Triggers are also classified according to whether they fire ",(0,n.jsx)(t.em,{children:"before"})," , ",(0,n.jsx)(t.em,{children:"after"})," , or ",(0,n.jsx)(t.em,{children:"instead of"})," the operation. These are referred to as ",(0,n.jsx)(t.code,{children:"BEFORE"})," triggers, ",(0,n.jsx)(t.code,{children:"AFTER"})," triggers, and ",(0,n.jsx)(t.code,{children:"INSTEAD OF"})," triggers respectively. Statement-level ",(0,n.jsx)(t.code,{children:"BEFORE"})," triggers naturally fire before the statement starts to do anything, while statement-level ",(0,n.jsx)(t.code,{children:"AFTER"})," triggers fire at the very end of the statement. These types of triggers may be defined on tables, views, or foreign tables. Row-level ",(0,n.jsx)(t.code,{children:"BEFORE"})," triggers fire immediately before a particular row is operated on, while row-level ",(0,n.jsx)(t.code,{children:"AFTER"})," triggers fire at the end of the statement (but before any statement-level ",(0,n.jsx)(t.code,{children:"AFTER"})," triggers). These types of triggers may only be defined on tables and foreign tables, not views. ",(0,n.jsx)(t.code,{children:"INSTEAD OF"})," triggers may only be defined on views, and only at row level; they fire immediately as each row in the view is identified as needing to be operated on."]}),"\n",(0,n.jsxs)(t.p,{children:["The execution of an ",(0,n.jsx)(t.code,{children:"AFTER"})," trigger can be deferred to the end of the transaction, rather than the end of the statement, if it was defined as a ",(0,n.jsx)(t.em,{children:"constraint trigger"}),". In all cases, a trigger is executed as part of the same transaction as the statement that triggered it, so if either the statement or the trigger causes an error, the effects of both will be rolled back."]}),"\n",(0,n.jsx)(t.p,{children:"A statement that targets a parent table in an inheritance or partitioning hierarchy does not cause the statement-level triggers of affected child tables to be fired; only the parent table's statement-level triggers are fired. However, row-level triggers of any affected child tables will be fired."}),"\n",(0,n.jsxs)(t.p,{children:["If an ",(0,n.jsx)(t.code,{children:"INSERT"})," contains an ",(0,n.jsx)(t.code,{children:"ON CONFLICT DO UPDATE"})," clause, it is possible that the effects of row-level ",(0,n.jsx)(t.code,{children:"BEFORE"})," ",(0,n.jsx)(t.code,{children:"INSERT"})," triggers and row-level ",(0,n.jsx)(t.code,{children:"BEFORE"})," ",(0,n.jsx)(t.code,{children:"UPDATE"})," triggers can both be applied in a way that is apparent from the final state of the updated row, if an ",(0,n.jsx)(t.code,{children:"EXCLUDED"})," column is referenced. There need not be an ",(0,n.jsx)(t.code,{children:"EXCLUDED"})," column reference for both sets of row-level ",(0,n.jsx)(t.code,{children:"BEFORE"})," triggers to execute, though. The possibility of surprising outcomes should be considered when there are both ",(0,n.jsx)(t.code,{children:"BEFORE"})," ",(0,n.jsx)(t.code,{children:"INSERT"})," and ",(0,n.jsx)(t.code,{children:"BEFORE"})," ",(0,n.jsx)(t.code,{children:"UPDATE"})," row-level triggers that change a row being inserted/updated (this can be problematic even if the modifications are more or less equivalent, if they're not also idempotent). Note that statement-level ",(0,n.jsx)(t.code,{children:"UPDATE"})," triggers are executed when ",(0,n.jsx)(t.code,{children:"ON CONFLICT DO UPDATE"})," is specified, regardless of whether or not any rows were affected by the ",(0,n.jsx)(t.code,{children:"UPDATE"})," (and regardless of whether the alternative ",(0,n.jsx)(t.code,{children:"UPDATE"})," path was ever taken). An ",(0,n.jsx)(t.code,{children:"INSERT"})," with an ",(0,n.jsx)(t.code,{children:"ON CONFLICT DO UPDATE"})," clause will execute statement-level ",(0,n.jsx)(t.code,{children:"BEFORE"})," ",(0,n.jsx)(t.code,{children:"INSERT"})," triggers first, then statement-level ",(0,n.jsx)(t.code,{children:"BEFORE"})," ",(0,n.jsx)(t.code,{children:"UPDATE"})," triggers, followed by statement-level ",(0,n.jsx)(t.code,{children:"AFTER"})," ",(0,n.jsx)(t.code,{children:"UPDATE"})," triggers and finally statement-level ",(0,n.jsx)(t.code,{children:"AFTER"})," ",(0,n.jsx)(t.code,{children:"INSERT"})," triggers."]}),"\n",(0,n.jsxs)(t.p,{children:["If an ",(0,n.jsx)(t.code,{children:"UPDATE"})," on a partitioned table causes a row to move to another partition, it will be performed as a ",(0,n.jsx)(t.code,{children:"DELETE"})," from the original partition followed by an ",(0,n.jsx)(t.code,{children:"INSERT"})," into the new partition. In this case, all row-level ",(0,n.jsx)(t.code,{children:"BEFORE"})," ",(0,n.jsx)(t.code,{children:"UPDATE"})," triggers and all row-level ",(0,n.jsx)(t.code,{children:"BEFORE"})," ",(0,n.jsx)(t.code,{children:"DELETE"})," triggers are fired on the original partition. Then all row-level ",(0,n.jsx)(t.code,{children:"BEFORE"})," ",(0,n.jsx)(t.code,{children:"INSERT"})," triggers are fired on the destination partition. The possibility of surprising outcomes should be considered when all these triggers affect the row being moved. As far as ",(0,n.jsx)(t.code,{children:"AFTER ROW"})," triggers are concerned, ",(0,n.jsx)(t.code,{children:"AFTER"})," ",(0,n.jsx)(t.code,{children:"DELETE"})," and ",(0,n.jsx)(t.code,{children:"AFTER"})," ",(0,n.jsx)(t.code,{children:"INSERT"})," triggers are applied; but ",(0,n.jsx)(t.code,{children:"AFTER"})," ",(0,n.jsx)(t.code,{children:"UPDATE"})," triggers are not applied because the ",(0,n.jsx)(t.code,{children:"UPDATE"})," has been converted to a ",(0,n.jsx)(t.code,{children:"DELETE"})," and an ",(0,n.jsx)(t.code,{children:"INSERT"}),". As far as statement-level triggers are concerned, none of the ",(0,n.jsx)(t.code,{children:"DELETE"})," or ",(0,n.jsx)(t.code,{children:"INSERT"})," triggers are fired, even if row movement occurs; only the ",(0,n.jsx)(t.code,{children:"UPDATE"})," triggers defined on the target table used in the ",(0,n.jsx)(t.code,{children:"UPDATE"})," statement will be fired."]}),"\n",(0,n.jsxs)(t.p,{children:["No separate triggers are defined for ",(0,n.jsx)(t.code,{children:"MERGE"}),". Instead, statement-level or row-level ",(0,n.jsx)(t.code,{children:"UPDATE"}),", ",(0,n.jsx)(t.code,{children:"DELETE"}),", and ",(0,n.jsx)(t.code,{children:"INSERT"})," triggers are fired depending on (for statement-level triggers) what actions are specified in the ",(0,n.jsx)(t.code,{children:"MERGE"})," query and (for row-level triggers) what actions are performed."]}),"\n",(0,n.jsxs)(t.p,{children:["While running a ",(0,n.jsx)(t.code,{children:"MERGE"})," command, statement-level ",(0,n.jsx)(t.code,{children:"BEFORE"})," and ",(0,n.jsx)(t.code,{children:"AFTER"})," triggers are fired for events specified in the actions of the ",(0,n.jsx)(t.code,{children:"MERGE"})," command, irrespective of whether or not the action is ultimately performed. This is the same as an ",(0,n.jsx)(t.code,{children:"UPDATE"})," statement that updates no rows, yet statement-level triggers are fired. The row-level triggers are fired only when a row is actually updated, inserted or deleted. So it's perfectly legal that while statement-level triggers are fired for certain types of action, no row-level triggers are fired for the same kind of action."]}),"\n",(0,n.jsxs)(t.p,{children:["Trigger functions invoked by per-statement triggers should always return ",(0,n.jsx)(t.code,{children:"NULL"}),". Trigger functions invoked by per-row triggers can return a table row (a value of type ",(0,n.jsx)(t.code,{children:"HeapTuple"}),") to the calling executor, if they choose. A row-level trigger fired before an operation has the following choices:"]}),"\n",(0,n.jsxs)(t.ul,{children:["\n",(0,n.jsxs)(t.li,{children:["\n",(0,n.jsxs)(t.p,{children:["It can return ",(0,n.jsx)(t.code,{children:"NULL"})," to skip the operation for the current row. This instructs the executor to not perform the row-level operation that invoked the trigger (the insertion, modification, or deletion of a particular table row)."]}),"\n"]}),"\n",(0,n.jsxs)(t.li,{children:["\n",(0,n.jsxs)(t.p,{children:["For row-level ",(0,n.jsx)(t.code,{children:"INSERT"})," and ",(0,n.jsx)(t.code,{children:"UPDATE"})," triggers only, the returned row becomes the row that will be inserted or will replace the row being updated. This allows the trigger function to modify the row being inserted or updated."]}),"\n"]}),"\n"]}),"\n",(0,n.jsxs)(t.p,{children:["A row-level ",(0,n.jsx)(t.code,{children:"BEFORE"})," trigger that does not intend to cause either of these behaviors must be careful to return as its result the same row that was passed in (that is, the ",(0,n.jsx)(t.code,{children:"NEW"})," row for ",(0,n.jsx)(t.code,{children:"INSERT"})," and ",(0,n.jsx)(t.code,{children:"UPDATE"})," triggers, the ",(0,n.jsx)(t.code,{children:"OLD"})," row for ",(0,n.jsx)(t.code,{children:"DELETE"})," triggers)."]}),"\n",(0,n.jsxs)(t.p,{children:["A row-level ",(0,n.jsx)(t.code,{children:"INSTEAD OF"})," trigger should either return ",(0,n.jsx)(t.code,{children:"NULL"})," to indicate that it did not modify any data from the view's underlying base tables, or it should return the view row that was passed in (the ",(0,n.jsx)(t.code,{children:"NEW"})," row for ",(0,n.jsx)(t.code,{children:"INSERT"})," and ",(0,n.jsx)(t.code,{children:"UPDATE"})," operations, or the ",(0,n.jsx)(t.code,{children:"OLD"})," row for ",(0,n.jsx)(t.code,{children:"DELETE"})," operations). A nonnull return value is used to signal that the trigger performed the necessary data modifications in the view. This will cause the count of the number of rows affected by the command to be incremented. For ",(0,n.jsx)(t.code,{children:"INSERT"})," and ",(0,n.jsx)(t.code,{children:"UPDATE"})," operations only, the trigger may modify the ",(0,n.jsx)(t.code,{children:"NEW"})," row before returning it. This will change the data returned by ",(0,n.jsx)(t.code,{children:"INSERT RETURNING"})," or ",(0,n.jsx)(t.code,{children:"UPDATE RETURNING"}),", and is useful when the view will not show exactly the same data that was provided."]}),"\n",(0,n.jsxs)(t.p,{children:["The return value is ignored for row-level triggers fired after an operation, and so they can return ",(0,n.jsx)(t.code,{children:"NULL"}),"."]}),"\n",(0,n.jsxs)(t.p,{children:["Some considerations apply for generated columns. Stored generated columns are computed after ",(0,n.jsx)(t.code,{children:"BEFORE"})," triggers and before ",(0,n.jsx)(t.code,{children:"AFTER"})," triggers. Therefore, the generated value can be inspected in ",(0,n.jsx)(t.code,{children:"AFTER"})," triggers. In ",(0,n.jsx)(t.code,{children:"BEFORE"})," triggers, the ",(0,n.jsx)(t.code,{children:"OLD"})," row contains the old generated value, as one would expect, but the ",(0,n.jsx)(t.code,{children:"NEW"})," row does not yet contain the new generated value and should not be accessed. In the C language interface, the content of the column is undefined at this point; a higher-level programming language should prevent access to a stored generated column in the ",(0,n.jsx)(t.code,{children:"NEW"})," row in a ",(0,n.jsx)(t.code,{children:"BEFORE"})," trigger. Changes to the value of a generated column in a ",(0,n.jsx)(t.code,{children:"BEFORE"})," trigger are ignored and will be overwritten."]}),"\n",(0,n.jsxs)(t.p,{children:["If more than one trigger is defined for the same event on the same relation, the triggers will be fired in alphabetical order by trigger name. In the case of ",(0,n.jsx)(t.code,{children:"BEFORE"})," and ",(0,n.jsx)(t.code,{children:"INSTEAD OF"})," triggers, the possibly-modified row returned by each trigger becomes the input to the next trigger. If any ",(0,n.jsx)(t.code,{children:"BEFORE"})," or ",(0,n.jsx)(t.code,{children:"INSTEAD OF"})," trigger returns ",(0,n.jsx)(t.code,{children:"NULL"}),", the operation is abandoned for that row and subsequent triggers are not fired (for that row)."]}),"\n",(0,n.jsxs)(t.p,{children:["A trigger definition can also specify a Boolean ",(0,n.jsx)(t.code,{children:"WHEN"})," condition, which will be tested to see whether the trigger should be fired. In row-level triggers the ",(0,n.jsx)(t.code,{children:"WHEN"})," condition can examine the old and/or new values of columns of the row. (Statement-level triggers can also have ",(0,n.jsx)(t.code,{children:"WHEN"})," conditions, although the feature is not so useful for them.) In a ",(0,n.jsx)(t.code,{children:"BEFORE"})," trigger, the ",(0,n.jsx)(t.code,{children:"WHEN"})," condition is evaluated just before the function is or would be executed, so using ",(0,n.jsx)(t.code,{children:"WHEN"})," is not materially different from testing the same condition at the beginning of the trigger function. However, in an ",(0,n.jsx)(t.code,{children:"AFTER"})," trigger, the ",(0,n.jsx)(t.code,{children:"WHEN"})," condition is evaluated just after the row update occurs, and it determines whether an event is queued to fire the trigger at the end of statement. So when an ",(0,n.jsx)(t.code,{children:"AFTER"})," trigger's ",(0,n.jsx)(t.code,{children:"WHEN"})," condition does not return true, it is not necessary to queue an event nor to re-fetch the row at end of statement. This can result in significant speedups in statements that modify many rows, if the trigger only needs to be fired for a few of the rows. ",(0,n.jsx)(t.code,{children:"INSTEAD OF"})," triggers do not support ",(0,n.jsx)(t.code,{children:"WHEN"})," conditions."]}),"\n",(0,n.jsxs)(t.p,{children:["Typically, row-level ",(0,n.jsx)(t.code,{children:"BEFORE"})," triggers are used for checking or modifying the data that will be inserted or updated. For example, a ",(0,n.jsx)(t.code,{children:"BEFORE"})," trigger might be used to insert the current time into a ",(0,n.jsx)(t.code,{children:"timestamp"})," column, or to check that two elements of the row are consistent. Row-level ",(0,n.jsx)(t.code,{children:"AFTER"})," triggers are most sensibly used to propagate the updates to other tables, or make consistency checks against other tables. The reason for this division of labor is that an ",(0,n.jsx)(t.code,{children:"AFTER"})," trigger can be certain it is seeing the final value of the row, while a ",(0,n.jsx)(t.code,{children:"BEFORE"})," trigger cannot; there might be other ",(0,n.jsx)(t.code,{children:"BEFORE"})," triggers firing after it. If you have no specific reason to make a trigger ",(0,n.jsx)(t.code,{children:"BEFORE"})," or ",(0,n.jsx)(t.code,{children:"AFTER"}),", the ",(0,n.jsx)(t.code,{children:"BEFORE"})," case is more efficient, since the information about the operation doesn't have to be saved until end of statement."]}),"\n",(0,n.jsxs)(t.p,{children:["If a trigger function executes SQL commands then these commands might fire triggers again. This is known as cascading triggers. There is no direct limitation on the number of cascade levels. It is possible for cascades to cause a recursive invocation of the same trigger; for example, an ",(0,n.jsx)(t.code,{children:"INSERT"})," trigger might execute a command that inserts an additional row into the same table, causing the ",(0,n.jsx)(t.code,{children:"INSERT"})," trigger to be fired again. It is the trigger programmer's responsibility to avoid infinite recursion in such scenarios."]}),"\n",(0,n.jsxs)(t.p,{children:["When a trigger is being defined, arguments can be specified for it. The purpose of including arguments in the trigger definition is to allow different triggers with similar requirements to call the same function. As an example, there could be a generalized trigger function that takes as its arguments two column names and puts the current user in one and the current time stamp in the other. Properly written, this trigger function would be independent of the specific table it is triggering on. So the same function could be used for ",(0,n.jsx)(t.code,{children:"INSERT"})," events on any table with suitable columns, to automatically track creation of records in a transaction table for example. It could also be used to track last-update events if defined as an ",(0,n.jsx)(t.code,{children:"UPDATE"})," trigger."]}),"\n",(0,n.jsxs)(t.p,{children:["Each programming language that supports triggers has its own method for making the trigger input data available to the trigger function. This input data includes the type of trigger event (e.g., ",(0,n.jsx)(t.code,{children:"INSERT"})," or ",(0,n.jsx)(t.code,{children:"UPDATE"}),") as well as any arguments that were listed in ",(0,n.jsx)(t.code,{children:"CREATE TRIGGER"}),". For a row-level trigger, the input data also includes the ",(0,n.jsx)(t.code,{children:"NEW"})," row for ",(0,n.jsx)(t.code,{children:"INSERT"})," and ",(0,n.jsx)(t.code,{children:"UPDATE"})," triggers, and/or the ",(0,n.jsx)(t.code,{children:"OLD"})," row for ",(0,n.jsx)(t.code,{children:"UPDATE"})," and ",(0,n.jsx)(t.code,{children:"DELETE"})," triggers."]}),"\n",(0,n.jsxs)(t.p,{children:["By default, statement-level triggers do not have any way to examine the individual row(s) modified by the statement. But an ",(0,n.jsx)(t.code,{children:"AFTER STATEMENT"})," trigger can request that ",(0,n.jsx)(t.em,{children:"transition tables"})," be created to make the sets of affected rows available to the trigger. ",(0,n.jsx)(t.code,{children:"AFTER ROW"})," triggers can also request transition tables, so that they can see the total changes in the table as well as the change in the individual row they are currently being fired for. The method for examining the transition tables again depends on the programming language that is being used, but the typical approach is to make the transition tables act like read-only temporary tables that can be accessed by SQL commands issued within the trigger function."]})]})}function h(e={}){const{wrapper:t}={...(0,o.R)(),...e.components};return t?(0,n.jsx)(t,{...e,children:(0,n.jsx)(l,{...e})}):l(e)}},28453:(e,t,r)=>{r.d(t,{R:()=>s,x:()=>a});var i=r(96540);const n={},o=i.createContext(n);function s(e){const t=i.useContext(o);return i.useMemo(function(){return"function"==typeof e?e(t):{...t,...e}},[t,e])}function a(e){let t;return t=e.disableParentContext?"function"==typeof e.components?e.components(n):e.components||n:s(e.components),i.createElement(o.Provider,{value:t},e.children)}}}]);