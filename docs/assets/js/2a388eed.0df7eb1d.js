"use strict";(self.webpackChunkpgdoc_02=self.webpackChunkpgdoc_02||[]).push([[40547],{4246:(e,n,t)=>{t.r(n),t.d(n,{assets:()=>r,contentTitle:()=>d,default:()=>h,frontMatter:()=>a,metadata:()=>s,toc:()=>c});const s=JSON.parse('{"id":"VII. Internals/Index Access Method Interface Definition/index-functions","title":"64.2. Index Access Method Functions","description":"Supported versions: 13 / 14 / 15 / 16 / 17","source":"@site/docs/VII. Internals/64. Index Access Method Interface Definition/index-functions.md","sourceDirName":"VII. Internals/64. Index Access Method Interface Definition","slug":"/VII. Internals/Index Access Method Interface Definition/index-functions","permalink":"/demo_docu/VII. Internals/Index Access Method Interface Definition/index-functions","draft":false,"unlisted":false,"tags":[],"version":"current","frontMatter":{"title":"64.2. Index Access Method Functions"},"sidebar":"autoGeneratedSidebar","previous":{"title":"64.6. Index Cost Estimation Functions","permalink":"/demo_docu/VII. Internals/Index Access Method Interface Definition/index-cost-estimation"},"next":{"title":"64.4. Index Locking Considerations","permalink":"/demo_docu/VII. Internals/Index Access Method Interface Definition/index-locking"}}');var i=t(74848),o=t(28453);const a={title:"64.2. Index Access Method Functions"},d=void 0,r={},c=[];function l(e){const n={a:"a",code:"code",em:"em",hr:"hr",p:"p",pre:"pre",...(0,o.R)(),...e.components};return(0,i.jsxs)(i.Fragment,{children:[(0,i.jsx)(n.p,{children:"Supported versions: 13 / 14 / 15 / 16 / 17"}),"\n",(0,i.jsx)(n.p,{children:"Development versions: 18 / devel"}),"\n",(0,i.jsx)(n.p,{children:"Unsupported versions: 10 / 11 / 12 / 7.1 / 7.2 / 7.3 / 7.4 / 8.0 / 8.1 / 8.2 / 8.3 / 8.4 / 9.0 / 9.1 / 9.2 / 9.3 / 9.4 / 9.5 / 9.6"}),"\n",(0,i.jsx)(n.hr,{}),"\n",(0,i.jsxs)(n.p,{children:["The index construction and maintenance functions that an index access method must provide in ",(0,i.jsx)(n.code,{children:"IndexAmRoutine"})," are:"]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{children:"IndexBuildResult *\nambuild (Relation heapRelation,\n         Relation indexRelation,\n         IndexInfo *indexInfo);\n"})}),"\n",(0,i.jsxs)(n.p,{children:["Build a new index. The index relation has been physically created, but is empty. It must be filled in with whatever fixed data the access method requires, plus entries for all tuples already existing in the table. Ordinarily the ",(0,i.jsx)(n.code,{children:"ambuild"})," function will call ",(0,i.jsx)(n.code,{children:"table_index_build_scan()"})," to scan the table for existing tuples and compute the keys that need to be inserted into the index. The function must return a palloc'd struct containing statistics about the new index."]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{children:"void\nambuildempty (Relation indexRelation);\n"})}),"\n",(0,i.jsxs)(n.p,{children:["Build an empty index, and write it to the initialization fork (",(0,i.jsx)(n.code,{children:"INIT_FORKNUM"}),") of the given relation. This method is called only for unlogged indexes; the empty index written to the initialization fork will be copied over the main relation fork on each server restart."]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{children:"bool\naminsert (Relation indexRelation,\n          Datum *values,\n          bool *isnull,\n          ItemPointer heap_tid,\n          Relation heapRelation,\n          IndexUniqueCheck checkUnique,\n          bool indexUnchanged,\n          IndexInfo *indexInfo);\n"})}),"\n",(0,i.jsxs)(n.p,{children:["Insert a new tuple into an existing index. The ",(0,i.jsx)(n.code,{children:"values"})," and ",(0,i.jsx)(n.code,{children:"isnull"})," arrays give the key values to be indexed, and ",(0,i.jsx)(n.code,{children:"heap_tid"})," is the TID to be indexed. If the access method supports unique indexes (its ",(0,i.jsx)(n.code,{children:"amcanunique"})," flag is true) then ",(0,i.jsx)(n.code,{children:"checkUnique"})," indicates the type of uniqueness check to perform. This varies depending on whether the unique constraint is deferrable; see ",(0,i.jsx)(n.a,{href:"/demo_docu/VII.%20Internals/Index%20Access%20Method%20Interface%20Definition/index-unique-checks",children:"Section 64.5"})," for details. Normally the access method only needs the ",(0,i.jsx)(n.code,{children:"heapRelation"})," parameter when performing uniqueness checking (since then it will have to look into the heap to verify tuple liveness)."]}),"\n",(0,i.jsxs)(n.p,{children:["The ",(0,i.jsx)(n.code,{children:"indexUnchanged"})," Boolean value gives a hint about the nature of the tuple to be indexed. When it is true, the tuple is a duplicate of some existing tuple in the index. The new tuple is a logically unchanged successor MVCC tuple version. This happens when an ",(0,i.jsx)(n.code,{children:"UPDATE"})," takes place that does not modify any columns covered by the index, but nevertheless requires a new version in the index. The index AM may use this hint to decide to apply bottom-up index deletion in parts of the index where many versions of the same logical row accumulate. Note that updating a non-key column or a column that only appears in a partial index predicate does not affect the value of ",(0,i.jsx)(n.code,{children:"indexUnchanged"}),". The core code determines each tuple's ",(0,i.jsx)(n.code,{children:"indexUnchanged"})," value using a low overhead approach that allows both false positives and false negatives. Index AMs must not treat ",(0,i.jsx)(n.code,{children:"indexUnchanged"})," as an authoritative source of information about tuple visibility or versioning."]}),"\n",(0,i.jsxs)(n.p,{children:["The function's Boolean result value is significant only when ",(0,i.jsx)(n.code,{children:"checkUnique"})," is ",(0,i.jsx)(n.code,{children:"UNIQUE_CHECK_PARTIAL"}),". In this case a true result means the new entry is known unique, whereas false means it might be non-unique (and a deferred uniqueness check must be scheduled). For other cases a constant false result is recommended."]}),"\n",(0,i.jsxs)(n.p,{children:["Some indexes might not index all tuples. If the tuple is not to be indexed, ",(0,i.jsx)(n.code,{children:"aminsert"})," should just return without doing anything."]}),"\n",(0,i.jsxs)(n.p,{children:["If the index AM wishes to cache data across successive index insertions within an SQL statement, it can allocate space in ",(0,i.jsx)(n.code,{children:"indexInfo->ii_Context"})," and store a pointer to the data in ",(0,i.jsx)(n.code,{children:"indexInfo->ii_AmCache"})," (which will be NULL initially)."]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{children:"IndexBulkDeleteResult *\nambulkdelete (IndexVacuumInfo *info,\n              IndexBulkDeleteResult *stats,\n              IndexBulkDeleteCallback callback,\n              void *callback_state);\n"})}),"\n",(0,i.jsxs)(n.p,{children:["Delete tuple(s) from the index. This is a \u201cbulk delete\u201d operation that is intended to be implemented by scanning the whole index and checking each entry to see if it should be deleted. The passed-in ",(0,i.jsx)(n.code,{children:"callback"})," function must be called, in the style ",(0,i.jsx)(n.code,{children:"callback(_"}),"TID",(0,i.jsx)(n.code,{children:"_ , callback_state) returns bool"}),", to determine whether any particular index entry, as identified by its referenced TID, is to be deleted. Must return either NULL or a palloc'd struct containing statistics about the effects of the deletion operation. It is OK to return NULL if no information needs to be passed on to ",(0,i.jsx)(n.code,{children:"amvacuumcleanup"}),"."]}),"\n",(0,i.jsxs)(n.p,{children:["Because of limited ",(0,i.jsx)(n.code,{children:"maintenance_work_mem"}),", ",(0,i.jsx)(n.code,{children:"ambulkdelete"})," might need to be called more than once when many tuples are to be deleted. The ",(0,i.jsx)(n.code,{children:"stats"})," argument is the result of the previous call for this index (it is NULL for the first call within a ",(0,i.jsx)(n.code,{children:"VACUUM"})," operation). This allows the AM to accumulate statistics across the whole operation. Typically, ",(0,i.jsx)(n.code,{children:"ambulkdelete"})," will modify and return the same struct if the passed ",(0,i.jsx)(n.code,{children:"stats"})," is not null."]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{children:"IndexBulkDeleteResult *\namvacuumcleanup (IndexVacuumInfo *info,\n                 IndexBulkDeleteResult *stats);\n"})}),"\n",(0,i.jsxs)(n.p,{children:["Clean up after a ",(0,i.jsx)(n.code,{children:"VACUUM"})," operation (zero or more ",(0,i.jsx)(n.code,{children:"ambulkdelete"})," calls). This does not have to do anything beyond returning index statistics, but it might perform bulk cleanup such as reclaiming empty index pages. ",(0,i.jsx)(n.code,{children:"stats"})," is whatever the last ",(0,i.jsx)(n.code,{children:"ambulkdelete"})," call returned, or NULL if ",(0,i.jsx)(n.code,{children:"ambulkdelete"})," was not called because no tuples needed to be deleted. If the result is not NULL it must be a palloc'd struct. The statistics it contains will be used to update ",(0,i.jsx)(n.code,{children:"pg_class"}),", and will be reported by ",(0,i.jsx)(n.code,{children:"VACUUM"})," if ",(0,i.jsx)(n.code,{children:"VERBOSE"})," is given. It is OK to return NULL if the index was not changed at all during the ",(0,i.jsx)(n.code,{children:"VACUUM"})," operation, but otherwise correct stats should be returned."]}),"\n",(0,i.jsxs)(n.p,{children:[(0,i.jsx)(n.code,{children:"amvacuumcleanup"})," will also be called at completion of an ",(0,i.jsx)(n.code,{children:"ANALYZE"})," operation. In this case ",(0,i.jsx)(n.code,{children:"stats"})," is always NULL and any return value will be ignored. This case can be distinguished by checking ",(0,i.jsx)(n.code,{children:"info->analyze_only"}),". It is recommended that the access method do nothing except post-insert cleanup in such a call, and that only in an autovacuum worker process."]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{children:"bool\namcanreturn (Relation indexRelation, int attno);\n"})}),"\n",(0,i.jsxs)(n.p,{children:["Check whether the index can support ",(0,i.jsx)(n.a,{href:"/demo_docu/II.%20The%20SQL%20Language/Indexes/indexes-index-only-scans",children:(0,i.jsx)(n.em,{children:"index-only scans"})})," on the given column, by returning the column's original indexed value. The attribute number is 1-based, i.e., the first column's attno is 1. Returns true if supported, else false. This function should always return true for included columns (if those are supported), since there's little point in an included column that can't be retrieved. If the access method does not support index-only scans at all, the ",(0,i.jsx)(n.code,{children:"amcanreturn"})," field in its ",(0,i.jsx)(n.code,{children:"IndexAmRoutine"})," struct can be set to NULL."]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{children:"void\namcostestimate (PlannerInfo *root,\n                IndexPath *path,\n                double loop_count,\n                Cost *indexStartupCost,\n                Cost *indexTotalCost,\n                Selectivity *indexSelectivity,\n                double *indexCorrelation,\n                double *indexPages);\n"})}),"\n",(0,i.jsxs)(n.p,{children:["Estimate the costs of an index scan. This function is described fully in ",(0,i.jsx)(n.a,{href:"/demo_docu/VII.%20Internals/Index%20Access%20Method%20Interface%20Definition/index-cost-estimation",children:"Section 64.6"}),", below."]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{children:"bytea *\namoptions (ArrayType *reloptions,\n           bool validate);\n"})}),"\n",(0,i.jsxs)(n.p,{children:["Parse and validate the reloptions array for an index. This is called only when a non-null reloptions array exists for the index. ",(0,i.jsx)(n.em,{children:(0,i.jsx)(n.code,{children:"reloptions"})})," is a ",(0,i.jsx)(n.code,{children:"text"})," array containing entries of the form ",(0,i.jsx)(n.em,{children:(0,i.jsx)(n.code,{children:"name"})}),(0,i.jsx)(n.code,{children:"="}),(0,i.jsx)(n.em,{children:(0,i.jsx)(n.code,{children:"value"})}),". The function should construct a ",(0,i.jsx)(n.code,{children:"bytea"})," value, which will be copied into the ",(0,i.jsx)(n.code,{children:"rd_options"})," field of the index's relcache entry. The data contents of the ",(0,i.jsx)(n.code,{children:"bytea"})," value are open for the access method to define; most of the standard access methods use struct ",(0,i.jsx)(n.code,{children:"StdRdOptions"}),". When ",(0,i.jsx)(n.em,{children:(0,i.jsx)(n.code,{children:"validate"})})," is true, the function should report a suitable error message if any of the options are unrecognized or have invalid values; when ",(0,i.jsx)(n.em,{children:(0,i.jsx)(n.code,{children:"validate"})})," is false, invalid entries should be silently ignored. (",(0,i.jsx)(n.em,{children:(0,i.jsx)(n.code,{children:"validate"})})," is false when loading options already stored in ",(0,i.jsx)(n.code,{children:"pg_catalog"}),"; an invalid entry could only be found if the access method has changed its rules for options, and in that case ignoring obsolete entries is appropriate.) It is OK to return NULL if default behavior is wanted."]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{children:"bool\namproperty (Oid index_oid, int attno,\n            IndexAMProperty prop, const char *propname,\n            bool *res, bool *isnull);\n"})}),"\n",(0,i.jsxs)(n.p,{children:["The ",(0,i.jsx)(n.code,{children:"amproperty"})," method allows index access methods to override the default behavior of ",(0,i.jsx)(n.code,{children:"pg_index_column_has_property"})," and related functions. If the access method does not have any special behavior for index property inquiries, the ",(0,i.jsx)(n.code,{children:"amproperty"})," field in its ",(0,i.jsx)(n.code,{children:"IndexAmRoutine"})," struct can be set to NULL. Otherwise, the ",(0,i.jsx)(n.code,{children:"amproperty"})," method will be called with ",(0,i.jsx)(n.em,{children:(0,i.jsx)(n.code,{children:"index_oid"})})," and ",(0,i.jsx)(n.em,{children:(0,i.jsx)(n.code,{children:"attno"})})," both zero for ",(0,i.jsx)(n.code,{children:"pg_indexam_has_property"})," calls, or with ",(0,i.jsx)(n.em,{children:(0,i.jsx)(n.code,{children:"index_oid"})})," valid and ",(0,i.jsx)(n.em,{children:(0,i.jsx)(n.code,{children:"attno"})})," zero for ",(0,i.jsx)(n.code,{children:"pg_index_has_property"})," calls, or with ",(0,i.jsx)(n.em,{children:(0,i.jsx)(n.code,{children:"index_oid"})})," valid and ",(0,i.jsx)(n.em,{children:(0,i.jsx)(n.code,{children:"attno"})})," greater than zero for ",(0,i.jsx)(n.code,{children:"pg_index_column_has_property"})," calls. ",(0,i.jsx)(n.em,{children:(0,i.jsx)(n.code,{children:"prop"})})," is an enum value identifying the property being tested, while ",(0,i.jsx)(n.em,{children:(0,i.jsx)(n.code,{children:"propname"})})," is the original property name string. If the core code does not recognize the property name then ",(0,i.jsx)(n.em,{children:(0,i.jsx)(n.code,{children:"prop"})})," is ",(0,i.jsx)(n.code,{children:"AMPROP_UNKNOWN"}),". Access methods can define custom property names by checking ",(0,i.jsx)(n.em,{children:(0,i.jsx)(n.code,{children:"propname"})})," for a match (use ",(0,i.jsx)(n.code,{children:"pg_strcasecmp"})," to match, for consistency with the core code); for names known to the core code, it's better to inspect ",(0,i.jsx)(n.em,{children:(0,i.jsx)(n.code,{children:"prop"})}),". If the ",(0,i.jsx)(n.code,{children:"amproperty"})," method returns ",(0,i.jsx)(n.code,{children:"true"})," then it has determined the property test result: it must set ",(0,i.jsx)(n.code,{children:"*res"})," to the Boolean value to return, or set ",(0,i.jsx)(n.code,{children:"*isnull"})," to ",(0,i.jsx)(n.code,{children:"true"})," to return a NULL. (Both of the referenced variables are initialized to ",(0,i.jsx)(n.code,{children:"false"})," before the call.) If the ",(0,i.jsx)(n.code,{children:"amproperty"})," method returns ",(0,i.jsx)(n.code,{children:"false"})," then the core code will proceed with its normal logic for determining the property test result."]}),"\n",(0,i.jsxs)(n.p,{children:["Access methods that support ordering operators should implement ",(0,i.jsx)(n.code,{children:"AMPROP_DISTANCE_ORDERABLE"})," property testing, as the core code does not know how to do that and will return NULL. It may also be advantageous to implement ",(0,i.jsx)(n.code,{children:"AMPROP_RETURNABLE"})," testing, if that can be done more cheaply than by opening the index and calling ",(0,i.jsx)(n.code,{children:"amcanreturn"}),", which is the core code's default behavior. The default behavior should be satisfactory for all other standard properties."]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{children:"char *\nambuildphasename (int64 phasenum);\n"})}),"\n",(0,i.jsxs)(n.p,{children:["Return the textual name of the given build phase number. The phase numbers are those reported during an index build via the ",(0,i.jsx)(n.code,{children:"pgstat_progress_update_param"})," interface. The phase names are then exposed in the ",(0,i.jsx)(n.code,{children:"pg_stat_progress_create_index"})," view."]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{children:"bool\namvalidate (Oid opclassoid);\n"})}),"\n",(0,i.jsxs)(n.p,{children:["Validate the catalog entries for the specified operator class, so far as the access method can reasonably do that. For example, this might include testing that all required support functions are provided. The ",(0,i.jsx)(n.code,{children:"amvalidate"})," function must return false if the opclass is invalid. Problems should be reported with ",(0,i.jsx)(n.code,{children:"ereport"})," messages, typically at ",(0,i.jsx)(n.code,{children:"INFO"})," level."]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{children:"void\namadjustmembers (Oid opfamilyoid,\n                 Oid opclassoid,\n                 List *operators,\n                 List *functions);\n"})}),"\n",(0,i.jsxs)(n.p,{children:["Validate proposed new operator and function members of an operator family, so far as the access method can reasonably do that, and set their dependency types if the default is not satisfactory. This is called during ",(0,i.jsx)(n.code,{children:"CREATE OPERATOR CLASS"})," and during ",(0,i.jsx)(n.code,{children:"ALTER OPERATOR FAMILY ADD"}),"; in the latter case ",(0,i.jsx)(n.em,{children:(0,i.jsx)(n.code,{children:"opclassoid"})})," is ",(0,i.jsx)(n.code,{children:"InvalidOid"}),". The ",(0,i.jsx)(n.code,{children:"List"})," arguments are lists of ",(0,i.jsx)(n.code,{children:"OpFamilyMember"})," structs, as defined in ",(0,i.jsx)(n.code,{children:"amapi.h"}),". Tests done by this function will typically be a subset of those performed by ",(0,i.jsx)(n.code,{children:"amvalidate"}),", since ",(0,i.jsx)(n.code,{children:"amadjustmembers"})," cannot assume that it is seeing a complete set of members. For example, it would be reasonable to check the signature of a support function, but not to check whether all required support functions are provided. Any problems can be reported by throwing an error. The dependency-related fields of the ",(0,i.jsx)(n.code,{children:"OpFamilyMember"})," structs are initialized by the core code to create hard dependencies on the opclass if this is ",(0,i.jsx)(n.code,{children:"CREATE OPERATOR CLASS"}),", or soft dependencies on the opfamily if this is ",(0,i.jsx)(n.code,{children:"ALTER OPERATOR FAMILY ADD"}),". ",(0,i.jsx)(n.code,{children:"amadjustmembers"})," can adjust these fields if some other behavior is more appropriate. For example, GIN, GiST, and SP-GiST always set operator members to have soft dependencies on the opfamily, since the connection between an operator and an opclass is relatively weak in these index types; so it is reasonable to allow operator members to be added and removed freely. Optional support functions are typically also given soft dependencies, so that they can be removed if necessary."]}),"\n",(0,i.jsxs)(n.p,{children:["The purpose of an index, of course, is to support scans for tuples matching an indexable ",(0,i.jsx)(n.code,{children:"WHERE"})," condition, often called a ",(0,i.jsx)(n.em,{children:"qualifier"})," or ",(0,i.jsx)(n.em,{children:"scan key"}),". The semantics of index scanning are described more fully in ",(0,i.jsx)(n.a,{href:"/demo_docu/VII.%20Internals/Index%20Access%20Method%20Interface%20Definition/index-scanning",children:"Section 64.3"}),", below. An index access method can support \u201cplain\u201d index scans, \u201cbitmap\u201d index scans, or both. The scan-related functions that an index access method must or may provide are:"]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{children:"IndexScanDesc\nambeginscan (Relation indexRelation,\n             int nkeys,\n             int norderbys);\n"})}),"\n",(0,i.jsxs)(n.p,{children:["Prepare for an index scan. The ",(0,i.jsx)(n.code,{children:"nkeys"})," and ",(0,i.jsx)(n.code,{children:"norderbys"})," parameters indicate the number of quals and ordering operators that will be used in the scan; these may be useful for space allocation purposes. Note that the actual values of the scan keys aren't provided yet. The result must be a palloc'd struct. For implementation reasons the index access method ",(0,i.jsx)(n.em,{children:"must"})," create this struct by calling ",(0,i.jsx)(n.code,{children:"RelationGetIndexScan()"}),". In most cases ",(0,i.jsx)(n.code,{children:"ambeginscan"})," does little beyond making that call and perhaps acquiring locks; the interesting parts of index-scan startup are in ",(0,i.jsx)(n.code,{children:"amrescan"}),"."]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{children:"void\namrescan (IndexScanDesc scan,\n          ScanKey keys,\n          int nkeys,\n          ScanKey orderbys,\n          int norderbys);\n"})}),"\n",(0,i.jsxs)(n.p,{children:["Start or restart an index scan, possibly with new scan keys. (To restart using previously-passed keys, NULL is passed for ",(0,i.jsx)(n.code,{children:"keys"})," and/or ",(0,i.jsx)(n.code,{children:"orderbys"}),".) Note that it is not allowed for the number of keys or order-by operators to be larger than what was passed to ",(0,i.jsx)(n.code,{children:"ambeginscan"}),". In practice the restart feature is used when a new outer tuple is selected by a nested-loop join and so a new key comparison value is needed, but the scan key structure remains the same."]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{children:"bool\namgettuple (IndexScanDesc scan,\n            ScanDirection direction);\n"})}),"\n",(0,i.jsxs)(n.p,{children:["Fetch the next tuple in the given scan, moving in the given direction (forward or backward in the index). Returns true if a tuple was obtained, false if no matching tuples remain. In the true case the tuple TID is stored into the ",(0,i.jsx)(n.code,{children:"scan"})," structure. Note that \u201csuccess\u201d means only that the index contains an entry that matches the scan keys, not that the tuple necessarily still exists in the heap or will pass the caller's snapshot test. On success, ",(0,i.jsx)(n.code,{children:"amgettuple"})," must also set ",(0,i.jsx)(n.code,{children:"scan->xs_recheck"})," to true or false. False means it is certain that the index entry matches the scan keys. True means this is not certain, and the conditions represented by the scan keys must be rechecked against the heap tuple after fetching it. This provision supports \u201clossy\u201d index operators. Note that rechecking will extend only to the scan conditions; a partial index predicate (if any) is never rechecked by ",(0,i.jsx)(n.code,{children:"amgettuple"})," callers."]}),"\n",(0,i.jsxs)(n.p,{children:["If the index supports ",(0,i.jsx)(n.a,{href:"/demo_docu/II.%20The%20SQL%20Language/Indexes/indexes-index-only-scans",children:"index-only scans"})," (i.e., ",(0,i.jsx)(n.code,{children:"amcanreturn"})," returns true for any of its columns), then on success the AM must also check ",(0,i.jsx)(n.code,{children:"scan->xs_want_itup"}),", and if that is true it must return the originally indexed data for the index entry. Columns for which ",(0,i.jsx)(n.code,{children:"amcanreturn"})," returns false can be returned as nulls. The data can be returned in the form of an ",(0,i.jsx)(n.code,{children:"IndexTuple"})," pointer stored at ",(0,i.jsx)(n.code,{children:"scan->xs_itup"}),", with tuple descriptor ",(0,i.jsx)(n.code,{children:"scan->xs_itupdesc"}),"; or in the form of a ",(0,i.jsx)(n.code,{children:"HeapTuple"})," pointer stored at ",(0,i.jsx)(n.code,{children:"scan->xs_hitup"}),", with tuple descriptor ",(0,i.jsx)(n.code,{children:"scan->xs_hitupdesc"}),". (The latter format should be used when reconstructing data that might possibly not fit into an ",(0,i.jsx)(n.code,{children:"IndexTuple"}),".) In either case, management of the data referenced by the pointer is the access method's responsibility. The data must remain good at least until the next ",(0,i.jsx)(n.code,{children:"amgettuple"}),", ",(0,i.jsx)(n.code,{children:"amrescan"}),", or ",(0,i.jsx)(n.code,{children:"amendscan"})," call for the scan."]}),"\n",(0,i.jsxs)(n.p,{children:["The ",(0,i.jsx)(n.code,{children:"amgettuple"})," function need only be provided if the access method supports \u201cplain\u201d index scans. If it doesn't, the ",(0,i.jsx)(n.code,{children:"amgettuple"})," field in its ",(0,i.jsx)(n.code,{children:"IndexAmRoutine"})," struct must be set to NULL."]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{children:"int64\namgetbitmap (IndexScanDesc scan,\n             TIDBitmap *tbm);\n"})}),"\n",(0,i.jsxs)(n.p,{children:["Fetch all tuples in the given scan and add them to the caller-supplied ",(0,i.jsx)(n.code,{children:"TIDBitmap"})," (that is, OR the set of tuple IDs into whatever set is already in the bitmap). The number of tuples fetched is returned (this might be just an approximate count, for instance some AMs do not detect duplicates). While inserting tuple IDs into the bitmap, ",(0,i.jsx)(n.code,{children:"amgetbitmap"})," can indicate that rechecking of the scan conditions is required for specific tuple IDs. This is analogous to the ",(0,i.jsx)(n.code,{children:"xs_recheck"})," output parameter of ",(0,i.jsx)(n.code,{children:"amgettuple"}),". Note: in the current implementation, support for this feature is conflated with support for lossy storage of the bitmap itself, and therefore callers recheck both the scan conditions and the partial index predicate (if any) for recheckable tuples. That might not always be true, however. ",(0,i.jsx)(n.code,{children:"amgetbitmap"})," and ",(0,i.jsx)(n.code,{children:"amgettuple"})," cannot be used in the same index scan; there are other restrictions too when using ",(0,i.jsx)(n.code,{children:"amgetbitmap"}),", as explained in ",(0,i.jsx)(n.a,{href:"/demo_docu/VII.%20Internals/Index%20Access%20Method%20Interface%20Definition/index-scanning",children:"Section 64.3"}),"."]}),"\n",(0,i.jsxs)(n.p,{children:["The ",(0,i.jsx)(n.code,{children:"amgetbitmap"})," function need only be provided if the access method supports \u201cbitmap\u201d index scans. If it doesn't, the ",(0,i.jsx)(n.code,{children:"amgetbitmap"})," field in its ",(0,i.jsx)(n.code,{children:"IndexAmRoutine"})," struct must be set to NULL."]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{children:"void\namendscan (IndexScanDesc scan);\n"})}),"\n",(0,i.jsxs)(n.p,{children:["End a scan and release resources. The ",(0,i.jsx)(n.code,{children:"scan"})," struct itself should not be freed, but any locks or pins taken internally by the access method must be released, as well as any other memory allocated by ",(0,i.jsx)(n.code,{children:"ambeginscan"})," and other scan-related functions."]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{children:"void\nammarkpos (IndexScanDesc scan);\n"})}),"\n",(0,i.jsx)(n.p,{children:"Mark current scan position. The access method need only support one remembered scan position per scan."}),"\n",(0,i.jsxs)(n.p,{children:["The ",(0,i.jsx)(n.code,{children:"ammarkpos"})," function need only be provided if the access method supports ordered scans. If it doesn't, the ",(0,i.jsx)(n.code,{children:"ammarkpos"})," field in its ",(0,i.jsx)(n.code,{children:"IndexAmRoutine"})," struct may be set to NULL."]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{children:"void\namrestrpos (IndexScanDesc scan);\n"})}),"\n",(0,i.jsx)(n.p,{children:"Restore the scan to the most recently marked position."}),"\n",(0,i.jsxs)(n.p,{children:["The ",(0,i.jsx)(n.code,{children:"amrestrpos"})," function need only be provided if the access method supports ordered scans. If it doesn't, the ",(0,i.jsx)(n.code,{children:"amrestrpos"})," field in its ",(0,i.jsx)(n.code,{children:"IndexAmRoutine"})," struct may be set to NULL."]}),"\n",(0,i.jsxs)(n.p,{children:["In addition to supporting ordinary index scans, some types of index may wish to support ",(0,i.jsx)(n.em,{children:"parallel index scans"})," , which allow multiple backends to cooperate in performing an index scan. The index access method should arrange things so that each cooperating process returns a subset of the tuples that would be performed by an ordinary, non-parallel index scan, but in such a way that the union of those subsets is equal to the set of tuples that would be returned by an ordinary, non-parallel index scan. Furthermore, while there need not be any global ordering of tuples returned by a parallel scan, the ordering of that subset of tuples returned within each cooperating backend must match the requested ordering. The following functions may be implemented to support parallel index scans:"]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{children:"Size\namestimateparallelscan (void);\n"})}),"\n",(0,i.jsxs)(n.p,{children:["Estimate and return the number of bytes of dynamic shared memory which the access method will be needed to perform a parallel scan. (This number is in addition to, not in lieu of, the amount of space needed for AM-independent data in ",(0,i.jsx)(n.code,{children:"ParallelIndexScanDescData"}),".)"]}),"\n",(0,i.jsx)(n.p,{children:"It is not necessary to implement this function for access methods which do not support parallel scans or for which the number of additional bytes of storage required is zero."}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{children:"void\naminitparallelscan (void *target);\n"})}),"\n",(0,i.jsxs)(n.p,{children:["This function will be called to initialize dynamic shared memory at the beginning of a parallel scan. ",(0,i.jsx)(n.em,{children:(0,i.jsx)(n.code,{children:"target"})})," will point to at least the number of bytes previously returned by ",(0,i.jsx)(n.code,{children:"amestimateparallelscan"}),", and this function may use that amount of space to store whatever data it wishes."]}),"\n",(0,i.jsx)(n.p,{children:"It is not necessary to implement this function for access methods which do not support parallel scans or in cases where the shared memory space required needs no initialization."}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{children:"void\namparallelrescan (IndexScanDesc scan);\n"})}),"\n",(0,i.jsxs)(n.p,{children:["This function, if implemented, will be called when a parallel index scan must be restarted. It should reset any shared state set up by ",(0,i.jsx)(n.code,{children:"aminitparallelscan"})," such that the scan will be restarted from the beginning."]})]})}function h(e={}){const{wrapper:n}={...(0,o.R)(),...e.components};return n?(0,i.jsx)(n,{...e,children:(0,i.jsx)(l,{...e})}):l(e)}},28453:(e,n,t)=>{t.d(n,{R:()=>a,x:()=>d});var s=t(96540);const i={},o=s.createContext(i);function a(e){const n=s.useContext(o);return s.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function d(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(i):e.components||i:a(e.components),s.createElement(o.Provider,{value:n},e.children)}}}]);