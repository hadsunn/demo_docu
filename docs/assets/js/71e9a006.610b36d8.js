"use strict";(self.webpackChunkpgdoc_02=self.webpackChunkpgdoc_02||[]).push([[78168],{28453:(e,t,n)=>{n.d(t,{R:()=>a,x:()=>o});var i=n(96540);const r={},s=i.createContext(r);function a(e){const t=i.useContext(s);return i.useMemo(function(){return"function"==typeof e?e(t):{...t,...e}},[t,e])}function o(e){let t;return t=e.disableParentContext?"function"==typeof e.components?e.components(r):e.components||r:a(e.components),i.createElement(s.Provider,{value:t},e.children)}},65971:(e,t,n)=>{n.r(t),n.d(t,{assets:()=>d,contentTitle:()=>o,default:()=>h,frontMatter:()=>a,metadata:()=>i,toc:()=>c});const i=JSON.parse('{"id":"II. The SQL Language/Queries/queries-with","title":"7.8. `WITH` Queries (Common Table Expressions)","description":"Supported versions: 13 / 14 / 15 / 16 / 17","source":"@site/docs/II. The SQL Language/7. Queries/queries-with.md","sourceDirName":"II. The SQL Language/7. Queries","slug":"/II. The SQL Language/Queries/queries-with","permalink":"/demo_docu/II. The SQL Language/Queries/queries-with","draft":false,"unlisted":false,"tags":[],"version":"current","frontMatter":{"title":"7.8. `WITH` Queries (Common Table Expressions)"},"sidebar":"autoGeneratedSidebar","previous":{"title":"7.7. `VALUES` Lists","permalink":"/demo_docu/II. The SQL Language/Queries/queries-values"},"next":{"title":"Chapter 8. Data Types","permalink":"/demo_docu/II. The SQL Language/Data Types/"}}');var r=n(74848),s=n(28453);const a={title:"7.8. `WITH` Queries (Common Table Expressions)"},o=void 0,d={},c=[{value:"7.8.1. <code>SELECT</code> in <code>WITH</code>",id:"781-select-in-with",level:3},{value:"7.8.2. Recursive Queries",id:"782-recursive-queries",level:3},{value:"Note",id:"note",level:3},{value:"7.8.2.1. Search Order",id:"7821-search-order",level:4},{value:"Tip",id:"tip",level:3},{value:"Tip",id:"tip-1",level:3},{value:"7.8.2.2. Cycle Detection",id:"7822-cycle-detection",level:4},{value:"Tip",id:"tip-2",level:3},{value:"Tip",id:"tip-3",level:3},{value:"7.8.3. Common Table Expression Materialization",id:"783-common-table-expression-materialization",level:3},{value:"7.8.4. Data-Modifying Statements in <code>WITH</code>",id:"784-data-modifying-statements-in-with",level:3}];function l(e){const t={a:"a",code:"code",em:"em",h3:"h3",h4:"h4",hr:"hr",li:"li",ol:"ol",p:"p",pre:"pre",strong:"strong",...(0,s.R)(),...e.components};return(0,r.jsxs)(r.Fragment,{children:[(0,r.jsx)(t.p,{children:"Supported versions: 13 / 14 / 15 / 16 / 17"}),"\n",(0,r.jsx)(t.p,{children:"Development versions: 18 / devel"}),"\n",(0,r.jsx)(t.p,{children:"Unsupported versions: 10 / 11 / 12 / 7.1 / 7.2 / 7.3 / 7.4 / 8.0 / 8.1 / 8.2 / 8.3 / 8.4 / 9.0 / 9.1 / 9.2 / 9.3 / 9.4 / 9.5 / 9.6"}),"\n",(0,r.jsx)(t.hr,{}),"\n",(0,r.jsx)(t.p,{children:(0,r.jsxs)(t.a,{href:"/demo_docu/II.%20The%20SQL%20Language/Queries/queries-with#781-select-in-with",children:["7.8.1. ",(0,r.jsx)(t.code,{children:"SELECT"})," in ",(0,r.jsx)(t.code,{children:"WITH"})]})}),"\n",(0,r.jsx)(t.p,{children:(0,r.jsx)(t.a,{href:"/demo_docu/II.%20The%20SQL%20Language/Queries/queries-with#782-recursive-queries",children:"7.8.2. Recursive Queries"})}),"\n",(0,r.jsx)(t.p,{children:(0,r.jsx)(t.a,{href:"/demo_docu/II.%20The%20SQL%20Language/Queries/queries-with#783-common-table-expression-materialization",children:"7.8.3. Common Table Expression Materialization"})}),"\n",(0,r.jsx)(t.p,{children:(0,r.jsxs)(t.a,{href:"/demo_docu/II.%20The%20SQL%20Language/Queries/queries-with#784-data-modifying-statements-in-with",children:["7.8.4. Data-Modifying Statements in ",(0,r.jsx)(t.code,{children:"WITH"})]})}),"\n",(0,r.jsxs)(t.p,{children:[(0,r.jsx)(t.code,{children:"WITH"})," provides a way to write auxiliary statements for use in a larger query. These statements, which are often referred to as Common Table Expressions or CTEs, can be thought of as defining temporary tables that exist just for one query. Each auxiliary statement in a ",(0,r.jsx)(t.code,{children:"WITH"})," clause can be a ",(0,r.jsx)(t.code,{children:"SELECT"}),", ",(0,r.jsx)(t.code,{children:"INSERT"}),", ",(0,r.jsx)(t.code,{children:"UPDATE"}),", or ",(0,r.jsx)(t.code,{children:"DELETE"}),"; and the ",(0,r.jsx)(t.code,{children:"WITH"})," clause itself is attached to a primary statement that can be a ",(0,r.jsx)(t.code,{children:"SELECT"}),", ",(0,r.jsx)(t.code,{children:"INSERT"}),", ",(0,r.jsx)(t.code,{children:"UPDATE"}),", ",(0,r.jsx)(t.code,{children:"DELETE"}),", or ",(0,r.jsx)(t.code,{children:"MERGE"}),"."]}),"\n",(0,r.jsxs)(t.h3,{id:"781-select-in-with",children:["7.8.1. ",(0,r.jsx)(t.code,{children:"SELECT"})," in ",(0,r.jsx)(t.code,{children:"WITH"})]}),"\n",(0,r.jsxs)(t.p,{children:["The basic value of ",(0,r.jsx)(t.code,{children:"SELECT"})," in ",(0,r.jsx)(t.code,{children:"WITH"})," is to break down complicated queries into simpler parts. An example is:"]}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{children:"WITH regional_sales AS (\n    SELECT region, SUM(amount) AS total_sales\n    FROM orders\n    GROUP BY region\n), top_regions AS (\n    SELECT region\n    FROM regional_sales\n    WHERE total_sales > (SELECT SUM(total_sales)/10 FROM regional_sales)\n)\nSELECT region,\n       product,\n       SUM(quantity) AS product_units,\n       SUM(amount) AS product_sales\nFROM orders\nWHERE region IN (SELECT region FROM top_regions)\nGROUP BY region, product;\n"})}),"\n",(0,r.jsxs)(t.p,{children:["which displays per-product sales totals in only the top sales regions. The ",(0,r.jsx)(t.code,{children:"WITH"})," clause defines two auxiliary statements named ",(0,r.jsx)(t.code,{children:"regional_sales"})," and ",(0,r.jsx)(t.code,{children:"top_regions"}),", where the output of ",(0,r.jsx)(t.code,{children:"regional_sales"})," is used in ",(0,r.jsx)(t.code,{children:"top_regions"})," and the output of ",(0,r.jsx)(t.code,{children:"top_regions"})," is used in the primary ",(0,r.jsx)(t.code,{children:"SELECT"})," query. This example could have been written without ",(0,r.jsx)(t.code,{children:"WITH"}),", but we'd have needed two levels of nested sub-",(0,r.jsx)(t.code,{children:"SELECT"}),"s. It's a bit easier to follow this way."]}),"\n",(0,r.jsx)(t.h3,{id:"782-recursive-queries",children:"7.8.2. Recursive Queries"}),"\n",(0,r.jsxs)(t.p,{children:["The optional ",(0,r.jsx)(t.code,{children:"RECURSIVE"})," modifier changes ",(0,r.jsx)(t.code,{children:"WITH"})," from a mere syntactic convenience into a feature that accomplishes things not otherwise possible in standard SQL. Using ",(0,r.jsx)(t.code,{children:"RECURSIVE"}),", a ",(0,r.jsx)(t.code,{children:"WITH"})," query can refer to its own output. A very simple example is this query to sum the integers from 1 through 100:"]}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{children:"WITH RECURSIVE t(n) AS (\n    VALUES (1)\n  UNION ALL\n    SELECT n+1 FROM t WHERE n < 100\n)\nSELECT sum(n) FROM t;\n"})}),"\n",(0,r.jsxs)(t.p,{children:["The general form of a recursive ",(0,r.jsx)(t.code,{children:"WITH"})," query is always a ",(0,r.jsx)(t.em,{children:"non-recursive term"})," , then ",(0,r.jsx)(t.code,{children:"UNION"})," (or ",(0,r.jsx)(t.code,{children:"UNION ALL"}),"), then a ",(0,r.jsx)(t.em,{children:"recursive term"})," , where only the recursive term can contain a reference to the query's own output. Such a query is executed as follows:"]}),"\n",(0,r.jsx)(t.p,{children:(0,r.jsx)(t.strong,{children:"Recursive Query Evaluation"})}),"\n",(0,r.jsxs)(t.ol,{children:["\n",(0,r.jsxs)(t.li,{children:["\n",(0,r.jsxs)(t.p,{children:["Evaluate the non-recursive term. For ",(0,r.jsx)(t.code,{children:"UNION"})," (but not ",(0,r.jsx)(t.code,{children:"UNION ALL"}),"), discard duplicate rows. Include all remaining rows in the result of the recursive query, and also place them in a temporary ",(0,r.jsx)(t.em,{children:"working table"}),"."]}),"\n"]}),"\n",(0,r.jsxs)(t.li,{children:["\n",(0,r.jsx)(t.p,{children:"So long as the working table is not empty, repeat these steps:"}),"\n",(0,r.jsxs)(t.ol,{children:["\n",(0,r.jsxs)(t.li,{children:["\n",(0,r.jsxs)(t.p,{children:["Evaluate the recursive term, substituting the current contents of the working table for the recursive self-reference. For ",(0,r.jsx)(t.code,{children:"UNION"})," (but not ",(0,r.jsx)(t.code,{children:"UNION ALL"}),"), discard duplicate rows and rows that duplicate any previous result row. Include all remaining rows in the result of the recursive query, and also place them in a temporary ",(0,r.jsx)(t.em,{children:"intermediate table"}),"."]}),"\n"]}),"\n",(0,r.jsxs)(t.li,{children:["\n",(0,r.jsx)(t.p,{children:"Replace the contents of the working table with the contents of the intermediate table, then empty the intermediate table."}),"\n"]}),"\n"]}),"\n"]}),"\n"]}),"\n",(0,r.jsx)(t.h3,{id:"note",children:"Note"}),"\n",(0,r.jsxs)(t.p,{children:["While ",(0,r.jsx)(t.code,{children:"RECURSIVE"})," allows queries to be specified recursively, internally such queries are evaluated iteratively."]}),"\n",(0,r.jsxs)(t.p,{children:["In the example above, the working table has just a single row in each step, and it takes on the values from 1 through 100 in successive steps. In the 100th step, there is no output because of the ",(0,r.jsx)(t.code,{children:"WHERE"})," clause, and so the query terminates."]}),"\n",(0,r.jsx)(t.p,{children:"Recursive queries are typically used to deal with hierarchical or tree-structured data. A useful example is this query to find all the direct and indirect sub-parts of a product, given only a table that shows immediate inclusions:"}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{children:"WITH RECURSIVE included_parts(sub_part, part, quantity) AS (\n    SELECT sub_part, part, quantity FROM parts WHERE part = 'our_product'\n  UNION ALL\n    SELECT p.sub_part, p.part, p.quantity * pr.quantity\n    FROM included_parts pr, parts p\n    WHERE p.part = pr.sub_part\n)\nSELECT sub_part, SUM(quantity) as total_quantity\nFROM included_parts\nGROUP BY sub_part\n"})}),"\n",(0,r.jsx)(t.h4,{id:"7821-search-order",children:"7.8.2.1. Search Order"}),"\n",(0,r.jsx)(t.p,{children:"When computing a tree traversal using a recursive query, you might want to order the results in either depth-first or breadth-first order. This can be done by computing an ordering column alongside the other data columns and using that to sort the results at the end. Note that this does not actually control in which order the query evaluation visits the rows; that is as always in SQL implementation-dependent. This approach merely provides a convenient way to order the results afterwards."}),"\n",(0,r.jsxs)(t.p,{children:["To create a depth-first order, we compute for each result row an array of rows that we have visited so far. For example, consider the following query that searches a table ",(0,r.jsx)(t.code,{children:"tree"})," using a ",(0,r.jsx)(t.code,{children:"link"})," field:"]}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{children:"WITH RECURSIVE search_tree(id, link, data) AS (\n    SELECT t.id, t.link, t.data\n    FROM tree t\n  UNION ALL\n    SELECT t.id, t.link, t.data\n    FROM tree t, search_tree st\n    WHERE t.id = st.link\n)\nSELECT * FROM search_tree;\n"})}),"\n",(0,r.jsx)(t.p,{children:"To add depth-first ordering information, you can write this:"}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{children:"WITH RECURSIVE search_tree(id, link, data, **path**) AS (\n    SELECT t.id, t.link, t.data, **ARRAY[t.id]**\n    FROM tree t\n  UNION ALL\n    SELECT t.id, t.link, t.data, **path || t.id**\n    FROM tree t, search_tree st\n    WHERE t.id = st.link\n)\nSELECT * FROM search_tree **ORDER BY path** ;\n"})}),"\n",(0,r.jsxs)(t.p,{children:["In the general case where more than one field needs to be used to identify a row, use an array of rows. For example, if we needed to track fields ",(0,r.jsx)(t.code,{children:"f1"})," and ",(0,r.jsx)(t.code,{children:"f2"}),":"]}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{children:"WITH RECURSIVE search_tree(id, link, data, **path**) AS (\n    SELECT t.id, t.link, t.data, **ARRAY[ROW(t.f1, t.f2)]**\n    FROM tree t\n  UNION ALL\n    SELECT t.id, t.link, t.data, **path || ROW(t.f1, t.f2)**\n    FROM tree t, search_tree st\n    WHERE t.id = st.link\n)\nSELECT * FROM search_tree **ORDER BY path** ;\n"})}),"\n",(0,r.jsx)(t.h3,{id:"tip",children:"Tip"}),"\n",(0,r.jsxs)(t.p,{children:["Omit the ",(0,r.jsx)(t.code,{children:"ROW()"})," syntax in the common case where only one field needs to be tracked. This allows a simple array rather than a composite-type array to be used, gaining efficiency."]}),"\n",(0,r.jsx)(t.p,{children:"To create a breadth-first order, you can add a column that tracks the depth of the search, for example:"}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{children:"WITH RECURSIVE search_tree(id, link, data, **depth**) AS (\n    SELECT t.id, t.link, t.data, **0**\n    FROM tree t\n  UNION ALL\n    SELECT t.id, t.link, t.data, **depth + 1**\n    FROM tree t, search_tree st\n    WHERE t.id = st.link\n)\nSELECT * FROM search_tree **ORDER BY depth** ;\n"})}),"\n",(0,r.jsx)(t.p,{children:"To get a stable sort, add data columns as secondary sorting columns."}),"\n",(0,r.jsx)(t.h3,{id:"tip-1",children:"Tip"}),"\n",(0,r.jsx)(t.p,{children:"The recursive query evaluation algorithm produces its output in breadth-first search order. However, this is an implementation detail and it is perhaps unsound to rely on it. The order of the rows within each level is certainly undefined, so some explicit ordering might be desired in any case."}),"\n",(0,r.jsx)(t.p,{children:"There is built-in syntax to compute a depth- or breadth-first sort column. For example:"}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{children:"WITH RECURSIVE search_tree(id, link, data) AS (\n    SELECT t.id, t.link, t.data\n    FROM tree t\n  UNION ALL\n    SELECT t.id, t.link, t.data\n    FROM tree t, search_tree st\n    WHERE t.id = st.link\n) **SEARCH DEPTH FIRST BY id SET ordercol**\nSELECT * FROM search_tree ORDER BY ordercol;\n\nWITH RECURSIVE search_tree(id, link, data) AS (\n    SELECT t.id, t.link, t.data\n    FROM tree t\n  UNION ALL\n    SELECT t.id, t.link, t.data\n    FROM tree t, search_tree st\n    WHERE t.id = st.link\n) **SEARCH BREADTH FIRST BY id SET ordercol**\nSELECT * FROM search_tree ORDER BY ordercol;\n"})}),"\n",(0,r.jsxs)(t.p,{children:["This syntax is internally expanded to something similar to the above hand-written forms. The ",(0,r.jsx)(t.code,{children:"SEARCH"})," clause specifies whether depth- or breadth first search is wanted, the list of columns to track for sorting, and a column name that will contain the result data that can be used for sorting. That column will implicitly be added to the output rows of the CTE."]}),"\n",(0,r.jsx)(t.h4,{id:"7822-cycle-detection",children:"7.8.2.2. Cycle Detection"}),"\n",(0,r.jsxs)(t.p,{children:["When working with recursive queries it is important to be sure that the recursive part of the query will eventually return no tuples, or else the query will loop indefinitely. Sometimes, using ",(0,r.jsx)(t.code,{children:"UNION"})," instead of ",(0,r.jsx)(t.code,{children:"UNION ALL"})," can accomplish this by discarding rows that duplicate previous output rows. However, often a cycle does not involve output rows that are completely duplicate: it may be necessary to check just one or a few fields to see if the same point has been reached before. The standard method for handling such situations is to compute an array of the already-visited values. For example, consider again the following query that searches a table ",(0,r.jsx)(t.code,{children:"graph"})," using a ",(0,r.jsx)(t.code,{children:"link"})," field:"]}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{children:"WITH RECURSIVE search_graph(id, link, data, depth) AS (\n    SELECT g.id, g.link, g.data, 0\n    FROM graph g\n  UNION ALL\n    SELECT g.id, g.link, g.data, sg.depth + 1\n    FROM graph g, search_graph sg\n    WHERE g.id = sg.link\n)\nSELECT * FROM search_graph;\n"})}),"\n",(0,r.jsxs)(t.p,{children:["This query will loop if the ",(0,r.jsx)(t.code,{children:"link"})," relationships contain cycles. Because we require a \u201cdepth\u201d output, just changing ",(0,r.jsx)(t.code,{children:"UNION ALL"})," to ",(0,r.jsx)(t.code,{children:"UNION"})," would not eliminate the looping. Instead we need to recognize whether we have reached the same row again while following a particular path of links. We add two columns ",(0,r.jsx)(t.code,{children:"is_cycle"})," and ",(0,r.jsx)(t.code,{children:"path"})," to the loop-prone query:"]}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{children:"WITH RECURSIVE search_graph(id, link, data, depth, **is_cycle, path**) AS (\n    SELECT g.id, g.link, g.data, 0,\n      **false,\n      ARRAY[g.id]**\n    FROM graph g\n  UNION ALL\n    SELECT g.id, g.link, g.data, sg.depth + 1,\n      **g.id = ANY(path),\n      path || g.id**\n    FROM graph g, search_graph sg\n    WHERE g.id = sg.link **AND NOT is_cycle**\n)\nSELECT * FROM search_graph;\n"})}),"\n",(0,r.jsx)(t.p,{children:"Aside from preventing cycles, the array value is often useful in its own right as representing the \u201cpath\u201d taken to reach any particular row."}),"\n",(0,r.jsxs)(t.p,{children:["In the general case where more than one field needs to be checked to recognize a cycle, use an array of rows. For example, if we needed to compare fields ",(0,r.jsx)(t.code,{children:"f1"})," and ",(0,r.jsx)(t.code,{children:"f2"}),":"]}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{children:"WITH RECURSIVE search_graph(id, link, data, depth, **is_cycle, path**) AS (\n    SELECT g.id, g.link, g.data, 0,\n      **false,\n      ARRAY[ROW(g.f1, g.f2)]**\n    FROM graph g\n  UNION ALL\n    SELECT g.id, g.link, g.data, sg.depth + 1,\n      **ROW(g.f1, g.f2) = ANY(path),\n      path || ROW(g.f1, g.f2)**\n    FROM graph g, search_graph sg\n    WHERE g.id = sg.link **AND NOT is_cycle**\n)\nSELECT * FROM search_graph;\n"})}),"\n",(0,r.jsx)(t.h3,{id:"tip-2",children:"Tip"}),"\n",(0,r.jsxs)(t.p,{children:["Omit the ",(0,r.jsx)(t.code,{children:"ROW()"})," syntax in the common case where only one field needs to be checked to recognize a cycle. This allows a simple array rather than a composite-type array to be used, gaining efficiency."]}),"\n",(0,r.jsx)(t.p,{children:"There is built-in syntax to simplify cycle detection. The above query can also be written like this:"}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{children:"WITH RECURSIVE search_graph(id, link, data, depth) AS (\n    SELECT g.id, g.link, g.data, 1\n    FROM graph g\n  UNION ALL\n    SELECT g.id, g.link, g.data, sg.depth + 1\n    FROM graph g, search_graph sg\n    WHERE g.id = sg.link\n) **CYCLE id SET is_cycle USING path**\nSELECT * FROM search_graph;\n"})}),"\n",(0,r.jsxs)(t.p,{children:["and it will be internally rewritten to the above form. The ",(0,r.jsx)(t.code,{children:"CYCLE"})," clause specifies first the list of columns to track for cycle detection, then a column name that will show whether a cycle has been detected, and finally the name of another column that will track the path. The cycle and path columns will implicitly be added to the output rows of the CTE."]}),"\n",(0,r.jsx)(t.h3,{id:"tip-3",children:"Tip"}),"\n",(0,r.jsxs)(t.p,{children:["The cycle path column is computed in the same way as the depth-first ordering column show in the previous section. A query can have both a ",(0,r.jsx)(t.code,{children:"SEARCH"})," and a ",(0,r.jsx)(t.code,{children:"CYCLE"})," clause, but a depth-first search specification and a cycle detection specification would create redundant computations, so it's more efficient to just use the ",(0,r.jsx)(t.code,{children:"CYCLE"})," clause and order by the path column. If breadth-first ordering is wanted, then specifying both ",(0,r.jsx)(t.code,{children:"SEARCH"})," and ",(0,r.jsx)(t.code,{children:"CYCLE"})," can be useful."]}),"\n",(0,r.jsxs)(t.p,{children:["A helpful trick for testing queries when you are not certain if they might loop is to place a ",(0,r.jsx)(t.code,{children:"LIMIT"})," in the parent query. For example, this query would loop forever without the ",(0,r.jsx)(t.code,{children:"LIMIT"}),":"]}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{children:"WITH RECURSIVE t(n) AS (\n    SELECT 1\n  UNION ALL\n    SELECT n+1 FROM t\n)\nSELECT n FROM t **LIMIT 100** ;\n"})}),"\n",(0,r.jsxs)(t.p,{children:["This works because PostgreSQL's implementation evaluates only as many rows of a ",(0,r.jsx)(t.code,{children:"WITH"})," query as are actually fetched by the parent query. Using this trick in production is not recommended, because other systems might work differently. Also, it usually won't work if you make the outer query sort the recursive query's results or join them to some other table, because in such cases the outer query will usually try to fetch all of the ",(0,r.jsx)(t.code,{children:"WITH"})," query's output anyway."]}),"\n",(0,r.jsx)(t.h3,{id:"783-common-table-expression-materialization",children:"7.8.3. Common Table Expression Materialization"}),"\n",(0,r.jsxs)(t.p,{children:["A useful property of ",(0,r.jsx)(t.code,{children:"WITH"})," queries is that they are normally evaluated only once per execution of the parent query, even if they are referred to more than once by the parent query or sibling ",(0,r.jsx)(t.code,{children:"WITH"})," queries. Thus, expensive calculations that are needed in multiple places can be placed within a ",(0,r.jsx)(t.code,{children:"WITH"})," query to avoid redundant work. Another possible application is to prevent unwanted multiple evaluations of functions with side-effects. However, the other side of this coin is that the optimizer is not able to push restrictions from the parent query down into a multiply-referenced ",(0,r.jsx)(t.code,{children:"WITH"})," query, since that might affect all uses of the ",(0,r.jsx)(t.code,{children:"WITH"})," query's output when it should affect only one. The multiply-referenced ",(0,r.jsx)(t.code,{children:"WITH"})," query will be evaluated as written, without suppression of rows that the parent query might discard afterwards. (But, as mentioned above, evaluation might stop early if the reference(s) to the query demand only a limited number of rows.)"]}),"\n",(0,r.jsxs)(t.p,{children:["However, if a ",(0,r.jsx)(t.code,{children:"WITH"})," query is non-recursive and side-effect-free (that is, it is a ",(0,r.jsx)(t.code,{children:"SELECT"})," containing no volatile functions) then it can be folded into the parent query, allowing joint optimization of the two query levels. By default, this happens if the parent query references the ",(0,r.jsx)(t.code,{children:"WITH"})," query just once, but not if it references the ",(0,r.jsx)(t.code,{children:"WITH"})," query more than once. You can override that decision by specifying ",(0,r.jsx)(t.code,{children:"MATERIALIZED"})," to force separate calculation of the ",(0,r.jsx)(t.code,{children:"WITH"})," query, or by specifying ",(0,r.jsx)(t.code,{children:"NOT MATERIALIZED"})," to force it to be merged into the parent query. The latter choice risks duplicate computation of the ",(0,r.jsx)(t.code,{children:"WITH"})," query, but it can still give a net savings if each usage of the ",(0,r.jsx)(t.code,{children:"WITH"})," query needs only a small part of the ",(0,r.jsx)(t.code,{children:"WITH"})," query's full output."]}),"\n",(0,r.jsx)(t.p,{children:"A simple example of these rules is"}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{children:"WITH w AS (\n    SELECT * FROM big_table\n)\nSELECT * FROM w WHERE key = 123;\n"})}),"\n",(0,r.jsxs)(t.p,{children:["This ",(0,r.jsx)(t.code,{children:"WITH"})," query will be folded, producing the same execution plan as"]}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{children:"SELECT * FROM big_table WHERE key = 123;\n"})}),"\n",(0,r.jsxs)(t.p,{children:["In particular, if there's an index on ",(0,r.jsx)(t.code,{children:"key"}),", it will probably be used to fetch just the rows having ",(0,r.jsx)(t.code,{children:"key = 123"}),". On the other hand, in"]}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{children:"WITH w AS (\n    SELECT * FROM big_table\n)\nSELECT * FROM w AS w1 JOIN w AS w2 ON w1.key = w2.ref\nWHERE w2.key = 123;\n"})}),"\n",(0,r.jsxs)(t.p,{children:["the ",(0,r.jsx)(t.code,{children:"WITH"})," query will be materialized, producing a temporary copy of ",(0,r.jsx)(t.code,{children:"big_table"})," that is then joined with itself \u2014 without benefit of any index. This query will be executed much more efficiently if written as"]}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{children:"WITH w AS NOT MATERIALIZED (\n    SELECT * FROM big_table\n)\nSELECT * FROM w AS w1 JOIN w AS w2 ON w1.key = w2.ref\nWHERE w2.key = 123;\n"})}),"\n",(0,r.jsxs)(t.p,{children:["so that the parent query's restrictions can be applied directly to scans of ",(0,r.jsx)(t.code,{children:"big_table"}),"."]}),"\n",(0,r.jsxs)(t.p,{children:["An example where ",(0,r.jsx)(t.code,{children:"NOT MATERIALIZED"})," could be undesirable is"]}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{children:"WITH w AS (\n    SELECT key, very_expensive_function(val) as f FROM some_table\n)\nSELECT * FROM w AS w1 JOIN w AS w2 ON w1.f = w2.f;\n"})}),"\n",(0,r.jsxs)(t.p,{children:["Here, materialization of the ",(0,r.jsx)(t.code,{children:"WITH"})," query ensures that ",(0,r.jsx)(t.code,{children:"very_expensive_function"})," is evaluated only once per table row, not twice."]}),"\n",(0,r.jsxs)(t.p,{children:["The examples above only show ",(0,r.jsx)(t.code,{children:"WITH"})," being used with ",(0,r.jsx)(t.code,{children:"SELECT"}),", but it can be attached in the same way to ",(0,r.jsx)(t.code,{children:"INSERT"}),", ",(0,r.jsx)(t.code,{children:"UPDATE"}),", ",(0,r.jsx)(t.code,{children:"DELETE"}),", or ",(0,r.jsx)(t.code,{children:"MERGE"}),". In each case it effectively provides temporary table(s) that can be referred to in the main command."]}),"\n",(0,r.jsxs)(t.h3,{id:"784-data-modifying-statements-in-with",children:["7.8.4. Data-Modifying Statements in ",(0,r.jsx)(t.code,{children:"WITH"})]}),"\n",(0,r.jsxs)(t.p,{children:["You can use most data-modifying statements (",(0,r.jsx)(t.code,{children:"INSERT"}),", ",(0,r.jsx)(t.code,{children:"UPDATE"}),", or ",(0,r.jsx)(t.code,{children:"DELETE"}),", but not ",(0,r.jsx)(t.code,{children:"MERGE"}),") in ",(0,r.jsx)(t.code,{children:"WITH"}),". This allows you to perform several different operations in the same query. An example is:"]}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{children:"WITH moved_rows AS (\n    DELETE FROM products\n    WHERE\n        \"date\" >= '2010-10-01' AND\n        \"date\" < '2010-11-01'\n    RETURNING *\n)\nINSERT INTO products_log\nSELECT * FROM moved_rows;\n"})}),"\n",(0,r.jsxs)(t.p,{children:["This query effectively moves rows from ",(0,r.jsx)(t.code,{children:"products"})," to ",(0,r.jsx)(t.code,{children:"products_log"}),". The ",(0,r.jsx)(t.code,{children:"DELETE"})," in ",(0,r.jsx)(t.code,{children:"WITH"})," deletes the specified rows from ",(0,r.jsx)(t.code,{children:"products"}),", returning their contents by means of its ",(0,r.jsx)(t.code,{children:"RETURNING"})," clause; and then the primary query reads that output and inserts it into ",(0,r.jsx)(t.code,{children:"products_log"}),"."]}),"\n",(0,r.jsxs)(t.p,{children:["A fine point of the above example is that the ",(0,r.jsx)(t.code,{children:"WITH"})," clause is attached to the ",(0,r.jsx)(t.code,{children:"INSERT"}),", not the sub-",(0,r.jsx)(t.code,{children:"SELECT"})," within the ",(0,r.jsx)(t.code,{children:"INSERT"}),". This is necessary because data-modifying statements are only allowed in ",(0,r.jsx)(t.code,{children:"WITH"})," clauses that are attached to the top-level statement. However, normal ",(0,r.jsx)(t.code,{children:"WITH"})," visibility rules apply, so it is possible to refer to the ",(0,r.jsx)(t.code,{children:"WITH"})," statement's output from the sub-",(0,r.jsx)(t.code,{children:"SELECT"}),"."]}),"\n",(0,r.jsxs)(t.p,{children:["Data-modifying statements in ",(0,r.jsx)(t.code,{children:"WITH"})," usually have ",(0,r.jsx)(t.code,{children:"RETURNING"})," clauses (see ",(0,r.jsx)(t.a,{href:"/demo_docu/II.%20The%20SQL%20Language/Data%20Manipulation/dml-returning",children:"Section 6.4"}),"), as shown in the example above. It is the output of the ",(0,r.jsx)(t.code,{children:"RETURNING"})," clause, ",(0,r.jsx)(t.em,{children:"not"})," the target table of the data-modifying statement, that forms the temporary table that can be referred to by the rest of the query. If a data-modifying statement in ",(0,r.jsx)(t.code,{children:"WITH"})," lacks a ",(0,r.jsx)(t.code,{children:"RETURNING"})," clause, then it forms no temporary table and cannot be referred to in the rest of the query. Such a statement will be executed nonetheless. A not-particularly-useful example is:"]}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{children:"WITH t AS (\n    DELETE FROM foo\n)\nDELETE FROM bar;\n"})}),"\n",(0,r.jsxs)(t.p,{children:["This example would remove all rows from tables ",(0,r.jsx)(t.code,{children:"foo"})," and ",(0,r.jsx)(t.code,{children:"bar"}),". The number of affected rows reported to the client would only include rows removed from ",(0,r.jsx)(t.code,{children:"bar"}),"."]}),"\n",(0,r.jsxs)(t.p,{children:["Recursive self-references in data-modifying statements are not allowed. In some cases it is possible to work around this limitation by referring to the output of a recursive ",(0,r.jsx)(t.code,{children:"WITH"}),", for example:"]}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{children:"WITH RECURSIVE included_parts(sub_part, part) AS (\n    SELECT sub_part, part FROM parts WHERE part = 'our_product'\n  UNION ALL\n    SELECT p.sub_part, p.part\n    FROM included_parts pr, parts p\n    WHERE p.part = pr.sub_part\n)\nDELETE FROM parts\n  WHERE part IN (SELECT part FROM included_parts);\n"})}),"\n",(0,r.jsx)(t.p,{children:"This query would remove all direct and indirect subparts of a product."}),"\n",(0,r.jsxs)(t.p,{children:["Data-modifying statements in ",(0,r.jsx)(t.code,{children:"WITH"})," are executed exactly once, and always to completion, independently of whether the primary query reads all (or indeed any) of their output. Notice that this is different from the rule for ",(0,r.jsx)(t.code,{children:"SELECT"})," in ",(0,r.jsx)(t.code,{children:"WITH"}),": as stated in the previous section, execution of a ",(0,r.jsx)(t.code,{children:"SELECT"})," is carried only as far as the primary query demands its output."]}),"\n",(0,r.jsxs)(t.p,{children:["The sub-statements in ",(0,r.jsx)(t.code,{children:"WITH"})," are executed concurrently with each other and with the main query. Therefore, when using data-modifying statements in ",(0,r.jsx)(t.code,{children:"WITH"}),", the order in which the specified updates actually happen is unpredictable. All the statements are executed with the same ",(0,r.jsx)(t.em,{children:"snapshot"})," (see ",(0,r.jsx)(t.a,{href:"/demo_docu/II.%20The%20SQL%20Language/Concurrency%20Control/",children:"Chapter 13"}),"), so they cannot \u201csee\u201d one another's effects on the target tables. This alleviates the effects of the unpredictability of the actual order of row updates, and means that ",(0,r.jsx)(t.code,{children:"RETURNING"})," data is the only way to communicate changes between different ",(0,r.jsx)(t.code,{children:"WITH"})," sub-statements and the main query. An example of this is that in"]}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{children:"WITH t AS (\n    UPDATE products SET price = price * 1.05\n    RETURNING *\n)\nSELECT * FROM products;\n"})}),"\n",(0,r.jsxs)(t.p,{children:["the outer ",(0,r.jsx)(t.code,{children:"SELECT"})," would return the original prices before the action of the ",(0,r.jsx)(t.code,{children:"UPDATE"}),", while in"]}),"\n",(0,r.jsx)(t.pre,{children:(0,r.jsx)(t.code,{children:"WITH t AS (\n    UPDATE products SET price = price * 1.05\n    RETURNING *\n)\nSELECT * FROM t;\n"})}),"\n",(0,r.jsxs)(t.p,{children:["the outer ",(0,r.jsx)(t.code,{children:"SELECT"})," would return the updated data."]}),"\n",(0,r.jsxs)(t.p,{children:["Trying to update the same row twice in a single statement is not supported. Only one of the modifications takes place, but it is not easy (and sometimes not possible) to reliably predict which one. This also applies to deleting a row that was already updated in the same statement: only the update is performed. Therefore you should generally avoid trying to modify a single row twice in a single statement. In particular avoid writing ",(0,r.jsx)(t.code,{children:"WITH"})," sub-statements that could affect the same rows changed by the main statement or a sibling sub-statement. The effects of such a statement will not be predictable."]}),"\n",(0,r.jsxs)(t.p,{children:["At present, any table used as the target of a data-modifying statement in ",(0,r.jsx)(t.code,{children:"WITH"})," must not have a conditional rule, nor an ",(0,r.jsx)(t.code,{children:"ALSO"})," rule, nor an ",(0,r.jsx)(t.code,{children:"INSTEAD"})," rule that expands to multiple statements."]})]})}function h(e={}){const{wrapper:t}={...(0,s.R)(),...e.components};return t?(0,r.jsx)(t,{...e,children:(0,r.jsx)(l,{...e})}):l(e)}}}]);