"use strict";(self.webpackChunkpgdoc_02=self.webpackChunkpgdoc_02||[]).push([[67534],{11195:(e,t,n)=>{n.r(t),n.d(t,{assets:()=>l,contentTitle:()=>a,default:()=>h,frontMatter:()=>r,metadata:()=>s,toc:()=>d});const s=JSON.parse('{"id":"VII. Internals/SP-GiST Indexes/spgist-extensibility","title":"69.3. Extensibility","description":"Supported versions: 13 / 14 / 15 / 16 / 17","source":"@site/docs/VII. Internals/69. SP-GiST Indexes/spgist-extensibility.md","sourceDirName":"VII. Internals/69. SP-GiST Indexes","slug":"/VII. Internals/SP-GiST Indexes/spgist-extensibility","permalink":"/demo_docu/VII. Internals/SP-GiST Indexes/spgist-extensibility","draft":false,"unlisted":false,"tags":[],"version":"current","frontMatter":{"title":"69.3. Extensibility"},"sidebar":"autoGeneratedSidebar","previous":{"title":"69.5. Examples","permalink":"/demo_docu/VII. Internals/SP-GiST Indexes/spgist-examples"},"next":{"title":"69.4. Implementation","permalink":"/demo_docu/VII. Internals/SP-GiST Indexes/spgist-implementation"}}');var o=n(74848),i=n(28453);const r={title:"69.3. Extensibility"},a=void 0,l={},d=[{value:"Note",id:"note",level:3}];function c(e){const t={a:"a",code:"code",em:"em",h3:"h3",hr:"hr",p:"p",pre:"pre",...(0,i.R)(),...e.components};return(0,o.jsxs)(o.Fragment,{children:[(0,o.jsx)(t.p,{children:"Supported versions: 13 / 14 / 15 / 16 / 17"}),"\n",(0,o.jsx)(t.p,{children:"Unsupported versions: 10 / 11 / 12 / 7.1 / 7.2 / 7.3 / 7.4 / 8.0 / 8.1 / 8.2 / 8.3 / 8.4 / 9.0 / 9.1 / 9.2 / 9.3 / 9.4 / 9.5 / 9.6"}),"\n",(0,o.jsx)(t.hr,{}),"\n",(0,o.jsx)(t.p,{children:"SP-GiST offers an interface with a high level of abstraction, requiring the access method developer to implement only methods specific to a given data type. The SP-GiST core is responsible for efficient disk mapping and searching the tree structure. It also takes care of concurrency and logging considerations."}),"\n",(0,o.jsx)(t.p,{children:"Leaf tuples of an SP-GiST tree usually contain values of the same data type as the indexed column, although it is also possible for them to contain lossy representations of the indexed column. Leaf tuples stored at the root level will directly represent the original indexed data value, but leaf tuples at lower levels might contain only a partial value, such as a suffix. In that case the operator class support functions must be able to reconstruct the original value using information accumulated from the inner tuples that are passed through to reach the leaf level."}),"\n",(0,o.jsxs)(t.p,{children:["When an SP-GiST index is created with ",(0,o.jsx)(t.code,{children:"INCLUDE"})," columns, the values of those columns are also stored in leaf tuples. The ",(0,o.jsx)(t.code,{children:"INCLUDE"})," columns are of no concern to the SP-GiST operator class, so they are not discussed further here."]}),"\n",(0,o.jsxs)(t.p,{children:["Inner tuples are more complex, since they are branching points in the search tree. Each inner tuple contains a set of one or more ",(0,o.jsx)(t.em,{children:"nodes"})," , which represent groups of similar leaf values. A node contains a downlink that leads either to another, lower-level inner tuple, or to a short list of leaf tuples that all lie on the same index page. Each node normally has a ",(0,o.jsx)(t.em,{children:"label"})," that describes it; for example, in a radix tree the node label could be the next character of the string value. (Alternatively, an operator class can omit the node labels, if it works with a fixed set of nodes for all inner tuples; see ",(0,o.jsx)(t.a,{href:"spgist-implementation.html#SPGIST-NULL-LABELS",title:"69.4.2.\xa0SP-GiST Without Node Labels",children:"Section 69.4.2"}),".) Optionally, an inner tuple can have a ",(0,o.jsx)(t.em,{children:"prefix"})," value that describes all its members. In a radix tree this could be the common prefix of the represented strings. The prefix value is not necessarily really a prefix, but can be any data needed by the operator class; for example, in a quad-tree it can store the central point that the four quadrants are measured with respect to. A quad-tree inner tuple would then also contain four nodes corresponding to the quadrants around this central point."]}),"\n",(0,o.jsxs)(t.p,{children:["Some tree algorithms require knowledge of level (or depth) of the current tuple, so the SP-GiST core provides the possibility for operator classes to manage level counting while descending the tree. There is also support for incrementally reconstructing the represented value when that is needed, and for passing down additional data (called ",(0,o.jsx)(t.em,{children:"traverse values"}),") during a tree descent."]}),"\n",(0,o.jsx)(t.h3,{id:"note",children:"Note"}),"\n",(0,o.jsx)(t.p,{children:"The SP-GiST core code takes care of null entries. Although SP-GiST indexes do store entries for nulls in indexed columns, this is hidden from the index operator class code: no null index entries or search conditions will ever be passed to the operator class methods. (It is assumed that SP-GiST operators are strict and so cannot succeed for null values.) Null values are therefore not discussed further here."}),"\n",(0,o.jsxs)(t.p,{children:["There are five user-defined methods that an index operator class for SP-GiST must provide, and two are optional. All five mandatory methods follow the convention of accepting two ",(0,o.jsx)(t.code,{children:"internal"})," arguments, the first of which is a pointer to a C struct containing input values for the support method, while the second argument is a pointer to a C struct where output values must be placed. Four of the mandatory methods just return ",(0,o.jsx)(t.code,{children:"void"}),", since all their results appear in the output struct; but ",(0,o.jsx)(t.code,{children:"leaf_consistent"})," returns a ",(0,o.jsx)(t.code,{children:"boolean"})," result. The methods must not modify any fields of their input structs. In all cases, the output struct is initialized to zeroes before calling the user-defined method. The optional sixth method ",(0,o.jsx)(t.code,{children:"compress"})," accepts a ",(0,o.jsx)(t.code,{children:"datum"})," to be indexed as the only argument and returns a value suitable for physical storage in a leaf tuple. The optional seventh method ",(0,o.jsx)(t.code,{children:"options"})," accepts an ",(0,o.jsx)(t.code,{children:"internal"})," pointer to a C struct, where opclass-specific parameters should be placed, and returns ",(0,o.jsx)(t.code,{children:"void"}),"."]}),"\n",(0,o.jsx)(t.p,{children:"The five mandatory user-defined methods are:"}),"\n",(0,o.jsx)(t.p,{children:(0,o.jsx)(t.code,{children:"config"})}),"\n",(0,o.jsx)(t.p,{children:"Returns static information about the index implementation, including the data type OIDs of the prefix and node label data types."}),"\n",(0,o.jsx)(t.p,{children:"The SQL declaration of the function must look like this:"}),"\n",(0,o.jsx)(t.pre,{children:(0,o.jsx)(t.code,{children:"CREATE FUNCTION my_config(internal, internal) RETURNS void ...\n"})}),"\n",(0,o.jsxs)(t.p,{children:["The first argument is a pointer to a ",(0,o.jsx)(t.code,{children:"spgConfigIn"})," C struct, containing input data for the function. The second argument is a pointer to a ",(0,o.jsx)(t.code,{children:"spgConfigOut"})," C struct, which the function must fill with result data."]}),"\n",(0,o.jsx)(t.pre,{children:(0,o.jsx)(t.code,{children:"typedef struct spgConfigIn\n{\n    Oid         attType;        /* Data type to be indexed */\n} spgConfigIn;\n\ntypedef struct spgConfigOut\n{\n    Oid         prefixType;     /* Data type of inner-tuple prefixes */\n    Oid         labelType;      /* Data type of inner-tuple node labels */\n    Oid         leafType;       /* Data type of leaf-tuple values */\n    bool        canReturnData;  /* Opclass can reconstruct original data */\n    bool        longValuesOK;   /* Opclass can cope with values > 1 page */\n} spgConfigOut;\n"})}),"\n",(0,o.jsxs)(t.p,{children:[(0,o.jsx)(t.code,{children:"attType"})," is passed in order to support polymorphic index operator classes; for ordinary fixed-data-type operator classes, it will always have the same value and so can be ignored."]}),"\n",(0,o.jsxs)(t.p,{children:["For operator classes that do not use prefixes, ",(0,o.jsx)(t.code,{children:"prefixType"})," can be set to ",(0,o.jsx)(t.code,{children:"VOIDOID"}),". Likewise, for operator classes that do not use node labels, ",(0,o.jsx)(t.code,{children:"labelType"})," can be set to ",(0,o.jsx)(t.code,{children:"VOIDOID"}),". ",(0,o.jsx)(t.code,{children:"canReturnData"})," should be set true if the operator class is capable of reconstructing the originally-supplied index value. ",(0,o.jsx)(t.code,{children:"longValuesOK"})," should be set true only when the ",(0,o.jsx)(t.code,{children:"attType"})," is of variable length and the operator class is capable of segmenting long values by repeated suffixing (see ",(0,o.jsx)(t.a,{href:"spgist-implementation.html#SPGIST-LIMITS",title:"69.4.1.\xa0SP-GiST Limits",children:"Section 69.4.1"}),")."]}),"\n",(0,o.jsxs)(t.p,{children:[(0,o.jsx)(t.code,{children:"leafType"})," should match the index storage type defined by the operator class's ",(0,o.jsx)(t.code,{children:"opckeytype"})," catalog entry. (Note that ",(0,o.jsx)(t.code,{children:"opckeytype"})," can be zero, implying the storage type is the same as the operator class's input type, which is the most common situation.) For reasons of backward compatibility, the ",(0,o.jsx)(t.code,{children:"config"})," method can set ",(0,o.jsx)(t.code,{children:"leafType"})," to some other value, and that value will be used; but this is deprecated since the index contents are then incorrectly identified in the catalogs. Also, it's permissible to leave ",(0,o.jsx)(t.code,{children:"leafType"})," uninitialized (zero); that is interpreted as meaning the index storage type derived from ",(0,o.jsx)(t.code,{children:"opckeytype"}),"."]}),"\n",(0,o.jsxs)(t.p,{children:["When ",(0,o.jsx)(t.code,{children:"attType"})," and ",(0,o.jsx)(t.code,{children:"leafType"})," are different, the optional method ",(0,o.jsx)(t.code,{children:"compress"})," must be provided. Method ",(0,o.jsx)(t.code,{children:"compress"})," is responsible for transformation of datums to be indexed from ",(0,o.jsx)(t.code,{children:"attType"})," to ",(0,o.jsx)(t.code,{children:"leafType"}),"."]}),"\n",(0,o.jsx)(t.p,{children:(0,o.jsx)(t.code,{children:"choose"})}),"\n",(0,o.jsx)(t.p,{children:"Chooses a method for inserting a new value into an inner tuple."}),"\n",(0,o.jsx)(t.p,{children:"The SQL declaration of the function must look like this:"}),"\n",(0,o.jsx)(t.pre,{children:(0,o.jsx)(t.code,{children:"CREATE FUNCTION my_choose(internal, internal) RETURNS void ...\n"})}),"\n",(0,o.jsxs)(t.p,{children:["The first argument is a pointer to a ",(0,o.jsx)(t.code,{children:"spgChooseIn"})," C struct, containing input data for the function. The second argument is a pointer to a ",(0,o.jsx)(t.code,{children:"spgChooseOut"})," C struct, which the function must fill with result data."]}),"\n",(0,o.jsx)(t.pre,{children:(0,o.jsx)(t.code,{children:"typedef struct spgChooseIn\n{\n    Datum       datum;          /* original datum to be indexed */\n    Datum       leafDatum;      /* current datum to be stored at leaf */\n    int         level;          /* current level (counting from zero) */\n\n    /* Data from current inner tuple */\n    bool        allTheSame;     /* tuple is marked all-the-same? */\n    bool        hasPrefix;      /* tuple has a prefix? */\n    Datum       prefixDatum;    /* if so, the prefix value */\n    int         nNodes;         /* number of nodes in the inner tuple */\n    Datum      *nodeLabels;     /* node label values (NULL if none) */\n} spgChooseIn;\n\ntypedef enum spgChooseResultType\n{\n    spgMatchNode = 1,           /* descend into existing node */\n    spgAddNode,                 /* add a node to the inner tuple */\n    spgSplitTuple               /* split inner tuple (change its prefix) */\n} spgChooseResultType;\n\ntypedef struct spgChooseOut\n{\n    spgChooseResultType resultType;     /* action code, see above */\n    union\n    {\n        struct                  /* results for spgMatchNode */\n        {\n            int         nodeN;      /* descend to this node (index from 0) */\n            int         levelAdd;   /* increment level by this much */\n            Datum       restDatum;  /* new leaf datum */\n        }           matchNode;\n        struct                  /* results for spgAddNode */\n        {\n            Datum       nodeLabel;  /* new node's label */\n            int         nodeN;      /* where to insert it (index from 0) */\n        }           addNode;\n        struct                  /* results for spgSplitTuple */\n        {\n            /* Info to form new upper-level inner tuple with one child tuple */\n            bool        prefixHasPrefix;    /* tuple should have a prefix? */\n            Datum       prefixPrefixDatum;  /* if so, its value */\n            int         prefixNNodes;       /* number of nodes */\n            Datum      *prefixNodeLabels;   /* their labels (or NULL for\n                                             * no labels) */\n            int         childNodeN;         /* which node gets child tuple */\n\n            /* Info to form new lower-level inner tuple with all old nodes */\n            bool        postfixHasPrefix;   /* tuple should have a prefix? */\n            Datum       postfixPrefixDatum; /* if so, its value */\n        }           splitTuple;\n    }           result;\n} spgChooseOut;\n"})}),"\n",(0,o.jsxs)(t.p,{children:[(0,o.jsx)(t.code,{children:"datum"})," is the original datum of ",(0,o.jsx)(t.code,{children:"spgConfigIn"}),".",(0,o.jsx)(t.code,{children:"attType"})," type that was to be inserted into the index. ",(0,o.jsx)(t.code,{children:"leafDatum"})," is a value of ",(0,o.jsx)(t.code,{children:"spgConfigOut"}),".",(0,o.jsx)(t.code,{children:"leafType"})," type, which is initially a result of method ",(0,o.jsx)(t.code,{children:"compress"})," applied to ",(0,o.jsx)(t.code,{children:"datum"})," when method ",(0,o.jsx)(t.code,{children:"compress"})," is provided, or the same value as ",(0,o.jsx)(t.code,{children:"datum"})," otherwise. ",(0,o.jsx)(t.code,{children:"leafDatum"})," can change at lower levels of the tree if the ",(0,o.jsx)(t.code,{children:"choose"})," or ",(0,o.jsx)(t.code,{children:"picksplit"})," methods change it. When the insertion search reaches a leaf page, the current value of ",(0,o.jsx)(t.code,{children:"leafDatum"})," is what will be stored in the newly created leaf tuple. ",(0,o.jsx)(t.code,{children:"level"})," is the current inner tuple's level, starting at zero for the root level. ",(0,o.jsx)(t.code,{children:"allTheSame"})," is true if the current inner tuple is marked as containing multiple equivalent nodes (see ",(0,o.jsx)(t.a,{href:"spgist-implementation.html#SPGIST-ALL-THE-SAME",title:"69.4.3.\xa0\u201cAll-the-Same\u201d Inner Tuples",children:"Section 69.4.3"}),"). ",(0,o.jsx)(t.code,{children:"hasPrefix"})," is true if the current inner tuple contains a prefix; if so, ",(0,o.jsx)(t.code,{children:"prefixDatum"})," is its value. ",(0,o.jsx)(t.code,{children:"nNodes"})," is the number of child nodes contained in the inner tuple, and ",(0,o.jsx)(t.code,{children:"nodeLabels"})," is an array of their label values, or NULL if there are no labels."]}),"\n",(0,o.jsxs)(t.p,{children:["The ",(0,o.jsx)(t.code,{children:"choose"})," function can determine either that the new value matches one of the existing child nodes, or that a new child node must be added, or that the new value is inconsistent with the tuple prefix and so the inner tuple must be split to create a less restrictive prefix."]}),"\n",(0,o.jsxs)(t.p,{children:["If the new value matches one of the existing child nodes, set ",(0,o.jsx)(t.code,{children:"resultType"})," to ",(0,o.jsx)(t.code,{children:"spgMatchNode"}),". Set ",(0,o.jsx)(t.code,{children:"nodeN"})," to the index (from zero) of that node in the node array. Set ",(0,o.jsx)(t.code,{children:"levelAdd"})," to the increment in ",(0,o.jsx)(t.code,{children:"level"})," caused by descending through that node, or leave it as zero if the operator class does not use levels. Set ",(0,o.jsx)(t.code,{children:"restDatum"})," to equal ",(0,o.jsx)(t.code,{children:"leafDatum"})," if the operator class does not modify datums from one level to the next, or otherwise set it to the modified value to be used as ",(0,o.jsx)(t.code,{children:"leafDatum"})," at the next level."]}),"\n",(0,o.jsxs)(t.p,{children:["If a new child node must be added, set ",(0,o.jsx)(t.code,{children:"resultType"})," to ",(0,o.jsx)(t.code,{children:"spgAddNode"}),". Set ",(0,o.jsx)(t.code,{children:"nodeLabel"})," to the label to be used for the new node, and set ",(0,o.jsx)(t.code,{children:"nodeN"})," to the index (from zero) at which to insert the node in the node array. After the node has been added, the ",(0,o.jsx)(t.code,{children:"choose"})," function will be called again with the modified inner tuple; that call should result in an ",(0,o.jsx)(t.code,{children:"spgMatchNode"})," result."]}),"\n",(0,o.jsxs)(t.p,{children:["If the new value is inconsistent with the tuple prefix, set ",(0,o.jsx)(t.code,{children:"resultType"})," to ",(0,o.jsx)(t.code,{children:"spgSplitTuple"}),". This action moves all the existing nodes into a new lower-level inner tuple, and replaces the existing inner tuple with a tuple having a single downlink pointing to the new lower-level inner tuple. Set ",(0,o.jsx)(t.code,{children:"prefixHasPrefix"})," to indicate whether the new upper tuple should have a prefix, and if so set ",(0,o.jsx)(t.code,{children:"prefixPrefixDatum"})," to the prefix value. This new prefix value must be sufficiently less restrictive than the original to accept the new value to be indexed. Set ",(0,o.jsx)(t.code,{children:"prefixNNodes"})," to the number of nodes needed in the new tuple, and set ",(0,o.jsx)(t.code,{children:"prefixNodeLabels"})," to a palloc'd array holding their labels, or to NULL if node labels are not required. Note that the total size of the new upper tuple must be no more than the total size of the tuple it is replacing; this constrains the lengths of the new prefix and new labels. Set ",(0,o.jsx)(t.code,{children:"childNodeN"})," to the index (from zero) of the node that will downlink to the new lower-level inner tuple. Set ",(0,o.jsx)(t.code,{children:"postfixHasPrefix"})," to indicate whether the new lower-level inner tuple should have a prefix, and if so set ",(0,o.jsx)(t.code,{children:"postfixPrefixDatum"})," to the prefix value. The combination of these two prefixes and the downlink node's label (if any) must have the same meaning as the original prefix, because there is no opportunity to alter the node labels that are moved to the new lower-level tuple, nor to change any child index entries. After the node has been split, the ",(0,o.jsx)(t.code,{children:"choose"})," function will be called again with the replacement inner tuple. That call may return an ",(0,o.jsx)(t.code,{children:"spgAddNode"})," result, if no suitable node was created by the ",(0,o.jsx)(t.code,{children:"spgSplitTuple"})," action. Eventually ",(0,o.jsx)(t.code,{children:"choose"})," must return ",(0,o.jsx)(t.code,{children:"spgMatchNode"})," to allow the insertion to descend to the next level."]}),"\n",(0,o.jsx)(t.p,{children:(0,o.jsx)(t.code,{children:"picksplit"})}),"\n",(0,o.jsx)(t.p,{children:"Decides how to create a new inner tuple over a set of leaf tuples."}),"\n",(0,o.jsx)(t.p,{children:"The SQL declaration of the function must look like this:"}),"\n",(0,o.jsx)(t.pre,{children:(0,o.jsx)(t.code,{children:"CREATE FUNCTION my_picksplit(internal, internal) RETURNS void ...\n"})}),"\n",(0,o.jsxs)(t.p,{children:["The first argument is a pointer to a ",(0,o.jsx)(t.code,{children:"spgPickSplitIn"})," C struct, containing input data for the function. The second argument is a pointer to a ",(0,o.jsx)(t.code,{children:"spgPickSplitOut"})," C struct, which the function must fill with result data."]}),"\n",(0,o.jsx)(t.pre,{children:(0,o.jsx)(t.code,{children:"typedef struct spgPickSplitIn\n{\n    int         nTuples;        /* number of leaf tuples */\n    Datum      *datums;         /* their datums (array of length nTuples) */\n    int         level;          /* current level (counting from zero) */\n} spgPickSplitIn;\n\ntypedef struct spgPickSplitOut\n{\n    bool        hasPrefix;      /* new inner tuple should have a prefix? */\n    Datum       prefixDatum;    /* if so, its value */\n\n    int         nNodes;         /* number of nodes for new inner tuple */\n    Datum      *nodeLabels;     /* their labels (or NULL for no labels) */\n\n    int        *mapTuplesToNodes;   /* node index for each leaf tuple */\n    Datum      *leafTupleDatums;    /* datum to store in each new leaf tuple */\n} spgPickSplitOut;\n"})}),"\n",(0,o.jsxs)(t.p,{children:[(0,o.jsx)(t.code,{children:"nTuples"})," is the number of leaf tuples provided. ",(0,o.jsx)(t.code,{children:"datums"})," is an array of their datum values of ",(0,o.jsx)(t.code,{children:"spgConfigOut"}),".",(0,o.jsx)(t.code,{children:"leafType"})," type. ",(0,o.jsx)(t.code,{children:"level"})," is the current level that all the leaf tuples share, which will become the level of the new inner tuple."]}),"\n",(0,o.jsxs)(t.p,{children:["Set ",(0,o.jsx)(t.code,{children:"hasPrefix"})," to indicate whether the new inner tuple should have a prefix, and if so set ",(0,o.jsx)(t.code,{children:"prefixDatum"})," to the prefix value. Set ",(0,o.jsx)(t.code,{children:"nNodes"})," to indicate the number of nodes that the new inner tuple will contain, and set ",(0,o.jsx)(t.code,{children:"nodeLabels"})," to an array of their label values, or to NULL if node labels are not required. Set ",(0,o.jsx)(t.code,{children:"mapTuplesToNodes"})," to an array that gives the index (from zero) of the node that each leaf tuple should be assigned to. Set ",(0,o.jsx)(t.code,{children:"leafTupleDatums"})," to an array of the values to be stored in the new leaf tuples (these will be the same as the input ",(0,o.jsx)(t.code,{children:"datums"})," if the operator class does not modify datums from one level to the next). Note that the ",(0,o.jsx)(t.code,{children:"picksplit"})," function is responsible for palloc'ing the ",(0,o.jsx)(t.code,{children:"nodeLabels"}),", ",(0,o.jsx)(t.code,{children:"mapTuplesToNodes"})," and ",(0,o.jsx)(t.code,{children:"leafTupleDatums"})," arrays."]}),"\n",(0,o.jsxs)(t.p,{children:["If more than one leaf tuple is supplied, it is expected that the ",(0,o.jsx)(t.code,{children:"picksplit"})," function will classify them into more than one node; otherwise it is not possible to split the leaf tuples across multiple pages, which is the ultimate purpose of this operation. Therefore, if the ",(0,o.jsx)(t.code,{children:"picksplit"})," function ends up placing all the leaf tuples in the same node, the core SP-GiST code will override that decision and generate an inner tuple in which the leaf tuples are assigned at random to several identically-labeled nodes. Such a tuple is marked ",(0,o.jsx)(t.code,{children:"allTheSame"})," to signify that this has happened. The ",(0,o.jsx)(t.code,{children:"choose"})," and ",(0,o.jsx)(t.code,{children:"inner_consistent"})," functions must take suitable care with such inner tuples. See ",(0,o.jsx)(t.a,{href:"spgist-implementation.html#SPGIST-ALL-THE-SAME",title:"69.4.3.\xa0\u201cAll-the-Same\u201d Inner Tuples",children:"Section 69.4.3"})," for more information."]}),"\n",(0,o.jsxs)(t.p,{children:[(0,o.jsx)(t.code,{children:"picksplit"})," can be applied to a single leaf tuple only in the case that the ",(0,o.jsx)(t.code,{children:"config"})," function set ",(0,o.jsx)(t.code,{children:"longValuesOK"})," to true and a larger-than-a-page input value has been supplied. In this case the point of the operation is to strip off a prefix and produce a new, shorter leaf datum value. The call will be repeated until a leaf datum short enough to fit on a page has been produced. See ",(0,o.jsx)(t.a,{href:"spgist-implementation.html#SPGIST-LIMITS",title:"69.4.1.\xa0SP-GiST Limits",children:"Section 69.4.1"})," for more information."]}),"\n",(0,o.jsx)(t.p,{children:(0,o.jsx)(t.code,{children:"inner_consistent"})}),"\n",(0,o.jsx)(t.p,{children:"Returns set of nodes (branches) to follow during tree search."}),"\n",(0,o.jsx)(t.p,{children:"The SQL declaration of the function must look like this:"}),"\n",(0,o.jsx)(t.pre,{children:(0,o.jsx)(t.code,{children:"CREATE FUNCTION my_inner_consistent(internal, internal) RETURNS void ...\n"})}),"\n",(0,o.jsxs)(t.p,{children:["The first argument is a pointer to a ",(0,o.jsx)(t.code,{children:"spgInnerConsistentIn"})," C struct, containing input data for the function. The second argument is a pointer to a ",(0,o.jsx)(t.code,{children:"spgInnerConsistentOut"})," C struct, which the function must fill with result data."]}),"\n",(0,o.jsx)(t.pre,{children:(0,o.jsx)(t.code,{children:"typedef struct spgInnerConsistentIn\n{\n    ScanKey     scankeys;       /* array of operators and comparison values */\n    ScanKey     orderbys;       /* array of ordering operators and comparison\n                                 * values */\n    int         nkeys;          /* length of scankeys array */\n    int         norderbys;      /* length of orderbys array */\n\n    Datum       reconstructedValue;     /* value reconstructed at parent */\n    void       *traversalValue; /* opclass-specific traverse value */\n    MemoryContext traversalMemoryContext;   /* put new traverse values here */\n    int         level;          /* current level (counting from zero) */\n    bool        returnData;     /* original data must be returned? */\n\n    /* Data from current inner tuple */\n    bool        allTheSame;     /* tuple is marked all-the-same? */\n    bool        hasPrefix;      /* tuple has a prefix? */\n    Datum       prefixDatum;    /* if so, the prefix value */\n    int         nNodes;         /* number of nodes in the inner tuple */\n    Datum      *nodeLabels;     /* node label values (NULL if none) */\n} spgInnerConsistentIn;\n\ntypedef struct spgInnerConsistentOut\n{\n    int         nNodes;         /* number of child nodes to be visited */\n    int        *nodeNumbers;    /* their indexes in the node array */\n    int        *levelAdds;      /* increment level by this much for each */\n    Datum      *reconstructedValues;    /* associated reconstructed values */\n    void      **traversalValues;        /* opclass-specific traverse values */\n    double    **distances;              /* associated distances */\n} spgInnerConsistentOut;\n"})}),"\n",(0,o.jsxs)(t.p,{children:["The array ",(0,o.jsx)(t.code,{children:"scankeys"}),", of length ",(0,o.jsx)(t.code,{children:"nkeys"}),", describes the index search condition(s). These conditions are combined with AND \u2014 only index entries that satisfy all of them are interesting. (Note that ",(0,o.jsx)(t.code,{children:"nkeys"})," = 0 implies that all index entries satisfy the query.) Usually the consistent function only cares about the ",(0,o.jsx)(t.code,{children:"sk_strategy"})," and ",(0,o.jsx)(t.code,{children:"sk_argument"})," fields of each array entry, which respectively give the indexable operator and comparison value. In particular it is not necessary to check ",(0,o.jsx)(t.code,{children:"sk_flags"})," to see if the comparison value is NULL, because the SP-GiST core code will filter out such conditions. The array ",(0,o.jsx)(t.code,{children:"orderbys"}),", of length ",(0,o.jsx)(t.code,{children:"norderbys"}),", describes ordering operators (if any) in the same manner. ",(0,o.jsx)(t.code,{children:"reconstructedValue"})," is the value reconstructed for the parent tuple; it is ",(0,o.jsx)(t.code,{children:"(Datum) 0"})," at the root level or if the ",(0,o.jsx)(t.code,{children:"inner_consistent"})," function did not provide a value at the parent level. ",(0,o.jsx)(t.code,{children:"traversalValue"})," is a pointer to any traverse data passed down from the previous call of ",(0,o.jsx)(t.code,{children:"inner_consistent"})," on the parent index tuple, or NULL at the root level. ",(0,o.jsx)(t.code,{children:"traversalMemoryContext"})," is the memory context in which to store output traverse values (see below). ",(0,o.jsx)(t.code,{children:"level"})," is the current inner tuple's level, starting at zero for the root level. ",(0,o.jsx)(t.code,{children:"returnData"})," is ",(0,o.jsx)(t.code,{children:"true"})," if reconstructed data is required for this query; this will only be so if the ",(0,o.jsx)(t.code,{children:"config"})," function asserted ",(0,o.jsx)(t.code,{children:"canReturnData"}),". ",(0,o.jsx)(t.code,{children:"allTheSame"})," is true if the current inner tuple is marked \u201call-the-same\u201d; in this case all the nodes have the same label (if any) and so either all or none of them match the query (see ",(0,o.jsx)(t.a,{href:"spgist-implementation.html#SPGIST-ALL-THE-SAME",title:"69.4.3.\xa0\u201cAll-the-Same\u201d Inner Tuples",children:"Section 69.4.3"}),"). ",(0,o.jsx)(t.code,{children:"hasPrefix"})," is true if the current inner tuple contains a prefix; if so, ",(0,o.jsx)(t.code,{children:"prefixDatum"})," is its value. ",(0,o.jsx)(t.code,{children:"nNodes"})," is the number of child nodes contained in the inner tuple, and ",(0,o.jsx)(t.code,{children:"nodeLabels"})," is an array of their label values, or NULL if the nodes do not have labels."]}),"\n",(0,o.jsxs)(t.p,{children:[(0,o.jsx)(t.code,{children:"nNodes"})," must be set to the number of child nodes that need to be visited by the search, and ",(0,o.jsx)(t.code,{children:"nodeNumbers"})," must be set to an array of their indexes. If the operator class keeps track of levels, set ",(0,o.jsx)(t.code,{children:"levelAdds"})," to an array of the level increments required when descending to each node to be visited. (Often these increments will be the same for all the nodes, but that's not necessarily so, so an array is used.) If value reconstruction is needed, set ",(0,o.jsx)(t.code,{children:"reconstructedValues"})," to an array of the values reconstructed for each child node to be visited; otherwise, leave ",(0,o.jsx)(t.code,{children:"reconstructedValues"})," as NULL. The reconstructed values are assumed to be of type ",(0,o.jsx)(t.code,{children:"spgConfigOut"}),".",(0,o.jsx)(t.code,{children:"leafType"}),". (However, since the core system will do nothing with them except possibly copy them, it is sufficient for them to have the same ",(0,o.jsx)(t.code,{children:"typlen"})," and ",(0,o.jsx)(t.code,{children:"typbyval"})," properties as ",(0,o.jsx)(t.code,{children:"leafType"}),".) If ordered search is performed, set ",(0,o.jsx)(t.code,{children:"distances"})," to an array of distance values according to ",(0,o.jsx)(t.code,{children:"orderbys"})," array (nodes with lowest distances will be processed first). Leave it NULL otherwise. If it is desired to pass down additional out-of-band information (\u201ctraverse values\u201d) to lower levels of the tree search, set ",(0,o.jsx)(t.code,{children:"traversalValues"})," to an array of the appropriate traverse values, one for each child node to be visited; otherwise, leave ",(0,o.jsx)(t.code,{children:"traversalValues"})," as NULL. Note that the ",(0,o.jsx)(t.code,{children:"inner_consistent"})," function is responsible for palloc'ing the ",(0,o.jsx)(t.code,{children:"nodeNumbers"}),", ",(0,o.jsx)(t.code,{children:"levelAdds"}),", ",(0,o.jsx)(t.code,{children:"distances"}),", ",(0,o.jsx)(t.code,{children:"reconstructedValues"}),", and ",(0,o.jsx)(t.code,{children:"traversalValues"})," arrays in the current memory context. However, any output traverse values pointed to by the ",(0,o.jsx)(t.code,{children:"traversalValues"})," array should be allocated in ",(0,o.jsx)(t.code,{children:"traversalMemoryContext"}),". Each traverse value must be a single palloc'd chunk."]}),"\n",(0,o.jsx)(t.p,{children:(0,o.jsx)(t.code,{children:"leaf_consistent"})}),"\n",(0,o.jsx)(t.p,{children:"Returns true if a leaf tuple satisfies a query."}),"\n",(0,o.jsx)(t.p,{children:"The SQL declaration of the function must look like this:"}),"\n",(0,o.jsx)(t.pre,{children:(0,o.jsx)(t.code,{children:"CREATE FUNCTION my_leaf_consistent(internal, internal) RETURNS bool ...\n"})}),"\n",(0,o.jsxs)(t.p,{children:["The first argument is a pointer to a ",(0,o.jsx)(t.code,{children:"spgLeafConsistentIn"})," C struct, containing input data for the function. The second argument is a pointer to a ",(0,o.jsx)(t.code,{children:"spgLeafConsistentOut"})," C struct, which the function must fill with result data."]}),"\n",(0,o.jsx)(t.pre,{children:(0,o.jsx)(t.code,{children:"typedef struct spgLeafConsistentIn\n{\n    ScanKey     scankeys;       /* array of operators and comparison values */\n    ScanKey     orderbys;       /* array of ordering operators and comparison\n                                 * values */\n    int         nkeys;          /* length of scankeys array */\n    int         norderbys;      /* length of orderbys array */\n\n    Datum       reconstructedValue;     /* value reconstructed at parent */\n    void       *traversalValue; /* opclass-specific traverse value */\n    int         level;          /* current level (counting from zero) */\n    bool        returnData;     /* original data must be returned? */\n\n    Datum       leafDatum;      /* datum in leaf tuple */\n} spgLeafConsistentIn;\n\ntypedef struct spgLeafConsistentOut\n{\n    Datum       leafValue;        /* reconstructed original data, if any */\n    bool        recheck;          /* set true if operator must be rechecked */\n    bool        recheckDistances; /* set true if distances must be rechecked */\n    double     *distances;        /* associated distances */\n} spgLeafConsistentOut;\n"})}),"\n",(0,o.jsxs)(t.p,{children:["The array ",(0,o.jsx)(t.code,{children:"scankeys"}),", of length ",(0,o.jsx)(t.code,{children:"nkeys"}),", describes the index search condition(s). These conditions are combined with AND \u2014 only index entries that satisfy all of them satisfy the query. (Note that ",(0,o.jsx)(t.code,{children:"nkeys"})," = 0 implies that all index entries satisfy the query.) Usually the consistent function only cares about the ",(0,o.jsx)(t.code,{children:"sk_strategy"})," and ",(0,o.jsx)(t.code,{children:"sk_argument"})," fields of each array entry, which respectively give the indexable operator and comparison value. In particular it is not necessary to check ",(0,o.jsx)(t.code,{children:"sk_flags"})," to see if the comparison value is NULL, because the SP-GiST core code will filter out such conditions. The array ",(0,o.jsx)(t.code,{children:"orderbys"}),", of length ",(0,o.jsx)(t.code,{children:"norderbys"}),", describes the ordering operators in the same manner. ",(0,o.jsx)(t.code,{children:"reconstructedValue"})," is the value reconstructed for the parent tuple; it is ",(0,o.jsx)(t.code,{children:"(Datum) 0"})," at the root level or if the ",(0,o.jsx)(t.code,{children:"inner_consistent"})," function did not provide a value at the parent level. ",(0,o.jsx)(t.code,{children:"traversalValue"})," is a pointer to any traverse data passed down from the previous call of ",(0,o.jsx)(t.code,{children:"inner_consistent"})," on the parent index tuple, or NULL at the root level. ",(0,o.jsx)(t.code,{children:"level"})," is the current leaf tuple's level, starting at zero for the root level. ",(0,o.jsx)(t.code,{children:"returnData"})," is ",(0,o.jsx)(t.code,{children:"true"})," if reconstructed data is required for this query; this will only be so if the ",(0,o.jsx)(t.code,{children:"config"})," function asserted ",(0,o.jsx)(t.code,{children:"canReturnData"}),". ",(0,o.jsx)(t.code,{children:"leafDatum"})," is the key value of ",(0,o.jsx)(t.code,{children:"spgConfigOut"}),".",(0,o.jsx)(t.code,{children:"leafType"})," stored in the current leaf tuple."]}),"\n",(0,o.jsxs)(t.p,{children:["The function must return ",(0,o.jsx)(t.code,{children:"true"})," if the leaf tuple matches the query, or ",(0,o.jsx)(t.code,{children:"false"})," if not. In the ",(0,o.jsx)(t.code,{children:"true"})," case, if ",(0,o.jsx)(t.code,{children:"returnData"})," is ",(0,o.jsx)(t.code,{children:"true"})," then ",(0,o.jsx)(t.code,{children:"leafValue"})," must be set to the value (of type ",(0,o.jsx)(t.code,{children:"spgConfigIn"}),".",(0,o.jsx)(t.code,{children:"attType"}),") originally supplied to be indexed for this leaf tuple. Also, ",(0,o.jsx)(t.code,{children:"recheck"})," may be set to ",(0,o.jsx)(t.code,{children:"true"})," if the match is uncertain and so the operator(s) must be re-applied to the actual heap tuple to verify the match. If ordered search is performed, set ",(0,o.jsx)(t.code,{children:"distances"})," to an array of distance values according to ",(0,o.jsx)(t.code,{children:"orderbys"})," array. Leave it NULL otherwise. If at least one of returned distances is not exact, set ",(0,o.jsx)(t.code,{children:"recheckDistances"})," to true. In this case, the executor will calculate the exact distances after fetching the tuple from the heap, and will reorder the tuples if needed."]}),"\n",(0,o.jsx)(t.p,{children:"The optional user-defined methods are:"}),"\n",(0,o.jsx)(t.p,{children:(0,o.jsx)(t.code,{children:"Datum compress(Datum in)"})}),"\n",(0,o.jsxs)(t.p,{children:["Converts a data item into a format suitable for physical storage in a leaf tuple of the index. It accepts a value of type ",(0,o.jsx)(t.code,{children:"spgConfigIn"}),".",(0,o.jsx)(t.code,{children:"attType"})," and returns a value of type ",(0,o.jsx)(t.code,{children:"spgConfigOut"}),".",(0,o.jsx)(t.code,{children:"leafType"}),". The output value must not contain an out-of-line TOAST pointer."]}),"\n",(0,o.jsxs)(t.p,{children:["Note: the ",(0,o.jsx)(t.code,{children:"compress"})," method is only applied to values to be stored. The consistent methods receive query ",(0,o.jsx)(t.code,{children:"scankeys"})," unchanged, without transformation using ",(0,o.jsx)(t.code,{children:"compress"}),"."]}),"\n",(0,o.jsx)(t.p,{children:(0,o.jsx)(t.code,{children:"options"})}),"\n",(0,o.jsx)(t.p,{children:"Defines a set of user-visible parameters that control operator class behavior."}),"\n",(0,o.jsx)(t.p,{children:"The SQL declaration of the function must look like this:"}),"\n",(0,o.jsx)(t.pre,{children:(0,o.jsx)(t.code,{children:"CREATE OR REPLACE FUNCTION my_options(internal)\nRETURNS void\nAS 'MODULE_PATHNAME'\nLANGUAGE C STRICT;\n"})}),"\n",(0,o.jsxs)(t.p,{children:["The function is passed a pointer to a ",(0,o.jsx)(t.code,{children:"local_relopts"})," struct, which needs to be filled with a set of operator class specific options. The options can be accessed from other support functions using the ",(0,o.jsx)(t.code,{children:"PG_HAS_OPCLASS_OPTIONS()"})," and ",(0,o.jsx)(t.code,{children:"PG_GET_OPCLASS_OPTIONS()"})," macros."]}),"\n",(0,o.jsx)(t.p,{children:"Since the representation of the key in SP-GiST is flexible, it may depend on user-specified parameters."}),"\n",(0,o.jsxs)(t.p,{children:["All the SP-GiST support methods are normally called in a short-lived memory context; that is, ",(0,o.jsx)(t.code,{children:"CurrentMemoryContext"})," will be reset after processing of each tuple. It is therefore not very important to worry about pfree'ing everything you palloc. (The ",(0,o.jsx)(t.code,{children:"config"})," method is an exception: it should try to avoid leaking memory. But usually the ",(0,o.jsx)(t.code,{children:"config"})," method need do nothing but assign constants into the passed parameter struct.)"]}),"\n",(0,o.jsxs)(t.p,{children:["If the indexed column is of a collatable data type, the index collation will be passed to all the support methods, using the standard ",(0,o.jsx)(t.code,{children:"PG_GET_COLLATION()"})," mechanism."]})]})}function h(e={}){const{wrapper:t}={...(0,i.R)(),...e.components};return t?(0,o.jsx)(t,{...e,children:(0,o.jsx)(c,{...e})}):c(e)}},28453:(e,t,n)=>{n.d(t,{R:()=>r,x:()=>a});var s=n(96540);const o={},i=s.createContext(o);function r(e){const t=s.useContext(i);return s.useMemo(function(){return"function"==typeof e?e(t):{...t,...e}},[t,e])}function a(e){let t;return t=e.disableParentContext?"function"==typeof e.components?e.components(o):e.components||o:r(e.components),s.createElement(i.Provider,{value:t},e.children)}}}]);