"use strict";(self.webpackChunkpgdoc_02=self.webpackChunkpgdoc_02||[]).push([[3954],{28453:(e,t,n)=>{n.d(t,{R:()=>s,x:()=>o});var a=n(96540);const i={},r=a.createContext(i);function s(e){const t=a.useContext(r);return a.useMemo(function(){return"function"==typeof e?e(t):{...t,...e}},[t,e])}function o(e){let t;return t=e.disableParentContext?"function"==typeof e.components?e.components(i):e.components||i:s(e.components),a.createElement(r.Provider,{value:t},e.children)}},79605:(e,t,n)=>{n.r(t),n.d(t,{assets:()=>l,contentTitle:()=>o,default:()=>h,frontMatter:()=>s,metadata:()=>a,toc:()=>c});const a=JSON.parse('{"id":"V. Server Programming/Extending SQL/xaggr","title":"38.12. User-Defined Aggregates","description":"Supported versions: 13 / 14 / 15 / 16 / 17","source":"@site/docs/V. Server Programming/38. Extending SQL/xaggr.md","sourceDirName":"V. Server Programming/38. Extending SQL","slug":"/V. Server Programming/Extending SQL/xaggr","permalink":"/demo_docu/V. Server Programming/Extending SQL/xaggr","draft":false,"unlisted":false,"tags":[],"version":"current","frontMatter":{"title":"38.12. User-Defined Aggregates"},"sidebar":"autoGeneratedSidebar","previous":{"title":"38.2. The PostgreSQL Type System","permalink":"/demo_docu/V. Server Programming/Extending SQL/extend-type-system"},"next":{"title":"38.10. C-Language Functions","permalink":"/demo_docu/V. Server Programming/Extending SQL/xfunc-c"}}');var i=n(74848),r=n(28453);const s={title:"38.12. User-Defined Aggregates"},o=void 0,l={},c=[{value:"Note",id:"note",level:3},{value:"38.12.1. Moving-Aggregate Mode",id:"38121-moving-aggregate-mode",level:3},{value:"38.12.2. Polymorphic and Variadic Aggregates",id:"38122-polymorphic-and-variadic-aggregates",level:3},{value:"Note",id:"note-1",level:3},{value:"38.12.3. Ordered-Set Aggregates",id:"38123-ordered-set-aggregates",level:3},{value:"38.12.4. Partial Aggregation",id:"38124-partial-aggregation",level:3},{value:"38.12.5. Support Functions for Aggregates",id:"38125-support-functions-for-aggregates",level:3}];function d(e){const t={a:"a",code:"code",em:"em",h3:"h3",hr:"hr",p:"p",pre:"pre",...(0,r.R)(),...e.components};return(0,i.jsxs)(i.Fragment,{children:[(0,i.jsx)(t.p,{children:"Supported versions: 13 / 14 / 15 / 16 / 17"}),"\n",(0,i.jsx)(t.p,{children:"Development versions: 18 / devel"}),"\n",(0,i.jsx)(t.p,{children:"Unsupported versions: 10 / 11 / 12 / 7.1 / 7.2 / 7.3 / 7.4 / 8.0 / 8.1 / 8.2 / 8.3 / 8.4 / 9.0 / 9.1 / 9.2 / 9.3 / 9.4 / 9.5 / 9.6"}),"\n",(0,i.jsx)(t.hr,{}),"\n",(0,i.jsx)(t.p,{children:(0,i.jsx)(t.a,{href:"/demo_docu/V.%20Server%20Programming/Extending%20SQL/xaggr#38121-moving-aggregate-mode",children:"38.12.1. Moving-Aggregate Mode"})}),"\n",(0,i.jsx)(t.p,{children:(0,i.jsx)(t.a,{href:"/demo_docu/V.%20Server%20Programming/Extending%20SQL/xaggr#38122-polymorphic-and-variadic-aggregates",children:"38.12.2. Polymorphic and Variadic Aggregates"})}),"\n",(0,i.jsx)(t.p,{children:(0,i.jsx)(t.a,{href:"/demo_docu/V.%20Server%20Programming/Extending%20SQL/xaggr#38123-ordered-set-aggregates",children:"38.12.3. Ordered-Set Aggregates"})}),"\n",(0,i.jsx)(t.p,{children:(0,i.jsx)(t.a,{href:"/demo_docu/V.%20Server%20Programming/Extending%20SQL/xaggr#38124-partial-aggregation",children:"38.12.4. Partial Aggregation"})}),"\n",(0,i.jsx)(t.p,{children:(0,i.jsx)(t.a,{href:"/demo_docu/V.%20Server%20Programming/Extending%20SQL/xaggr#38125-support-functions-for-aggregates",children:"38.12.5. Support Functions for Aggregates"})}),"\n",(0,i.jsxs)(t.p,{children:["Aggregate functions in PostgreSQL are defined in terms of ",(0,i.jsx)(t.em,{children:"state values"})," and ",(0,i.jsx)(t.em,{children:"state transition functions"}),". That is, an aggregate operates using a state value that is updated as each successive input row is processed. To define a new aggregate function, one selects a data type for the state value, an initial value for the state, and a state transition function. The state transition function takes the previous state value and the aggregate's input value(s) for the current row, and returns a new state value. A ",(0,i.jsx)(t.em,{children:"final function"})," can also be specified, in case the desired result of the aggregate is different from the data that needs to be kept in the running state value. The final function takes the ending state value and returns whatever is wanted as the aggregate result. In principle, the transition and final functions are just ordinary functions that could also be used outside the context of the aggregate. (In practice, it's often helpful for performance reasons to create specialized transition functions that can only work when called as part of an aggregate.)"]}),"\n",(0,i.jsx)(t.p,{children:"Thus, in addition to the argument and result data types seen by a user of the aggregate, there is an internal state-value data type that might be different from both the argument and result types."}),"\n",(0,i.jsxs)(t.p,{children:["If we define an aggregate that does not use a final function, we have an aggregate that computes a running function of the column values from each row. ",(0,i.jsx)(t.code,{children:"sum"})," is an example of this kind of aggregate. ",(0,i.jsx)(t.code,{children:"sum"})," starts at zero and always adds the current row's value to its running total. For example, if we want to make a ",(0,i.jsx)(t.code,{children:"sum"})," aggregate to work on a data type for complex numbers, we only need the addition function for that data type. The aggregate definition would be:"]}),"\n",(0,i.jsx)(t.pre,{children:(0,i.jsx)(t.code,{children:"CREATE AGGREGATE sum (complex)\n(\n    sfunc = complex_add,\n    stype = complex,\n    initcond = '(0,0)'\n);\n"})}),"\n",(0,i.jsx)(t.p,{children:"which we might use like this:"}),"\n",(0,i.jsx)(t.pre,{children:(0,i.jsx)(t.code,{children:"SELECT sum(a) FROM test_complex;\n\n   sum\n-----------\n (34,53.9)\n"})}),"\n",(0,i.jsxs)(t.p,{children:["(Notice that we are relying on function overloading: there is more than one aggregate named ",(0,i.jsx)(t.code,{children:"sum"}),", but PostgreSQL can figure out which kind of sum applies to a column of type ",(0,i.jsx)(t.code,{children:"complex"}),".)"]}),"\n",(0,i.jsxs)(t.p,{children:["The above definition of ",(0,i.jsx)(t.code,{children:"sum"})," will return zero (the initial state value) if there are no nonnull input values. Perhaps we want to return null in that case instead \u2014 the SQL standard expects ",(0,i.jsx)(t.code,{children:"sum"})," to behave that way. We can do this simply by omitting the ",(0,i.jsx)(t.code,{children:"initcond"})," phrase, so that the initial state value is null. Ordinarily this would mean that the ",(0,i.jsx)(t.code,{children:"sfunc"})," would need to check for a null state-value input. But for ",(0,i.jsx)(t.code,{children:"sum"})," and some other simple aggregates like ",(0,i.jsx)(t.code,{children:"max"})," and ",(0,i.jsx)(t.code,{children:"min"}),", it is sufficient to insert the first nonnull input value into the state variable and then start applying the transition function at the second nonnull input value. PostgreSQL will do that automatically if the initial state value is null and the transition function is marked \u201cstrict\u201d (i.e., not to be called for null inputs)."]}),"\n",(0,i.jsx)(t.p,{children:"Another bit of default behavior for a \u201cstrict\u201d transition function is that the previous state value is retained unchanged whenever a null input value is encountered. Thus, null values are ignored. If you need some other behavior for null inputs, do not declare your transition function as strict; instead code it to test for null inputs and do whatever is needed."}),"\n",(0,i.jsxs)(t.p,{children:[(0,i.jsx)(t.code,{children:"avg"})," (average) is a more complex example of an aggregate. It requires two pieces of running state: the sum of the inputs and the count of the number of inputs. The final result is obtained by dividing these quantities. Average is typically implemented by using an array as the state value. For example, the built-in implementation of ",(0,i.jsx)(t.code,{children:"avg(float8)"})," looks like:"]}),"\n",(0,i.jsx)(t.pre,{children:(0,i.jsx)(t.code,{children:"CREATE AGGREGATE avg (float8)\n(\n    sfunc = float8_accum,\n    stype = float8[],\n    finalfunc = float8_avg,\n    initcond = '{0,0,0}'\n);\n"})}),"\n",(0,i.jsx)(t.h3,{id:"note",children:"Note"}),"\n",(0,i.jsxs)(t.p,{children:[(0,i.jsx)(t.code,{children:"float8_accum"})," requires a three-element array, not just two elements, because it accumulates the sum of squares as well as the sum and count of the inputs. This is so that it can be used for some other aggregates as well as ",(0,i.jsx)(t.code,{children:"avg"}),"."]}),"\n",(0,i.jsxs)(t.p,{children:["Aggregate function calls in SQL allow ",(0,i.jsx)(t.code,{children:"DISTINCT"})," and ",(0,i.jsx)(t.code,{children:"ORDER BY"})," options that control which rows are fed to the aggregate's transition function and in what order. These options are implemented behind the scenes and are not the concern of the aggregate's support functions."]}),"\n",(0,i.jsxs)(t.p,{children:["For further details see the ",(0,i.jsx)(t.a,{href:"/VI.%20Reference/I.%20SQL%20Commands/sql-createaggregate.md",children:"CREATE AGGREGATE"})," command."]}),"\n",(0,i.jsx)(t.h3,{id:"38121-moving-aggregate-mode",children:"38.12.1. Moving-Aggregate Mode"}),"\n",(0,i.jsxs)(t.p,{children:["Aggregate functions can optionally support ",(0,i.jsx)(t.em,{children:"moving-aggregate mode"})," , which allows substantially faster execution of aggregate functions within windows with moving frame starting points. (See ",(0,i.jsx)(t.a,{href:"/I.%20Tutorial/3.%20Advanced%20Features/tutorial-window.md",children:"Section 3.5"})," for information about use of aggregate functions as window functions.) The basic idea is that in addition to a normal \u201cforward\u201d transition function, the aggregate provides an ",(0,i.jsx)(t.em,{children:"inverse transition function"})," , which allows rows to be removed from the aggregate's running state value when they exit the window frame. For example a ",(0,i.jsx)(t.code,{children:"sum"})," aggregate, which uses addition as the forward transition function, would use subtraction as the inverse transition function. Without an inverse transition function, the window function mechanism must recalculate the aggregate from scratch each time the frame starting point moves, resulting in run time proportional to the number of input rows times the average frame length. With an inverse transition function, the run time is only proportional to the number of input rows."]}),"\n",(0,i.jsx)(t.p,{children:"The inverse transition function is passed the current state value and the aggregate input value(s) for the earliest row included in the current state. It must reconstruct what the state value would have been if the given input row had never been aggregated, but only the rows following it. This sometimes requires that the forward transition function keep more state than is needed for plain aggregation mode. Therefore, the moving-aggregate mode uses a completely separate implementation from the plain mode: it has its own state data type, its own forward transition function, and its own final function if needed. These can be the same as the plain mode's data type and functions, if there is no need for extra state."}),"\n",(0,i.jsxs)(t.p,{children:["As an example, we could extend the ",(0,i.jsx)(t.code,{children:"sum"})," aggregate given above to support moving-aggregate mode like this:"]}),"\n",(0,i.jsx)(t.pre,{children:(0,i.jsx)(t.code,{children:"CREATE AGGREGATE sum (complex)\n(\n    sfunc = complex_add,\n    stype = complex,\n    initcond = '(0,0)',\n    msfunc = complex_add,\n    minvfunc = complex_sub,\n    mstype = complex,\n    minitcond = '(0,0)'\n);\n"})}),"\n",(0,i.jsxs)(t.p,{children:["The parameters whose names begin with ",(0,i.jsx)(t.code,{children:"m"})," define the moving-aggregate implementation. Except for the inverse transition function ",(0,i.jsx)(t.code,{children:"minvfunc"}),", they correspond to the plain-aggregate parameters without ",(0,i.jsx)(t.code,{children:"m"}),"."]}),"\n",(0,i.jsxs)(t.p,{children:["The forward transition function for moving-aggregate mode is not allowed to return null as the new state value. If the inverse transition function returns null, this is taken as an indication that the inverse function cannot reverse the state calculation for this particular input, and so the aggregate calculation will be redone from scratch for the current frame starting position. This convention allows moving-aggregate mode to be used in situations where there are some infrequent cases that are impractical to reverse out of the running state value. The inverse transition function can \u201cpunt\u201d on these cases, and yet still come out ahead so long as it can work for most cases. As an example, an aggregate working with floating-point numbers might choose to punt when a ",(0,i.jsx)(t.code,{children:"NaN"})," (not a number) input has to be removed from the running state value."]}),"\n",(0,i.jsxs)(t.p,{children:["When writing moving-aggregate support functions, it is important to be sure that the inverse transition function can reconstruct the correct state value exactly. Otherwise there might be user-visible differences in results depending on whether the moving-aggregate mode is used. An example of an aggregate for which adding an inverse transition function seems easy at first, yet where this requirement cannot be met is ",(0,i.jsx)(t.code,{children:"sum"})," over ",(0,i.jsx)(t.code,{children:"float4"})," or ",(0,i.jsx)(t.code,{children:"float8"})," inputs. A naive declaration of ",(0,i.jsx)(t.code,{children:"sum("}),"float8",(0,i.jsx)(t.code,{children:")"})," could be"]}),"\n",(0,i.jsx)(t.pre,{children:(0,i.jsx)(t.code,{children:"CREATE AGGREGATE unsafe_sum (float8)\n(\n    stype = float8,\n    sfunc = float8pl,\n    mstype = float8,\n    msfunc = float8pl,\n    minvfunc = float8mi\n);\n"})}),"\n",(0,i.jsx)(t.p,{children:"This aggregate, however, can give wildly different results than it would have without the inverse transition function. For example, consider"}),"\n",(0,i.jsx)(t.pre,{children:(0,i.jsx)(t.code,{children:"SELECT\n  unsafe_sum(x) OVER (ORDER BY n ROWS BETWEEN CURRENT ROW AND 1 FOLLOWING)\nFROM (VALUES (1, 1.0e20::float8),\n             (2, 1.0::float8)) AS v (n,x);\n"})}),"\n",(0,i.jsxs)(t.p,{children:["This query returns ",(0,i.jsx)(t.code,{children:"0"})," as its second result, rather than the expected answer of ",(0,i.jsx)(t.code,{children:"1"}),". The cause is the limited precision of floating-point values: adding ",(0,i.jsx)(t.code,{children:"1"})," to ",(0,i.jsx)(t.code,{children:"1e20"})," results in ",(0,i.jsx)(t.code,{children:"1e20"})," again, and so subtracting ",(0,i.jsx)(t.code,{children:"1e20"})," from that yields ",(0,i.jsx)(t.code,{children:"0"}),", not ",(0,i.jsx)(t.code,{children:"1"}),". Note that this is a limitation of floating-point arithmetic in general, not a limitation of PostgreSQL."]}),"\n",(0,i.jsx)(t.h3,{id:"38122-polymorphic-and-variadic-aggregates",children:"38.12.2. Polymorphic and Variadic Aggregates"}),"\n",(0,i.jsxs)(t.p,{children:["Aggregate functions can use polymorphic state transition functions or final functions, so that the same functions can be used to implement multiple aggregates. See ",(0,i.jsx)(t.a,{href:"extend-type-system.html#EXTEND-TYPES-POLYMORPHIC",title:"38.2.5.\xa0Polymorphic Types",children:"Section 38.2.5"})," for an explanation of polymorphic functions. Going a step further, the aggregate function itself can be specified with polymorphic input type(s) and state type, allowing a single aggregate definition to serve for multiple input data types. Here is an example of a polymorphic aggregate:"]}),"\n",(0,i.jsx)(t.pre,{children:(0,i.jsx)(t.code,{children:"CREATE AGGREGATE array_accum (anycompatible)\n(\n    sfunc = array_append,\n    stype = anycompatiblearray,\n    initcond = '{}'\n);\n"})}),"\n",(0,i.jsxs)(t.p,{children:["Here, the actual state type for any given aggregate call is the array type having the actual input type as elements. The behavior of the aggregate is to concatenate all the inputs into an array of that type. (Note: the built-in aggregate ",(0,i.jsx)(t.code,{children:"array_agg"})," provides similar functionality, with better performance than this definition would have.)"]}),"\n",(0,i.jsx)(t.p,{children:"Here's the output using two different actual data types as arguments:"}),"\n",(0,i.jsx)(t.pre,{children:(0,i.jsx)(t.code,{children:"SELECT attrelid::regclass, array_accum(attname)\n    FROM pg_attribute\n    WHERE attnum > 0 AND attrelid = 'pg_tablespace'::regclass\n    GROUP BY attrelid;\n\n   attrelid    |              array_accum\n---------------+---------------------------------------\n pg_tablespace | {spcname,spcowner,spcacl,spcoptions}\n(1 row)\n\nSELECT attrelid::regclass, array_accum(atttypid::regtype)\n    FROM pg_attribute\n    WHERE attnum > 0 AND attrelid = 'pg_tablespace'::regclass\n    GROUP BY attrelid;\n\n   attrelid    |        array_accum\n---------------+---------------------------\n pg_tablespace | {name,oid,aclitem[],text[]}\n(1 row)\n"})}),"\n",(0,i.jsxs)(t.p,{children:["Ordinarily, an aggregate function with a polymorphic result type has a polymorphic state type, as in the above example. This is necessary because otherwise the final function cannot be declared sensibly: it would need to have a polymorphic result type but no polymorphic argument type, which ",(0,i.jsx)(t.code,{children:"CREATE FUNCTION"})," will reject on the grounds that the result type cannot be deduced from a call. But sometimes it is inconvenient to use a polymorphic state type. The most common case is where the aggregate support functions are to be written in C and the state type should be declared as ",(0,i.jsx)(t.code,{children:"internal"})," because there is no SQL-level equivalent for it. To address this case, it is possible to declare the final function as taking extra \u201cdummy\u201d arguments that match the input arguments of the aggregate. Such dummy arguments are always passed as null values since no specific value is available when the final function is called. Their only use is to allow a polymorphic final function's result type to be connected to the aggregate's input type(s). For example, the definition of the built-in aggregate ",(0,i.jsx)(t.code,{children:"array_agg"})," is equivalent to"]}),"\n",(0,i.jsx)(t.pre,{children:(0,i.jsx)(t.code,{children:"CREATE FUNCTION array_agg_transfn(internal, anynonarray)\n  RETURNS internal ...;\nCREATE FUNCTION array_agg_finalfn(internal, anynonarray)\n  RETURNS anyarray ...;\n\nCREATE AGGREGATE array_agg (anynonarray)\n(\n    sfunc = array_agg_transfn,\n    stype = internal,\n    finalfunc = array_agg_finalfn,\n    finalfunc_extra\n);\n"})}),"\n",(0,i.jsxs)(t.p,{children:["Here, the ",(0,i.jsx)(t.code,{children:"finalfunc_extra"})," option specifies that the final function receives, in addition to the state value, extra dummy argument(s) corresponding to the aggregate's input argument(s). The extra ",(0,i.jsx)(t.code,{children:"anynonarray"})," argument allows the declaration of ",(0,i.jsx)(t.code,{children:"array_agg_finalfn"})," to be valid."]}),"\n",(0,i.jsxs)(t.p,{children:["An aggregate function can be made to accept a varying number of arguments by declaring its last argument as a ",(0,i.jsx)(t.code,{children:"VARIADIC"})," array, in much the same fashion as for regular functions; see ",(0,i.jsx)(t.a,{href:"xfunc-sql.html#XFUNC-SQL-VARIADIC-FUNCTIONS",title:"38.5.6.\xa0SQL Functions with Variable Numbers of Arguments",children:"Section 38.5.6"}),". The aggregate's transition function(s) must have the same array type as their last argument. The transition function(s) typically would also be marked ",(0,i.jsx)(t.code,{children:"VARIADIC"}),", but this is not strictly required."]}),"\n",(0,i.jsx)(t.h3,{id:"note-1",children:"Note"}),"\n",(0,i.jsxs)(t.p,{children:["Variadic aggregates are easily misused in connection with the ",(0,i.jsx)(t.code,{children:"ORDER BY"})," option (see ",(0,i.jsx)(t.a,{href:"sql-expressions.html#SYNTAX-AGGREGATES",title:"4.2.7.\xa0Aggregate Expressions",children:"Section 4.2.7"}),"), since the parser cannot tell whether the wrong number of actual arguments have been given in such a combination. Keep in mind that everything to the right of ",(0,i.jsx)(t.code,{children:"ORDER BY"})," is a sort key, not an argument to the aggregate. For example, in"]}),"\n",(0,i.jsx)(t.pre,{children:(0,i.jsx)(t.code,{children:"SELECT myaggregate(a ORDER BY a, b, c) FROM ...\n"})}),"\n",(0,i.jsx)(t.p,{children:"the parser will see this as a single aggregate function argument and three sort keys. However, the user might have intended"}),"\n",(0,i.jsx)(t.pre,{children:(0,i.jsx)(t.code,{children:"SELECT myaggregate(a, b, c ORDER BY a) FROM ...\n"})}),"\n",(0,i.jsxs)(t.p,{children:["If ",(0,i.jsx)(t.code,{children:"myaggregate"})," is variadic, both these calls could be perfectly valid."]}),"\n",(0,i.jsx)(t.p,{children:"For the same reason, it's wise to think twice before creating aggregate functions with the same names and different numbers of regular arguments."}),"\n",(0,i.jsx)(t.h3,{id:"38123-ordered-set-aggregates",children:"38.12.3. Ordered-Set Aggregates"}),"\n",(0,i.jsxs)(t.p,{children:["The aggregates we have been describing so far are \u201cnormal\u201d aggregates. PostgreSQL also supports ",(0,i.jsx)(t.em,{children:"ordered-set aggregates"})," , which differ from normal aggregates in two key ways. First, in addition to ordinary aggregated arguments that are evaluated once per input row, an ordered-set aggregate can have \u201cdirect\u201d arguments that are evaluated only once per aggregation operation. Second, the syntax for the ordinary aggregated arguments specifies a sort ordering for them explicitly. An ordered-set aggregate is usually used to implement a computation that depends on a specific row ordering, for instance rank or percentile, so that the sort ordering is a required aspect of any call. For example, the built-in definition of ",(0,i.jsx)(t.code,{children:"percentile_disc"})," is equivalent to:"]}),"\n",(0,i.jsx)(t.pre,{children:(0,i.jsx)(t.code,{children:"CREATE FUNCTION ordered_set_transition(internal, anyelement)\n  RETURNS internal ...;\nCREATE FUNCTION percentile_disc_final(internal, float8, anyelement)\n  RETURNS anyelement ...;\n\nCREATE AGGREGATE percentile_disc (float8 ORDER BY anyelement)\n(\n    sfunc = ordered_set_transition,\n    stype = internal,\n    finalfunc = percentile_disc_final,\n    finalfunc_extra\n);\n"})}),"\n",(0,i.jsxs)(t.p,{children:["This aggregate takes a ",(0,i.jsx)(t.code,{children:"float8"})," direct argument (the percentile fraction) and an aggregated input that can be of any sortable data type. It could be used to obtain a median household income like this:"]}),"\n",(0,i.jsx)(t.pre,{children:(0,i.jsx)(t.code,{children:"SELECT percentile_disc(0.5) WITHIN GROUP (ORDER BY income) FROM households;\n percentile_disc\n-----------------\n           50489\n"})}),"\n",(0,i.jsxs)(t.p,{children:["Here, ",(0,i.jsx)(t.code,{children:"0.5"})," is a direct argument; it would make no sense for the percentile fraction to be a value varying across rows."]}),"\n",(0,i.jsxs)(t.p,{children:["Unlike the case for normal aggregates, the sorting of input rows for an ordered-set aggregate is ",(0,i.jsx)(t.em,{children:"not"})," done behind the scenes, but is the responsibility of the aggregate's support functions. The typical implementation approach is to keep a reference to a \u201ctuplesort\u201d object in the aggregate's state value, feed the incoming rows into that object, and then complete the sorting and read out the data in the final function. This design allows the final function to perform special operations such as injecting additional \u201chypothetical\u201d rows into the data to be sorted. While normal aggregates can often be implemented with support functions written in PL/pgSQL or another PL language, ordered-set aggregates generally have to be written in C, since their state values aren't definable as any SQL data type. (In the above example, notice that the state value is declared as type ",(0,i.jsx)(t.code,{children:"internal"})," \u2014 this is typical.) Also, because the final function performs the sort, it is not possible to continue adding input rows by executing the transition function again later. This means the final function is not ",(0,i.jsx)(t.code,{children:"READ_ONLY"}),"; it must be declared in ",(0,i.jsx)(t.a,{href:"/VI.%20Reference/I.%20SQL%20Commands/sql-createaggregate.md",children:(0,i.jsx)(t.code,{children:"CREATE AGGREGATE"})})," as ",(0,i.jsx)(t.code,{children:"READ_WRITE"}),", or as ",(0,i.jsx)(t.code,{children:"SHAREABLE"})," if it's possible for additional final-function calls to make use of the already-sorted state."]}),"\n",(0,i.jsxs)(t.p,{children:["The state transition function for an ordered-set aggregate receives the current state value plus the aggregated input values for each row, and returns the updated state value. This is the same definition as for normal aggregates, but note that the direct arguments (if any) are not provided. The final function receives the last state value, the values of the direct arguments if any, and (if ",(0,i.jsx)(t.code,{children:"finalfunc_extra"})," is specified) null values corresponding to the aggregated input(s). As with normal aggregates, ",(0,i.jsx)(t.code,{children:"finalfunc_extra"})," is only really useful if the aggregate is polymorphic; then the extra dummy argument(s) are needed to connect the final function's result type to the aggregate's input type(s)."]}),"\n",(0,i.jsx)(t.p,{children:"Currently, ordered-set aggregates cannot be used as window functions, and therefore there is no need for them to support moving-aggregate mode."}),"\n",(0,i.jsx)(t.h3,{id:"38124-partial-aggregation",children:"38.12.4. Partial Aggregation"}),"\n",(0,i.jsxs)(t.p,{children:["Optionally, an aggregate function can support ",(0,i.jsx)(t.em,{children:"partial aggregation"}),". The idea of partial aggregation is to run the aggregate's state transition function over different subsets of the input data independently, and then to combine the state values resulting from those subsets to produce the same state value that would have resulted from scanning all the input in a single operation. This mode can be used for parallel aggregation by having different worker processes scan different portions of a table. Each worker produces a partial state value, and at the end those state values are combined to produce a final state value. (In the future this mode might also be used for purposes such as combining aggregations over local and remote tables; but that is not implemented yet.)"]}),"\n",(0,i.jsxs)(t.p,{children:["To support partial aggregation, the aggregate definition must provide a ",(0,i.jsx)(t.em,{children:"combine function"})," , which takes two values of the aggregate's state type (representing the results of aggregating over two subsets of the input rows) and produces a new value of the state type, representing what the state would have been after aggregating over the combination of those sets of rows. It is unspecified what the relative order of the input rows from the two sets would have been. This means that it's usually impossible to define a useful combine function for aggregates that are sensitive to input row order."]}),"\n",(0,i.jsxs)(t.p,{children:["As simple examples, ",(0,i.jsx)(t.code,{children:"MAX"})," and ",(0,i.jsx)(t.code,{children:"MIN"})," aggregates can be made to support partial aggregation by specifying the combine function as the same greater-of-two or lesser-of-two comparison function that is used as their transition function. ",(0,i.jsx)(t.code,{children:"SUM"})," aggregates just need an addition function as combine function. (Again, this is the same as their transition function, unless the state value is wider than the input data type.)"]}),"\n",(0,i.jsxs)(t.p,{children:["The combine function is treated much like a transition function that happens to take a value of the state type, not of the underlying input type, as its second argument. In particular, the rules for dealing with null values and strict functions are similar. Also, if the aggregate definition specifies a non-null ",(0,i.jsx)(t.code,{children:"initcond"}),", keep in mind that that will be used not only as the initial state for each partial aggregation run, but also as the initial state for the combine function, which will be called to combine each partial result into that state."]}),"\n",(0,i.jsxs)(t.p,{children:["If the aggregate's state type is declared as ",(0,i.jsx)(t.code,{children:"internal"}),", it is the combine function's responsibility that its result is allocated in the correct memory context for aggregate state values. This means in particular that when the first input is ",(0,i.jsx)(t.code,{children:"NULL"})," it's invalid to simply return the second input, as that value will be in the wrong context and will not have sufficient lifespan."]}),"\n",(0,i.jsxs)(t.p,{children:["When the aggregate's state type is declared as ",(0,i.jsx)(t.code,{children:"internal"}),", it is usually also appropriate for the aggregate definition to provide a ",(0,i.jsx)(t.em,{children:"serialization function"})," and a ",(0,i.jsx)(t.em,{children:"deserialization function"})," , which allow such a state value to be copied from one process to another. Without these functions, parallel aggregation cannot be performed, and future applications such as local/remote aggregation will probably not work either."]}),"\n",(0,i.jsxs)(t.p,{children:["A serialization function must take a single argument of type ",(0,i.jsx)(t.code,{children:"internal"})," and return a result of type ",(0,i.jsx)(t.code,{children:"bytea"}),", which represents the state value packaged up into a flat blob of bytes. Conversely, a deserialization function reverses that conversion. It must take two arguments of types ",(0,i.jsx)(t.code,{children:"bytea"})," and ",(0,i.jsx)(t.code,{children:"internal"}),", and return a result of type ",(0,i.jsx)(t.code,{children:"internal"}),". (The second argument is unused and is always zero, but it is required for type-safety reasons.) The result of the deserialization function should simply be allocated in the current memory context, as unlike the combine function's result, it is not long-lived."]}),"\n",(0,i.jsxs)(t.p,{children:["Worth noting also is that for an aggregate to be executed in parallel, the aggregate itself must be marked ",(0,i.jsx)(t.code,{children:"PARALLEL SAFE"}),". The parallel-safety markings on its support functions are not consulted."]}),"\n",(0,i.jsx)(t.h3,{id:"38125-support-functions-for-aggregates",children:"38.12.5. Support Functions for Aggregates"}),"\n",(0,i.jsxs)(t.p,{children:["A function written in C can detect that it is being called as an aggregate support function by calling ",(0,i.jsx)(t.code,{children:"AggCheckCallContext"}),", for example:"]}),"\n",(0,i.jsx)(t.pre,{children:(0,i.jsx)(t.code,{children:"if (AggCheckCallContext(fcinfo, NULL))\n"})}),"\n",(0,i.jsxs)(t.p,{children:["One reason for checking this is that when it is true, the first input must be a temporary state value and can therefore safely be modified in-place rather than allocating a new copy. See ",(0,i.jsx)(t.code,{children:"int8inc()"})," for an example. (While aggregate transition functions are always allowed to modify the transition value in-place, aggregate final functions are generally discouraged from doing so; if they do so, the behavior must be declared when creating the aggregate. See ",(0,i.jsx)(t.a,{href:"/VI.%20Reference/I.%20SQL%20Commands/sql-createaggregate.md",children:"CREATE AGGREGATE"})," for more detail.)"]}),"\n",(0,i.jsxs)(t.p,{children:["The second argument of ",(0,i.jsx)(t.code,{children:"AggCheckCallContext"})," can be used to retrieve the memory context in which aggregate state values are being kept. This is useful for transition functions that wish to use \u201cexpanded\u201d objects (see ",(0,i.jsx)(t.a,{href:"xtypes.html#XTYPES-TOAST",title:"38.13.1.\xa0TOAST Considerations",children:"Section 38.13.1"}),") as their state values. On first call, the transition function should return an expanded object whose memory context is a child of the aggregate state context, and then keep returning the same expanded object on subsequent calls. See ",(0,i.jsx)(t.code,{children:"array_append()"})," for an example. (",(0,i.jsx)(t.code,{children:"array_append()"})," is not the transition function of any built-in aggregate, but it is written to behave efficiently when used as transition function of a custom aggregate.)"]}),"\n",(0,i.jsxs)(t.p,{children:["Another support routine available to aggregate functions written in C is ",(0,i.jsx)(t.code,{children:"AggGetAggref"}),", which returns the ",(0,i.jsx)(t.code,{children:"Aggref"})," parse node that defines the aggregate call. This is mainly useful for ordered-set aggregates, which can inspect the substructure of the ",(0,i.jsx)(t.code,{children:"Aggref"})," node to find out what sort ordering they are supposed to implement. Examples can be found in ",(0,i.jsx)(t.code,{children:"orderedsetaggs.c"})," in the PostgreSQL source code."]})]})}function h(e={}){const{wrapper:t}={...(0,r.R)(),...e.components};return t?(0,i.jsx)(t,{...e,children:(0,i.jsx)(d,{...e})}):d(e)}}}]);